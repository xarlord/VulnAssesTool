NODE_ENV=production
RATE_LIMIT_MAX=1000000
# Isolated E2E data dir (repo-relative; webServer runs from the repo root).
# Keeps E2E runs off the developer's real ~/.vulnassesstool database so seeded
# fixtures are the only data the suite sees.
DATA_DIR=.e2e-data
