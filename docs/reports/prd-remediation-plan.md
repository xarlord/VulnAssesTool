# PRD Remediation Plan — all open requirements

> **Scope note added 2026-08-22.** This plan tracks the **original 59 requirements** (FR-01 …
> FR-11, NFR, SR, CR). It does not cover the requirements added on 2026-08-22 by
> [requirements-gap-analysis-2026-08-22.md](requirements-gap-analysis-2026-08-22.md), because
> those describe work that was **already shipped** — FR-12 … FR-24, SR-01.2, SR-04 and CR-04 are
> retro-specifications of existing code, traced in
> [spec-traceability-matrix.md](spec-traceability-matrix.md), and there is nothing to remediate.
>
> Planned requirements **FR-25 … FR-38 and NFR-09** are likewise out of scope here: they are not
> defects or gaps against a shipped feature, they are unbuilt product. They live in `PRD.md`
> under Planned Functional Requirements.
>
> The requirement count in this document is therefore 59, not the 76 now traced in the matrix.

> **CURRENT STATUS (2026-08-07) — read this first.** ~46 of 59 requirements are DONE. Both
> remaining product features (FR-03.6 auto-refresh, FR-04.3 CVSS temporal metrics) shipped. The
> **only** remaining work is 13 test/coverage/infra items — see
> [Re-validation at HEAD — 2026-08-06](#re-validation-at-head--2026-08-06-post-pr-8-merge) below.
> The `## Summary` counts and per-item labels that follow are the ORIGINAL 2026-07-31 snapshot,
> kept for history — they are stale; trust the re-validation section.

**Generated:** 2026-07-31 from a per-group re-validation of docs/reports/requirement-validation-2026-07-25.md against current HEAD.
**Scope:** 62 sub-requirements audited; **52 still actionable** (10 already fixed since the report).

> Each item was re-checked against HEAD (the 2026-07-25 report is partly stale). Status legend:
> STILL_OPEN = defect reproduced at HEAD · PARTIALLY_FIXED = some bullets closed since report · FIXED_SINCE_REPORT = no longer applicable.

## Summary

- By current status: {"PARTIALLY_FIXED":13,"STILL_OPEN":39,"FIXED_SINCE_REPORT":10}
- By priority: {"High":28,"Medium":26,"Low":8}
- By effort: {"S":32,"M":23,"L":7}
- Product-code changes: 37 of 62

> **The per-item statuses BELOW are stale** (recorded 2026-07-31, before waves 3–6 landed and PR #8 merged). See the re-validation directly below for the actual state at HEAD.

## Re-validation at HEAD — 2026-08-06 (post PR #8 merge)

All 59 items were re-audited against the current code (branch off master, includes merged waves 1–6). **The overwhelming majority are now DONE** — waves 3–6 closed them but the per-item labels below were never updated. Corrected counts:

- **DONE at HEAD: ~44** (every FR/NFR/SR not listed in the gap table below)
- **Genuinely remaining: 15** (4 OPEN, 11 PARTIAL) — the ONLY actionable work:

| ID                  | real status    | remaining work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | kind    | effort |
| ------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | ------ |
| ~~FR-03.6~~         | DONE (c5094d3) | shipped: autoRefreshScheduler.ts (interval + battery guard + new-critical toast), Settings interval select + battery toggle, pauseOnBattery setting, tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | product | L      |
| ~~FR-04.3~~         | DONE (fcf24b8) | shipped: temporal E/RL/RC parsed in cvss/parser.ts + TemporalMetrics block in CvssMetricsGrid.tsx + tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | product | M      |
| NFR-08.4            | PARTIAL        | 2026-08-21: 82 -> **99 scenarios / 473 steps**, all green. `nvd-database.feature` was `@wip` because "no CRUD `nvdDb.ts` module exists" — false: only the _renderer_ lacks one, `server/database/nvdDb.ts` is 1,241 lines. 17 scenarios now drive it (`database.steps.ts`). Remaining `@wip` is genuinely absent behaviour, itemised in `tests/bdd/README.md`                                                                                                                                                                                                                                                                | test    | M      |
| NFR-07.1 / NFR-08.1 | PARTIAL        | 2026-08-21: **95.37 stmts / 89.73 branch / 94.65 func / 96.21 lines**; floors ratcheted 94/88/93/95 -> **95/89/94/96**, so stmts and lines now _enforce_ the PRD 95%. **Functions need only ~30 more covered to reach 95%; branches need ~575** - chase functions. 18 taken (registerCommands.ts 14, projectPersistence.ts 4); covering the command actions found placeholder GitHub URLs shipping in the palette. Do NOT test hybridScanner.ts (declared stub) or src/renderer/lib/export/types.ts (its 3 error classes are imported nowhere) - deleting the dead one raises coverage honestly, testing it is metric-gaming | test    | M      |
| FR-02.3 (c)         | DONE           | VERIFIED: cascade covered by TC-SBOM-004 + neighbours; audit-trail case at ProjectDetail.test.tsx:991                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | test    | S      |
| FR-01.1             | DONE           | VERIFIED: tests/perf/projects.perf.test.ts 'creates, updates, and deletes across 1,000 projects'; now RUN by perf.yml                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | test    | S      |
| FR-10.1             | DONE           | VERIFIED: settings.spec.ts:116 font-size reload persistence; :87-105 system theme via emulateMedia                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | test    | S      |
| NFR-01.1            | DONE           | VERIFIED: e2e/perf 'reaches a usable dashboard within budget on a cold navigation'; now RUN by perf.yml                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | test    | S      |
| NFR-01.3            | DONE           | VERIFIED: tests/perf/scan.perf.test.ts 'realistic-latency 1000-component scan within the 60s API budget'; now RUN by perf.yml                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | test    | S      |
| NFR-01.4            | DONE           | VERIFIED: e2e/perf 'renders the full 100-project list within budget' — was RED (non-hermetic after boot hydration), fixed; now RUN by perf.yml                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | test    | S      |
| NFR-01.6            | DONE           | VERIFIED: e2e/perf 'opens the command palette within budget (median of 7 Ctrl+K presses)'; now RUN by perf.yml                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | test    | S      |
| NFR-02.2            | DONE           | VERIFIED: src/renderer/components/VirtualList.scale.test.tsx — 50,000 items through REAL Virtuoso; runs in the default vitest suite                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | test    | S      |
| NFR-02.3            | DONE           | VERIFIED: tests/perf/nvdSearchScale.perf.test.ts — 1,000,001 rows: count, exact-id, needle-token; now RUN by perf.yml                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | test    | S      |
| NFR-02.5            | DONE           | EXPLAIN-plan assertions for all three search methods; the searchCVEsByText FTS case was the only one missing and is added                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | test    | S      |
| NFR-04.5            | DONE           | a11y gate now scans /audit + /project/:id/fpf (PR #34); ProjectDetail+graph were already covered and there is no VEX route — the original description was wrong                                                                                                                                                                                                                                                                                                                                                                                                                                                              | test    | S      |

### The exit-139 segfault — root-caused 2026-08-21

Logged for months as an unowned intermittent. `ci.yml` blames the macOS runner ("better-sqlite3 +
vitest-worker + v8-coverage combo") and drops macOS from the matrix, but it kept appearing on
ubuntu CI and Windows local. The variable is the **vitest-worker**, not the OS: better-sqlite3 is a
native N-API addon and its handles are finalized during _worker-thread_ teardown. Measured on this
branch, 209 files / 6,163 tests — threads: 4 runs, 1 SIGSEGV + 2 with a failing file + 1 clean;
forks: 3 runs, 3 clean, coverage produced every time, and no slower (181-227s vs 172-238s).
`vitest.config.ts` now uses `pool: 'forks'`. Its `singleThread`/`minThreads`/`maxThreads` keys were
dropped in the same edit: they are not Vitest 4 options, so the 4-worker cap they read as had never
applied. Follow-up worth running: put macOS back in the ci.yml matrix and see if it stays green.

Only **FR-03.6** and **FR-04.3** were product-code features — **both are now shipped** (c5094d3, fcf24b8). **Status re-verified against HEAD on 2026-08-21: 11 of the 13 "remaining" rows were already DONE** — the tests existed, and in six cases nothing ever ran them (see .github/workflows/perf.yml, added for exactly that reason). Verify a row against HEAD before working it; the labels in this document have been wrong far more often than they have been right. Genuinely open: the coverage climb (branch 89.70 -> 95, functions 94.16 -> 95) and BDD scenario breadth.

## Execution sequence (waves — each wave is PR-sized; eslint + build:all + vitest green, then targeted e2e, after each)

**Wave 0 — Verify the 10 FIXED_SINCE_REPORT claims** before skipping them (the re-validation agents may be over-optimistic, mirroring how the original report was over-pessimistic). Spot-check each against HEAD; demote any that aren't actually done back into a wave.

**Wave 1 — Correctness bugs + audit trail (highest value, mostly S):** FR-01.1 (unique-name + updatedAt-on-edit), FR-01.2 (wire `deleteProjectFromServer` cascade), FR-05.1 (trend "unknown" not "stable"), FR-03.3 (OSV response caching), FR-07.1 (emit SCAN/EXPORT audit events from real flows), FR-07.2 (`/audit` route + nav + viewer + date filter), FR-07.3 (audit export reachable), FR-08.3 (close shallow filter coverage), FR-02.1 (CycloneDX XML round-trip e2e).

**Wave 2 — Search / listing / filtering features:** FR-04.1 (CVE-ID search box, user-selectable sort, bulk-select-to-export), FR-08.2 (component patch-availability filter + save/load/delete presets), FR-03.5 (cache-busting UI control).

**Wave 3 — SBOM & scanning depth:** FR-02.2 (wire upload dialog to accept SPDX tag-value/RDF), FR-02.3 (formatVersion + dedup/remove tests), FR-03.1 (version-range fuzzy CPE matching + perf guard, L), FR-03.4 (hybrid API fallback on local miss).

**Wave 4 — Health / Executive / Reporting:** FR-05.3 (comparison color map + tests), FR-06.1 (exploited-vuln count + metrics tests), FR-06.2 (Top-10 critical widget + 6-month trend window), FR-09.2 (PDF exec summary: charts/logo/sorted table/CVSS vector, L), FR-06.3 (dashboard customization, L/Low — candidate to defer).

**Wave 5 — Settings & dependency graph:** FR-10.1–10.5 (theme/font tests, default-profile UI, bandwidth/storage-location, notification-prefs UI, CVSS config), FR-11.1 (graph-gen tests), FR-11.2 residuals (cycle test, size-limit, node-click detail, export-as-image).

**Wave 6 — NFR + Security/CI:** NFR-01.x perf budgets + NFR-02.x scale (benchmark harness with thresholds), NFR-04.4/04.5 (keyboard + WCAG AA), NFR-07.1/08.1 (coverage gates), NFR-07.3 (API docs), NFR-08.4 (BDD), SR-02.1 (Dependabot), SR-02.2 (SAST — CodeQL/Semgrep), SR-01.3 (telemetry consent), NFR-07.2 (eslint zero-warnings — currently deprioritized per CLAUDE.md).

> The full per-item detail (root cause · fix · tests · effort · risk) is in the group sections below.

---

## FR-02 SBOM Import and Parsing

### FR-02.1 — CycloneDX Support (JSON + XML, v1.0-1.6) — close the e2e coverage gap left after the XML crash fix

- **Priority:** High · **Report:** GAP · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** no
- **Since report:** The report's actual defect (parseCycloneDX crashing with `TypeError: Cannot read properties of undefined (reading 'toLowerCase')` at the old cyclonedx.ts:446 mapComponentType, because extractComponentsFromXml treated the fast-xml-parser wrapper object `{ component: [...] }` as a component) is FIXED at HEAD by commit 642e519. Verified by reading src/renderer/lib/parsers/cyclonedx.ts:241-340 (the `isArray: (name) => ['component','vulnerability'].includes(name)` parser option plus extractComponentsFromXml's `.component` unwrap at lines 320-327) and by running the real (unmocked) test suite: `npx vitest run cyclonedx.test.ts spdx.test.ts` -> 109/109 pass. cyclonedx.test.ts:5 now states explicitly 'fast-xml-parser is intentionally NOT mocked'. What survives from the report: the orphaned e2e fixture (e2e/fixtures/sbom-cyclonedx.xml) is still referenced by zero e2e spec (confirmed via repo-wide grep across e2e/features, e2e/workflows, e2e/critical-flows) — so nothing proves the fix holds through the real browser file-picker -> detectFormat -> parseCycloneDX(xml) -> Zustand store -> Components-tab render path. That residual is what this task closes.
- **Root cause:** No product defect remains — this is a test-pyramid gap, not a code gap. src/renderer/lib/parsers/cyclonedx.ts's XML path is correct and unit-tested against real (unmocked) fast-xml-parser output (cyclonedx.test.ts describe('XML format', ...), lines 119-224). But e2e/fixtures/sbom-cyclonedx.xml (2 real components: express@4.18.2, lodash@4.17.21, both with purl+MIT license) has zero e2e spec referencing it, and e2e/workflows/sbom-vulnerability-scan.spec.ts — which already has 'upload CycloneDX SBOM produces parsed components' (JSON, line 32) and 'upload SPDX SBOM produces parsed components' (JSON, line 52) — has no XML sibling. A regression in the browser-integration layer (e.g. the `accept` attribute on SbomUploadDialog.tsx:364, or detectFormat()'s content-sniffing at SbomUploadDialog.tsx:91-121 misclassifying real XML) would not be caught by any current test.
- **Fix:** No production code change is required (the defect is already fixed). Add one e2e test to e2e/workflows/sbom-vulnerability-scan.spec.ts, in the same file/describe block as the existing CycloneDX-JSON and SPDX-JSON upload tests, that uploads e2e/fixtures/sbom-cyclonedx.xml via the existing uploadSbomFile(page, sbomPath) helper (e2e/shared-helpers.ts:330) and asserts on navigateToComponentsTab that both 'express' and 'lodash' render with their exact versions (4.18.2 / 4.17.21), mirroring the assertion style already used at sbom-vulnerability-scan.spec.ts:44-49. Reference the fixture at its current path (e2e/fixtures/sbom-cyclonedx.xml) directly — do not move it into e2e/fixtures/sbom/ (that directory's FIXTURES_DIR constant is a local convenience in that one spec file; moving a fixture used implicitly by its file name elsewhere is unnecessary churn for a one-line path join).
- **Tests:** e2e/workflows/sbom-vulnerability-scan.spec.ts: add `test('upload CycloneDX XML SBOM produces parsed components', ...)` — create project, `uploadSbomFile(page, path.join(import.meta.dirname, '..', 'fixtures', 'sbom-cyclonedx.xml'))`, `navigateToComponentsTab(page)`, then `expect(page.getByText('express').first()).toBeVisible()` and `expect(page.getByText('4.18.2').first()).toBeVisible()` (and the lodash/4.17.21 pair). Before writing it as a permanent regression guard, prove it has teeth by temporarily reverting the `isArray` fix locally (change `isArray: (name) => ['component','vulnerability'].includes(name)` back to also matching 'components'/'vulnerabilities', or otherwise reintroducing the wrapper-object shape) and confirming the new e2e test fails with the same class of error the report described, then restore the fix and confirm it passes — this is what proves the test actually encodes the XML-unwrap business rule rather than just smoke-testing the button click (Rule 9).
- **Risk:** Low. Test-only change against already-fixed, already-unit-tested code. Only risk is e2e flakiness inherent to the existing Playwright harness (same class of risk as the other upload tests in the same file, which already pass reliably).

### FR-02.2 — SPDX Support (2.2/2.3, tag-value + RDF, relationships) — parser done; wire the upload dialog to actually accept the new formats

- **Priority:** High · **Report:** GAP · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** yes
- **Since report:** Both Must-Have bullets the report called unimplemented are now genuinely implemented at HEAD (commits b339a54 'support SPDX tag-value (.spdx) format', 2c5ef5f 'support SPDX RDF/XML format', 88d7f3f 'extract SPDX package relationships into dependencies', 19c4c9e follow-up fixes). Verified by reading src/renderer/lib/parsers/spdx.ts in full: parseSpdx() (line 60) now routes `.spdx/.tag/.tv` to parseSpdxTagValue() (line 111, full tag-value tokenizer at tagValueToSpdxJson, lines 145-276) and `.rdf/.xml` to parseSpdxRdfXml() (line 287, RDF walker at rdfXmlToSpdxJson, lines 426-477); relationships are read via extractComponentsFromSpdxJson/applySpdxRelationships (lines 498-564), mapping DEPENDS_ON/DEPENDENCY_OF into Component.dependencies. spdx.test.ts has three new, non-trivial describe blocks — 'package relationships (FR-02.2)' (line 177), 'tag-value format (FR-02.2)' (line 247), 'RDF/XML format (FR-02.2)' (line 337) — all passing in the real (unmocked) 109/109 run. This closes the report's stated GAP. However, tracing the only user-facing entry point (SbomUploadDialog.tsx) surfaced a new, narrower gap not in the original report: the dialog's file-picker and copy were never updated to match, so the feature is effectively unreachable from the primary upload flow — see rootCause.
- **Root cause:** src/renderer/components/SbomUploadDialog.tsx:364 `<input type="file" accept=".json,.xml,.yaml,.yml" ...>` was not updated when tag-value/RDF parsing landed, so native OS/browser file-open dialogs filter out `.spdx`, `.tag`, `.tv`, and `.rdf` files by default — a user cannot select them through the primary 'Upload SBOM' flow without manually switching their file picker to 'All Files'. The dialog's 'Supported formats' copy (lines 376-382, 'CycloneDX JSON and XML' / 'SPDX JSON') and the helper text under the dropzone (line 372, 'CycloneDX or SPDX JSON files (.json)') also still claim SPDX is JSON-only, so even a user who does select a `.spdx`/`.rdf` file has been told the format isn't supported. The existing dialog-level test SbomUploadDialog.test.tsx:188 'should show file input with correct accept attribute' is a false-confidence test — it only asserts `document.querySelector('input[type="file"]')` is truthy, never reading the `accept` attribute's actual value, so it cannot fail when the attribute is wrong or stale (Rule 9 violation already present in the suite).
- **Fix:** 1. SbomUploadDialog.tsx:364 — widen `accept` to `.json,.xml,.yaml,.yml,.spdx,.tag,.tv,.rdf` (the exact extension set parseSpdx()/parseCycloneDX() branch on; see spdx.ts:60-78). 2. SbomUploadDialog.tsx:372 and 376-382 — update the dropzone helper text and 'Supported formats' list to mention 'SPDX (JSON, tag-value, RDF/XML)' alongside CycloneDX. No change is needed to detectFormat()/handleFileSelect() dispatch logic itself — it already forwards `file.name` unchanged into parseSpdx(content, file.name), which already branches correctly by extension; the gap is purely in what the browser will let a user pick and what the UI claims.
- **Tests:** Strengthen (not duplicate) src/renderer/components/SbomUploadDialog.test.tsx:188 'should show file input with correct accept attribute' — TDD order: (1) first add the real assertion `expect(document.querySelector('input[type="file"]')?.getAttribute('accept')).toContain('.spdx')` and `.toContain('.rdf')` to the existing test; confirm it fails against current HEAD (proving the old test's silence was a real gap, not just paranoia); (2) apply the accept-attribute fix; (3) confirm it passes. This must be a direct DOM/attribute assertion, not a `user.upload()` + success-state assertion — jsdom's userEvent.upload() sets `input.files` directly and does not enforce the `accept` filter the way a real browser does, so an upload-and-assert-success test would pass today even with the wrong `accept` value and would not encode the actual business rule (a user must be able to pick the file in the first place). Do not add a new e2e format test for tag-value/RDF beyond this — SPDX-JSON is already e2e-proven end-to-end (sbom-vulnerability-scan.spec.ts:52) and the tag-value/RDF _parsing_ logic is already proven against real content in spdx.test.ts; a third e2e format test is not the minimum needed to close this specific PRD bullet (the remaining gap is the picker/copy, which a Vitest DOM assertion covers directly and faster).
- **Risk:** Low. Presentation/attribute-only change in one file; no parsing or state-shape changes. Verified no existing test asserts the old accept string's exact value (only the one shallow existence check), so nothing else to update in lockstep.

### FR-02.3 — Multiple SBOM Files per Project — fix mislabeled formatVersion, harden dedup tests, add handleRemoveSbom coverage

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** All three sub-issues the report identified are unchanged at HEAD — none were touched by the FR-02.1/FR-02.2 parser work. Re-verified directly against current source (see rootCause per item).
- **Root cause:** (a) SbomUploadDialog.tsx:231 `formatVersion: parsedData.format === 'cyclonedx' ? '1.5' : '2.3'` ignores the real parsed version — both parsers already return the actual value on `result.metadata.formatVersion` (cyclonedx.ts:222/280; spdx.ts:134) — so e.g. a real CycloneDX 1.2 or SPDX 2.2 file is mislabeled '1.5'/'2.3' in the stored SbomFile metadata. The correct pattern already exists elsewhere in the codebase at src/renderer/components/BinarySbomDialog.tsx:108 (`formatVersion: parsedResult.metadata.formatVersion`) — Rule 11 says follow that established, correct convention rather than re-deriving one. (b) SbomUploadDialog.test.tsx:360-434 'TC-SBOM-005: should merge components...' asserts only `newComponents.length).toBeGreaterThanOrEqual(1)` (line 427) and that `comp-new-1` is present (line 428) — it never asserts the duplicate `comp-existing-1` is ABSENT, so the test stays green even if the dedup filter (SbomUploadDialog.tsx:241, `!existingComponents.some((existing) => existing.id === newComp.id)`) were deleted entirely (length would be 2, still `>=1`). The sibling test 'should not add duplicate components to project' (line 581-605) never clicks 'Add to Project' and never inspects `mockUpdateProject`'s payload at all — it only asserts the parse succeeded, so it cannot fail on a dedup regression by construction. (c) ProjectDetail.tsx:146-199 handleRemoveSbom (cascades SBOM-file removal to its components, strips those component ids from every vulnerability's affectedComponents, drops any vulnerability left with zero affectedComponents, and recomputes statistics) has zero test coverage anywhere — no OverviewTab.test.tsx exists, and ProjectDetail.test.tsx has no reference to RemoveSbom (confirmed via grep).
- **Fix:** (a) In SbomUploadDialog.tsx: add `formatVersion: string` to the `parsedData` state's type (near line 56-60) and set it from `result.metadata.formatVersion` at both places handleFileSelect populates `parsedData` — the CPE-estimation-timeout early return (~line 182-187) and the normal success path (~line 202-206) — then in handleConfirm (line 231) replace the hardcoded ternary with `formatVersion: parsedData.formatVersion`. (b) and (c) are test-only; no product code change — the dedup filter and handleRemoveSbom logic are both already correct on inspection, so this is closing a test-coverage gap, not a functional bug (confirm via the failing-test-first step below in case inspection missed something).
- **Tests:** TDD order, all in Vitest: 1) SbomUploadDialog.test.tsx — add a case mocking `parseCycloneDXMock` to resolve `metadata: { format: 'cyclonedx', formatVersion: '1.2' }` (deliberately NOT '1.5'), upload, click 'Add to Project', and assert `mockUpdateProject`'s call included `sbomFiles: [{ ..., formatVersion: '1.2' }]` — fails today (hardcoded '1.5'), passes after fix (a); repeat with SPDX `formatVersion: '2.2'` to prove it isn't silently coerced to '2.3'. 2) Strengthen TC-SBOM-005 (line 360-434): after clicking 'Add to Project', add `expect(newComponents.some((c) => c.id === 'comp-existing-1')).toBe(false)` and tighten the length assertion to `.toBe(1)` (only comp-new-1) — this is the assertion that actually fails if dedup breaks. Rewrite 'should not add duplicate components to project' (line 581) to seed the target project's existing components with `comp-1` already present (via the store mock), actually click 'Add to Project', and assert the `components` array in `mockUpdateProject`'s payload contains exactly one `comp-1` entry (i.e. the new one is filtered out) — today this test does none of that and would pass unconditionally. 3) New describe block in ProjectDetail.test.tsx (co-located with the existing project-mutation tests, since handleRemoveSbom's business logic lives in ProjectDetail.tsx, not OverviewTab.tsx) covering: 'removes only the target SBOM file's components' (seed 2 sbomFiles with components tagged by distinct sbomFileId, remove one, assert the other SBOM's components survive in the updateProject call); 'strips the removed component from surviving vulnerabilities and drops vulnerabilities left with none' (seed one vulnerability affecting both a removed and a surviving component — assert its affectedComponents keeps only the survivor; seed a second vulnerability affecting only the removed component — assert it is absent from the updated vulnerabilities array); 'recomputes statistics after removal' (assert `statistics.totalVulnerabilities`/`criticalCount`/etc. reflect only the post-removal vulnerabilities, not stale counts) — this last case encodes the WHY: a stale statistics object would show a false vulnerability count on the Overview tab immediately after a user removes an SBOM. Optionally add one OverviewTab.test.tsx case asserting the 'Remove' button (OverviewTab.tsx:134-139) calls `onRemoveSbom(file.id)` with the correct id, mirroring the existing onViewComponent-click pattern in RemediationQueue.test.tsx.
- **Risk:** Low-medium. The formatVersion fix (a) touches shared upload state used by every existing SbomUploadDialog test, but none of them currently assert on the literal output value of formatVersion (confirmed via grep), so no existing test should break. The test-hardening items (b, c) carry a small chance of surfacing a real latent bug in handleRemoveSbom or the dedup filter once genuinely exercised (per Rule 4, loop until the new assertions are green against the real code, not just written) — if so, that becomes a small follow-up fix, not a redesign, since the surrounding logic is otherwise straightforward array filtering.

---

## FR-06 — Executive Dashboard

### FR-06.1 — Aggregate Metrics: add exploited-vulnerability count; fix fixable-% denominator

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** S · **Product code:** yes
- **Root cause:** Two distinct, still-live defects in src/renderer/lib/analytics/metricsCalculator.ts. (1) `OverallMetrics` (interface at lines 17-28; computed in `calculateOverallMetrics`, lines 86-134) has no field for 'exploited vulnerability count'; `Vulnerability.isKev` (src/shared/types.ts:185), which is genuinely populated by the real enrichment pipeline (src/renderer/lib/services/intelligence/enrichVulnerabilities.ts:48/128), is never read anywhere in metricsCalculator.ts, insightsGenerator.ts, or projectAggregates.ts (re-confirmed zero grep hits) — PRD's 'Exploited vulnerability count' bullet is a hard gap. (2) `calculateComplianceMetrics`'s `remediationRate` (lines 298-313) computes `totalFixable / totalWithPatchInfo` where `totalWithPatchInfo` only counts vulns that happen to carry a `patchInfo` object (line 299/305), not all vulnerabilities — so it answers 'what % of vulns-with-patch-metadata are fixable', not the PRD's literal 'fixable vulnerability percentage' (of all vulnerabilities). This value is the only thing standing in for that PRD bullet (rendered as 'Remediation Rate' in ComplianceStatus.tsx and reportBuilder.ts:316).
- **Fix:** 1) In metricsCalculator.ts: add `exploitedCount: number` to the `OverallMetrics` interface (~line 17-28). In `calculateOverallMetrics` (~line 99, alongside the existing per-project health-score loop), add a loop over `projects[].vulnerabilities` incrementing `exploitedCount` when `vuln.isKev` is true; include it in the returned object (~line 124-133). 2) Extend `RiskGauge.tsx`'s existing 'Stats' grid (lines 108-117, currently `grid-cols-2` with Critical/High cells) with a third cell for 'Exploited' reading `metrics.exploitedCount`, using the exact existing cell markup (`text-center p-2 bg-muted rounded` + label) — no new prop needed since RiskGauge already receives the full `OverallMetrics` object. 3) In `calculateComplianceMetrics` (lines 298-313), replace the `totalWithPatchInfo` accumulator with a counter incremented for every vulnerability in the loop (not just ones with `patchInfo`), and divide `totalFixable / totalVulnerabilitiesCounted` (renaming the variable for clarity). No UI change needed for this part — ComplianceStatus.tsx already renders `compliance.remediationRate` under 'Remediation Rate'. Pure-function edits only; no new files, no new deps, no server changes.
- **Tests:** New file `src/renderer/lib/analytics/metricsCalculator.test.ts` (none exists today — confirmed via Glob). TDD failing-first: (a) `calculateOverallMetrics`: 2 mock Projects with vulnerabilities mixing `isKev: true` (one critical, one medium) and `isKev: false/undefined`; assert `exploitedCount === 2` regardless of severity (fails today: field doesn't exist). (b) 'returns 0 exploited count when no vuln is in the KEV catalog' (regression guard against a future default-to-undefined bug). (c) `calculateComplianceMetrics`: one project, 4 vulnerabilities total, only 2 with a `patchInfo` object, 1 of those `patchAvailability: 'available'`; assert `remediationRate === 25` (1/4), not `50` (1/2) — encodes the WHY (PRD wants % of ALL vulns, not % of vulns-with-patch-metadata) and fails against current code. (d) 'remediationRate is 100 when a project has zero vulnerabilities' (preserve existing zero-division edge case). Then `src/renderer/components/executive/widgets/RiskGauge.test.tsx`: add 'should display an Exploited stat cell driven by metrics.exploitedCount', extending the `createMockOverallMetrics` factory with `exploitedCount`, asserting the number and label render (mirrors the existing Critical/High assertions at lines ~58-75). No e2e change: exercising exploitedCount end-to-end needs a real scan producing isKev-tagged vulnerabilities tied to a project, which is disproportionate for a Medium/SHALLOW item; unit coverage matches the existing convention (RiskGauge/ComplianceStatus have no numeric e2e assertions today either).
- **Risk:** Low. Additive field on an already-returned object (no breaking shape change). The remediationRate denominator change alters a displayed percentage, but verified no existing test hardcodes the old value (ComplianceStatus.test.tsx doesn't exist; reportBuilder.ts only reads the field, asserts nothing numeric) so nothing else breaks silently.

### FR-06.2 — Executive Widgets: extend trend chart to 6 months; add Top-10 critical vulnerabilities widget

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** The report additionally cited e2e/features/executive-dashboard.spec.ts test.skip() at lines 83/104/111 (vulnerability-trend-chart, action-items, team-productivity) as evidence these widgets had 'no passing e2e coverage'. Re-verified at HEAD: that file was rewritten in commit 8c3cc40 ('harden ... into real contracts') — WIDGET_HEADINGS (lines 26-32) now asserts real headings for all six widgets including 'Vulnerability Trends', 'Team Productivity', 'Action Items', and only one unrelated, legitimately-documented test.skip remains ('navigate to a project from the Action Items widget', offline-infeasible). So the e2e-coverage sub-claim is FIXED_SINCE_REPORT, but the two underlying content defects that actually caused the SHALLOW grade (3-month window instead of 6; no CVE-level top-10 list) are unchanged in the source — hence the requirement overall is still STILL_OPEN.
- **Root cause:** (1) `calculateTrendMetrics` (metricsCalculator.ts:170) hardcodes `const weeksToInclude = 12` (~3 months), not PRD's explicit 'last 6 months'; `VulnerabilityTrendChart.tsx:14` additionally re-slices to `.slice(-12)`, so even if the calculator produced more periods the widget would still cap at 12. (2) 'Top 10 critical vulnerabilities' has zero CVE-level implementation anywhere: `ActionItems.tsx` only renders up to 5 generic `Recommendation`s (line 71: `recommendations.slice(0, 5)`) plus up to 3 project-level `RiskItem`s (line ~109: `topRisks.slice(0, 3)`) — never an individual-CVE list; re-confirmed zero `cveId`/`CVE-` references across executive/ and analytics/.
- **Fix:** 1) Trend window: change `weeksToInclude` at metricsCalculator.ts:170 from `12` to `26` (26 weeks ≈ 6 months; update the inline comment). Update `VulnerabilityTrendChart.tsx:14` `.slice(-12)` to `.slice(-26)` (and its comment) so the widget's own defensive slice matches the new data window instead of truncating it back to 12. 2) New minimal Top-10 widget (smallest credible slice — reuses existing patterns, no new aggregation subsystem): add `calculateTopCriticalVulnerabilities(projects: Project[], limit = 10): TopVulnerabilityItem[]` to metricsCalculator.ts, where `TopVulnerabilityItem = { id: string; severity: Vulnerability['severity']; cvssScore?: number; projectId: string; projectName: string; affectedComponentCount: number }`. Implementation: flatten `projects[].vulnerabilities` filtered to `severity === 'critical'`, dedupe by `vuln.id` (a CVE can appear in multiple projects — keep the highest-cvssScore occurrence), sort by `cvssScore` descending (undefined last), slice to `limit`. Add `topCriticalVulnerabilities` to the `ExecutiveMetrics` interface (~line 9-15) and wire it into `calculateExecutiveMetrics` (lines 364-372). New component `src/renderer/components/executive/widgets/TopCriticalVulnerabilities.tsx` following the exact sibling-widget shell convention (`bg-card rounded-lg border p-4 h-full flex flex-col`, per RiskGauge.tsx), rendering a ranked list (rank, CVE id, project name, CVSS badge), rows clickable via an `onProjectClick?: (projectId: string) => void` prop (same convention `ActionItems` already uses). Wire into `ExecutiveDashboard.tsx`: one more `lazy()` import (matching lines 14-25 pattern) and one grid cell (matching the col-span/row-span convention at lines 188-217), reusing the already-defined `handleProjectClick` (lines 86-91, currently only passed to ActionItems). Add the new heading text to `WIDGET_HEADINGS` in e2e/features/executive-dashboard.spec.ts (lines 26-32) so the file's existing 'renders all N executive widgets' content-contract test covers it for free.
- **Tests:** In `src/renderer/lib/analytics/metricsCalculator.test.ts` (shared file with FR-06.1, new describe blocks; TDD failing-first): `calculateTrendMetrics` — 'covers a 6-month (26-week) window, not 3 months': build >26 weeks of project history, assert the earliest included period is ~182 days back, not ~84 (fails today at weeksToInclude=12). `calculateTopCriticalVulnerabilities` — 'returns only critical-severity vulnerabilities, sorted by CVSS descending, capped at 10' (12 mock criticals across 3 projects plus several high/medium ones; assert length 10, all critical, non-increasing cvssScore); 'deduplicates the same CVE id appearing in multiple projects, keeping one entry' (two projects sharing a vuln id, e.g. 'CVE-2021-44228'; assert it appears once). New file `src/renderer/components/executive/widgets/TopCriticalVulnerabilities.test.tsx` (mirrors RiskGauge.test.tsx conventions): renders a 3-item list and asserts each CVE id/project name renders; renders an empty list and asserts an empty-state message (no crash); clicking a row calls `onProjectClick` with that row's projectId. `ExecutiveDashboard.test.tsx`: add `vi.mock('./widgets/TopCriticalVulnerabilities', ...)` (matching the existing per-widget mock block, lines 65-105) and a new 'Widget Sections' test asserting `findByTestId('top-critical-vulnerabilities')`. `e2e/features/executive-dashboard.spec.ts`: add the new widget's exact heading text to `WIDGET_HEADINGS` — no separate new e2e test needed, the file's existing 'renders all six (now seven) executive widgets' and desktop-responsive tests cover it automatically by design.
- **Risk:** Medium. Adds a field to the shared ExecutiveMetrics/calculateExecutiveMetrics return shape (backward compatible — additive only, verified no consumer destructures/hardcodes an exhaustive field list). The trend-window change alters VulnerabilityTrendChart's rendered data, but no VulnerabilityTrendChart.test.tsx exists to break, and reportBuilder.ts's PDF trend table uses an independent `.slice(-8)` unaffected by the 12→26 change (verified).

### FR-06.3 — Dashboard Customization: add/remove/reorder/resize widgets, saved layout profiles

- **Priority:** Low · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** L · **Product code:** yes
- **Root cause:** `ExecutiveDashboard.tsx`'s widget grid (lines 188-217) is hardcoded JSX — `grid-cols-9` with a fixed `col-span-*`/`row-span-*` div per widget in a fixed order; nothing reads state to decide which widgets render, in what order, or at what size. `DashboardConfig.tsx` (the only 'customization' UI in the app) exposes solely date-range presets (lines 45-50) and project-scope filtering (lines 140-157) plus Export/Refresh actions — no widget visibility/order/size concept anywhere. Repo-wide grep for dashboardProfile|dashboardLayout|widgetLayout|saveDashboard across src/, server/, cli/ returns zero matches (re-confirmed). All four PRD bullets (add/remove/reorder, resize, save configurations, multiple profiles) are unimplemented.
- **Fix:** FLAGGED AS A LARGER FEATURE: true pointer-based drag-and-drop reorder/resize (e.g. via react-grid-layout) requires a new dependency, collision/grid-resolution logic, and cross-browser drag testing — disproportionate to a Low-priority 'Nice to Have' bullet. Smallest credible slice below satisfies all 4 bullets literally using button/dropdown controls and the persistence infrastructure already in this codebase (zustand `persist`, useStore.ts:69/433-445), with NO new dependency: 1) New file `src/renderer/lib/dashboard/dashboardLayout.ts`: `DashboardWidgetId` union (one id per existing widget + the new FR-06.2 widget), `WidgetSizePreset = 'small'|'medium'|'large'` with a `WIDGET_SIZE_CLASSES` map to Tailwind col-span/row-span strings (discrete presets = 'resize'), `DashboardWidgetSlot { id; visible: boolean; size: WidgetSizePreset }`, `DashboardLayoutProfile { id; name; widgets: DashboardWidgetSlot[] }` (deliberately named 'DashboardLayoutProfile', not 'Profile', to avoid colliding with the unrelated existing `profiles`/`activeProfileId` settings-profile concept at useStore.ts:34/83-149), and `DEFAULT_DASHBOARD_LAYOUT` seeded to reproduce today's exact hardcoded order/sizes so existing users see zero visual change until they customize. 2) Extend `useStore.ts`: add `dashboardLayoutProfiles: DashboardLayoutProfile[]` (seeded with one 'Default' profile from DEFAULT_DASHBOARD_LAYOUT) and `activeDashboardLayoutProfileId: string`, plus actions `addDashboardLayoutProfile(name)`, `deleteDashboardLayoutProfile(id)`, `setActiveDashboardLayoutProfileId(id)`, `updateDashboardLayoutWidgets(profileId, widgets)` (one setter covers visibility-toggle/reorder/resize since all three are 'replace this profile's widgets array'). Add both new fields to the existing `partialize` allowlist (lines 434-442) — additive only — so 'Save dashboard configurations' is satisfied by reusing existing localStorage persistence, and the array+active-id shape satisfies 'Multiple dashboard profiles'. 3) New component `src/renderer/components/executive/widgets/DashboardLayoutEditor.tsx` (kept separate from DashboardConfig.tsx, which owns a different concern — date-range/scope/export): a Dialog (reusing @/components/ui/dialog exactly like DashboardConfig.tsx) listing the active profile's widgets in order, each row with a visibility checkbox, move-up/move-down buttons, a size <select>, plus a profile <select> to switch `activeDashboardLayoutProfileId` and a 'Save as new profile' input calling `addDashboardLayoutProfile`. 4) `ExecutiveDashboard.tsx`: replace the hardcoded grid (lines 188-217) with a data-driven render — a `WIDGET_COMPONENTS` lookup keyed by `DashboardWidgetId` (built from the existing lazy() imports at lines 14-25, unchanged) plus a small per-id props-builder function (each widget takes different metrics slices today), mapping `activeProfile.widgets.filter(w => w.visible)` in order to `<div className={sizeClasses[slot.size]}>{renderWidget(slot.id)}</div>`. Add the DashboardLayoutEditor trigger next to the existing DashboardConfig trigger in PageHeader's actions (~lines 92-103).
- **Tests:** New file `src/renderer/lib/dashboard/dashboardLayout.test.ts`: 'DEFAULT_DASHBOARD_LAYOUT contains exactly the widgets currently hardcoded in ExecutiveDashboard.tsx, in the same order' — regression anchor; if the default drifts from what ExecutiveDashboard.tsx actually renders, existing users' first dashboard view silently changes shape. `src/renderer/store/useStore.test.ts` (existing file, new describe 'Dashboard layout profiles'): 'seeds a Default profile matching DEFAULT_DASHBOARD_LAYOUT on a fresh store'; 'updateDashboardLayoutWidgets replaces the widgets array for the given profile only' (two profiles, update one, assert the other untouched — catches accidental global mutation); 'addDashboardLayoutProfile creates a new profile without changing the active profile id' (documents intended UX: adding ≠ switching); 'setActiveDashboardLayoutProfileId switches which profile is active'; 'persists dashboardLayoutProfiles and activeDashboardLayoutProfileId through the partialize allowlist' — asserts the persisted/serialized state includes both new fields, so a future edit to the allowlist that omits them (silently breaking 'Save dashboard configurations') fails this test instead of shipping unnoticed. New file `DashboardLayoutEditor.test.tsx`: unchecking a widget and saving calls `updateDashboardLayoutWidgets` with `visible:false` for it; clicking move-down swaps two widgets' order in the saved payload; changing a size preset to 'large' is reflected in the saved payload; creating a profile from the name input calls `addDashboardLayoutProfile` and switches the selector to it. `ExecutiveDashboard.test.tsx`: new test mocking the store to return a profile with only 2 of the widgets visible and in non-default order — assert only those 2 testids render AND that DOM order matches the profile order (this is the test that falsifies a regression back to hardcoded JSX, unlike the current wiring-only tests). E2E: new `test.describe('Layout customization')` in executive-dashboard.spec.ts — open the editor, uncheck one widget, save, assert its heading disappears and the rest remain, then reload the page and re-assert the same heading is still absent (proves persistence across reload, offline-feasible since it's pure client-side zustand-persist state, consistent with this file's existing 'Dashboard Settings dialog' tests and the project's established theme-persist-reload e2e pattern).
- **Risk:** Medium-High. Refactors ExecutiveDashboard.tsx's render path from static, e2e-hardened JSX to a data-driven loop; an error in the per-widget props-builder could silently drop or mis-supply a widget's props (each widget takes a different metrics slice) without a type error. Also touches the `persist` `partialize` allowlist (useStore.ts:434) — must be strictly additive; accidentally dropping an existing field there would be a serious data-loss regression, which is specifically why the 'persists through partialize' test above is required, not optional.

---

## FR-04 — Vulnerability Details and Presentation

### FR-04.1 — Vulnerability Listing: add CVE-ID/keyword search, user-selectable sort (CVSS/date), and bulk-select-to-export

- **Priority:** High · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** L · **Product code:** yes
- **Since report:** Re-validated at HEAD (no diff vs the 2026-07-25 report for this file): src/renderer/pages/project-detail/VulnerabilitiesTab.tsx still has no search box, no bulk-select, and no user-selectable sort field. Only the severity filter <select> (line 245-255), CVSS-range/source/reference-tag advanced filters (261-342), and the fixed sortBySeverity()-then-CVSS ordering (line 383, backed by src/renderer/lib/api/vulnMatcher.ts:567-585) exist. Report evidence is accurate and current.
- **Root cause:** src/renderer/pages/project-detail/VulnerabilitiesTab.tsx has no <input> for CVE-ID/keyword search anywhere in the file; the only text-entry-adjacent UI is the Advanced Filters panel (lines 261-342), which covers CVSS range/source/reference-tag/patch/exploit but never a free-text field. Sorting is hardcoded at line 383 to `sortBySeverity(filteredVulns)` (vulnMatcher.ts:567-585, severity desc then CVSS desc) with zero UI control to switch to CVSS-only or publication-date ordering. There is no checkbox/selection state (`React.useState<Set<...>>`) anywhere in the file and no toolbar for acting on a selection, so 'bulk select for operations' is structurally absent.
- **Fix:** Smallest credible slice — reuse existing, already-tested utilities; do not invent new mutation/backend flows:

1. src/renderer/pages/project-detail/helpers.ts: add `export function matchesVulnerabilitySearch(vuln: Vulnerability, query: string): boolean` — case-insensitive substring match against `vuln.id`, `vuln.aliases ?? []`, and `vuln.description`; empty/whitespace query always matches. Mirrors ComponentsTab.tsx:131-133's inline `.toLowerCase().includes()` convention, extracted because it now has 3 branches worth unit-testing.
2. src/renderer/lib/api/vulnMatcher.ts: add two exported, non-mutating sort helpers next to `sortBySeverity` (line 567): `sortByCvssScore(vulnerabilities): Vulnerability[]` (desc by `cvssScore ?? 0`) and `sortByPublicationDate(vulnerabilities): Vulnerability[]` (desc by `publishedAt`, `undefined` sorts last). Same `[...vulnerabilities].sort(...)` pattern as the existing function.
3. src/renderer/pages/project-detail/VulnerabilitiesTab.tsx:
   a. Add `aria-label="Filter by severity"` to the existing severity `<select>` (currently unlabeled, line ~245) — required so it stays uniquely targetable once a second `<select>` exists (see risk/tests).
   b. Add `vulnSearch` state + a labeled `<input aria-label="Search vulnerabilities" placeholder="Search by CVE ID or keyword...">` next to the severity select (same visual row, same styling as ComponentsTab.tsx:38-45); wire `matchesVulnerabilitySearch` into the existing filter pipeline (applied alongside `applyAdvancedFilters`, before `sortBySeverity`).
   c. Add `sortField` state (`'severity' | 'cvss' | 'date'`, default `'severity'`) + a labeled `<select aria-label="Sort by">` with those 3 options. Refactor the `groupedVulns`/`severityConfig` computation (lines 386-418) and the `Object.entries(groupedVulns).filter(...).map(...)` at line 441 into a generic `sections: Array<{key,label,color,bgColor,borderColor,vulns}>` array: when `sortField === 'severity'` it's the current 4-group split (unchanged behavior/output); when `'cvss'`/`'date'` it's a single neutral-styled section holding `sortByCvssScore(sortedVulns)`/`sortByPublicationDate(sortedVulns)`. The existing large per-row render callback (lines 464-684) is reused unchanged — only the grouping computation changes.
   d. Add `selectedVulnIds` state (`Set<string>`, mirrors Dashboard.tsx:87 naming) + a checkbox per row (next to the expand chevron, ~line 482-502; `aria-label={`Select ${primaryId}`}`) and a small toolbar shown when `selectedVulnIds.size > 0` (pattern: MissFilterPanel.tsx's 'N item(s) selected' bar) with 'Clear' and 'Export Selected (n)'. 'Export Selected' calls `exportVulnerabilitiesToCsv(selected, buildComponentMap(selected))` + `downloadCsv(csv, generateFilename(project.name, 'csv', 'vulnerabilities'))` (all imported from `@/lib/export/csv`, zero new CSV logic) where `selected = project.vulnerabilities.filter(v => selectedVulnIds.has(v.id))`. This is the single 'operation' delivered for bulk-select — do not add bulk mark-as-false-positive/reviewed actions here; that requires new store/audit plumbing that belongs to the separate FR-07 audit-wiring gap, not this slice.

- **Tests:** TDD order, Vitest unless noted:

1. src/renderer/pages/project-detail/helpers.test.tsx — new `describe('matchesVulnerabilitySearch', ...)`: (a) matches by CVE-ID substring case-insensitively; (b) matches by an alias (e.g. GHSA id) when the primary id itself doesn't match — WHY: OSV-sourced vulns are looked up by GHSA/OSV id as often as CVE; (c) matches by a keyword present only in `description`, not in id/aliases — WHY: PRD requires keyword search, not just ID lookup; (d) empty/whitespace query matches everything (guards the 'cleared search box' case); (e) returns false when the query is in none of id/aliases/description.
2. src/renderer/lib/api/vulnMatcher.test.ts — new `describe('sortByCvssScore', ...)` and `describe('sortByPublicationDate', ...)` mirroring the existing `sortBySeverity` block (lines 544-573): highest score/most-recent-date first; entries with `cvssScore`/`publishedAt` undefined sort last (assert explicitly, not just 'somewhere'); does not mutate the input array.
3. src/renderer/pages/project-detail/VulnerabilitiesTab.test.tsx — new `describe('VulnerabilitiesTab search, sort and bulk-select (FR-04.1)', ...)` using the file's existing `makeVuln`/`makeProject`/`criticalGroup`/`highGroup` fixtures, plus a third vuln with a distinct `description` and `publishedAt`: (a) typing a CVE-ID substring into the search box hides the non-matching severity group — proves the box is wired into the filter pipeline, not decorative; (b) typing a keyword found only in one vuln's description narrows to that vuln; (c) selecting 'Publication Date' from 'Sort by' renders vulns in a single flat section ordered newest-first (assert DOM order, not just presence) — proves the dropdown actually reorders rather than being cosmetic; (d) checking two vulnerabilities then clicking 'Export Selected' calls the mocked `exportVulnerabilitiesToCsv` (`vi.spyOn` on `@/lib/export/csv`) with exactly those two vulnerabilities, not all of them — this is the assertion that catches 'selection state ignored, exports everything (or nothing)'; (e) 'Clear' empties the selection and hides the toolbar.
4. Playwright e2e — extend e2e/critical-flows/vulnerability-details.spec.ts's existing scanned-4-vulns test (or a new test in the same file) with a content-contract case: after the real scan of sbom-with-vulns.json, type '44228' into the new search box and assert only CVE-2021-44228 remains visible / CVE-2022-24999 is not visible; then check exactly the log4j row's checkbox, click 'Export Selected', capture the download via `page.waitForEvent('download')` (pattern: e2e/features/export-dialog.spec.ts:295-313), read the file, and assert it has exactly 1 data row for CVE-2021-44228 — proving the exported CSV is scoped to the selection, not the whole project (same 'catches a real regression' intent as the unit spy test, at the integration layer).
5. Required companion test-maintenance (caused directly by adding a second always-visible `<select>` on this tab): e2e/critical-flows/vulnerability-details.spec.ts:94,112 and e2e/features/kev-epss-intelligence.spec.ts:80,142 currently do `main.locator('select').selectOption(...)`, which uniquely resolves today only because the severity select is the sole visible `<select>`. Once the new 'Sort by' select exists, both lines hit a Playwright strict-mode 'multiple elements' failure. Update both to `main.getByLabel('Filter by severity').selectOption(...)` using the new aria-label added in the fix.

- **Risk:** Medium: the render refactor from a hardcoded 4-way severity split to a generic `sections` array touches ~250 lines of an already-large, heavily-tested component (VulnerabilitiesTab.tsx + its 1096-line neighbor ProjectDetail.test.tsx also exercises this tab) — must verify no behavioral change when sortField stays 'severity' (the default), i.e. the existing 'VulnerabilitiesTab advanced filters (FR-08.3)' suite and ProjectDetail.test.tsx's 'Vulnerability Filtering - TC-VM Scenarios' block must stay green untouched. Concrete, already-identified regression: adding an always-visible second `<select>` breaks e2e/critical-flows/vulnerability-details.spec.ts and e2e/features/kev-epss-intelligence.spec.ts's `main.locator('select')` calls unless both are updated in the same change (see tests #5).

### FR-04.3 — CVSS Breakdown: parse and render temporal metrics (E, RL, RC) when present in the vector

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Re-validated at HEAD: `grep -rn "temporalMetrics|temporalScore" src/renderer` (excluding tests) still returns zero hits outside the type definition (src/shared/types/cvss.ts:82-86). Confirmed via nvdProvider.ts:306-316 and osvProvider.ts:281-294 that real vector strings flowing through the app (NVD API `cvssData.vectorString`, OSV `severity[].score` for CVSS_V3) go straight into `parseCvssVector`, which never looks for E:/RL:/RC: segments. Confirmed against the seeded e2e NVD DB: CVE-2021-44228's stored vector is base-only (`CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H`, no temporal segment) — consistent with NVD not publishing temporal scores, so this gap is real but low-visibility for NVD-sourced data; OSV-sourced advisories (npm/GHSA) more plausibly carry a full vector including temporal metrics. Report evidence is accurate and current; no change since 2026-07-25.
- **Root cause:** src/renderer/lib/cvss/parser.ts's `parseCvssVector` (lines 14-63) only ever reads the 8 base-metric prefixes (AV/AC/PR/UI/S/C/I/A) via `parseMetricValue(parts, prefix)`; it never scans `parts` for `E:`/`RL:`/`RC:` segments and never constructs `CvssBreakdown.temporalMetrics` (typed at src/shared/types/cvss.ts:75-86 but with no write site anywhere in the codebase). Even if it were populated, src/renderer/components/cvss/CvssMetricsGrid.tsx (the only consumer that renders per-metric detail, lines 46-52) iterates only `breakdown.explanations` and has no branch reading `breakdown.temporalMetrics`, so nothing would render it regardless.
- **Fix:** Minimum slice — plumb the data through the existing structures, do not add full narrative explanation dictionaries for temporal metrics (PRD's FR-04.3 bullet for full per-value explanations is scoped explicitly to AV/AC/PR/UI/S/C/I/A only; the temporal bullet just says 'if available', i.e. show it when present):

1. src/shared/constants.ts: extend `CVSS_METRIC_VALUES` (lines 238-277) with three more entries whose keys match the `CvssBreakdown.temporalMetrics` field names 1:1 with the union types in cvss.ts:83-85: `exploitCodeMaturity: { X: 'Not Defined', H: 'High', F: 'Functional', P: 'Proof-of-Concept', U: 'Unproven' }`, `remediationLevel: { X: 'Not Defined', O: 'Official Fix', T: 'Temporary Fix', W: 'Workaround', U: 'Unavailable' }`, `reportConfidence: { X: 'Not Defined', C: 'Confirmed', R: 'Reasonable', U: 'Unknown' }`.
2. src/renderer/lib/cvss/parser.ts: in `parseCvssVector`, after building the base `metrics`, scan `parts` for optional `E`/`RL`/`RC` segments (each is genuinely optional — absent in most NVD vectors). For each present segment, reuse the existing `parseMetricValue`-style lookup against the new `CVSS_METRIC_VALUES` entries (extend `PREFIX_TO_METRIC_KEY`, lines 69-78, with `E: 'exploitCodeMaturity'`, `RL: 'remediationLevel'`, `RC: 'reportConfidence'`); an unrecognized value for a _present_ temporal segment must throw (return null for the whole vector), matching the existing fail-loud contract for base metrics — an invalid temporal code means the vector is malformed, not that temporal should be silently dropped. Only set `temporalMetrics` on the returned breakdown when at least one of E/RL/RC was actually present in the vector string; leave it `undefined` otherwise (this is the flag the UI branches on).
3. src/renderer/components/cvss/CvssMetricsGrid.tsx: add a small conditional block after the existing explanations grid (after line 52) — `{breakdown.temporalMetrics && <TemporalMetricsSection temporalMetrics={breakdown.temporalMetrics} />}` — rendering simple label:value rows (e.g. 'Exploit Code Maturity: Functional') for whichever of the 3 fields are defined; no new dictionary lookups needed, the values are already the full display strings from step 2.

- **Tests:** TDD order:

1. src/renderer/lib/cvss/parser.test.ts (extend the existing FR-04.3/CR-03.3 suite): (a) a vector with `/E:F/RL:O/RC:C` appended to a valid base vector parses `temporalMetrics` to `{ exploitCodeMaturity: 'Functional', remediationLevel: 'Official Fix', reportConfidence: 'Confirmed' }` — the core positive case; (b) a base-only vector (e.g. the exact log4j vector `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H` from the seeded e2e DB) leaves `result.temporalMetrics` `undefined` — WHY: this is the majority real-world case (NVD data) and the UI must not render an empty/broken temporal section for it; (c) `E:X` (Not Defined) is accepted and maps to `'Not Defined'`, distinguishing 'explicitly not defined' from 'absent'; (d) an invalid temporal value (e.g. `E:Q`) makes `parseCvssVector` return `null` for the whole vector — parity with the existing base-metric fail-loud test.
2. src/renderer/components/cvss/CvssMetricsGrid.test.tsx: (a) a breakdown fixture with `temporalMetrics` populated renders the 'Exploit Code Maturity' label and its value when the grid is expanded; (b) a breakdown fixture with `temporalMetrics: undefined` (the existing default fixture) renders NO temporal section/heading at all — WHY: proves 'if available' is honored and the common case doesn't show a broken empty block.
3. Playwright e2e: not practical against the current offline-seeded 16-CVE NVD DB — verified none of the seeded vectors (including log4j) carry temporal segments, and EPSS/OSV/live-sync (the more likely real-world source of temporal vectors) are explicitly unavailable offline per the e2e harness. Flagging this explicitly rather than writing an e2e test that can't exercise the real code path: unit (parser.test.ts) + component (CvssMetricsGrid.test.tsx) tests are the credible coverage for this slice; e2e temporal-metrics coverage would require adding a synthetic CVE row with a temporal vector to `.e2e-data/nvd-data.db`, which is out of scope for this fix (shared fixture data affects other suites).

- **Risk:** Low: additive-only change (new optional field population + new optional render branch); no existing behavior changes when temporalMetrics is absent, which is the case for every currently-seeded fixture/e2e vector, so no existing test's assertions are expected to shift.

### NOTE-cvss-parser-null — parseCvssVector() always-null regression (CVSS_METRIC_VALUES key mismatch) — verify fix already shipped

- **Priority:** High · **Report:** SHALLOW · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** This is the single biggest divergence from the 2026-07-25 report: the bug is no longer present at HEAD. `git log --oneline -- src/renderer/lib/cvss/parser.ts` shows commit 4b3b178 'fix(cvss): map metric prefixes to full keys so vectors parse', already merged into this branch's history (before this task started — `git status --porcelain` shows zero uncommitted diff for parser.ts). The fix adds `PREFIX_TO_METRIC_KEY` (parser.ts:69-78), a `Record<string, keyof typeof CVSS_METRIC_VALUES>` mapping each vector prefix (AV/AC/PR/UI/S/C/I/A) to the full-name key CVSS_METRIC_VALUES is actually keyed by, and `parseMetricValue` (parser.ts:83-104) now looks up `CVSS_METRIC_VALUES[PREFIX_TO_METRIC_KEY[prefix]]` instead of `CVSS_METRIC_VALUES[prefix]`. A dedicated regression suite already exists at src/renderer/lib/cvss/parser.test.ts (6 tests) and was re-run as part of this validation: `npx vitest run src/renderer/lib/cvss/parser.test.ts` → 6/6 passed, including 'parses a real CVSS 3.1 critical vector ... with correct full metric names' asserting the exact previously-broken vector now returns baseScore 9.8/severity critical instead of null.
- **Root cause:** (Historical — already fixed.) Before commit 4b3b178, `parseMetricValue` (then ~parser.ts:83-90) looked up `CVSS_METRIC_VALUES[prefix]` (e.g. `CVSS_METRIC_VALUES['AV']`), but `CVSS_METRIC_VALUES` (src/shared/constants.ts:238-277) is keyed by full metric names (`attackVector`, `attackComplexity`, ...), not raw vector prefixes. Every base-metric lookup except the hand-cased `'S'` (scope) resolved to `undefined`, so `mapping[value]` threw inside `parseMetricValue`, the `try/catch` in `parseCvssVector` (then lines ~59-62) swallowed it, and every real CVSS 3.1/3.0 vector returned `null` — silently disabling `cvssBreakdown` for every NVD/OSV-sourced vulnerability across nvdProvider.ts and osvProvider.ts.
- **Fix:** No further product change required — already shipped in commit 4b3b178. Recommended follow-up (verification only, not a new fix): re-run `npx vitest run src/renderer/lib/cvss/parser.test.ts` as part of this remediation pass's final verification sweep to have a fresh, dated confirmation attached to the closeout, and re-run the FR-04.3 temporal-metrics work above on top of the fixed parser (that task's design already assumes the corrected `PREFIX_TO_METRIC_KEY` pattern and extends it rather than re-deriving it).
- **Tests:** Already present and passing: src/renderer/lib/cvss/parser.test.ts's 6 cases cover (a) a real CVSS 3.1 critical vector parses to the correct full metric names and the NVD-reference baseScore 9.8/critical; (b) a real CVSS 3.0 scope-Changed vector computes the NVD-reference baseScore 9.6/critical; (c)-(e) null-return cases (empty string, too-few metrics, unrecognized metric value) still correctly return null post-fix, proving the fix didn't loosen the fail-loud contract. No further test authoring needed for this item; it should simply be kept green (it already is) as a regression guard while FR-04.3's temporal-metrics extension lands on top of the same file.
- **Risk:** None outstanding. Residual risk is only that FR-04.3's temporal-metrics fix (above) edits the same file/mapping table — that change must not regress this already-fixed base-metric path, which is exactly why FR-04.3's test plan re-runs the base-metric-unaffected assertions (log4j base-only vector still parses, still no temporalMetrics) alongside the new temporal cases.

---

## FR-03 Vulnerability Scanning

### FR-03.1 — Local Database Scanning — implement real version-range (fuzzy) CPE matching; add a perf regression guard

- **Priority:** High · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** L · **Product code:** yes
- **Since report:** Re-verified at HEAD, unchanged since the report. Directly queried the seeded e2e DB (.e2e-data/nvd-data.db): CVE-2021-44228's only cpe_matches row is a literal exact-version entry (cpe:2.3:a:apache:log4j:2.14.1:\*:...) with all four version_start/end columns NULL — so the one e2e test that 'proves' CPE matching works never exercises a range-based row, which is how most real NVD CVEs are actually published.
- **Root cause:** server/database/nvdDb.ts:789-808 searchCVEsByCPE does a plain `cp.cpe23_uri LIKE '%<full-cpe-with-version>%'` string match. NVD CVE applicability data commonly stores cpe23*uri with version='*' plus separate version*start_including/version_start_excluding/version_end_including/version_end_excluding columns (schema at nvdDb.ts:324-327, populated at :595-612, and already surfaced on CveWithDetails.cpeMatches for display) — but nothing ever reads those columns to decide inclusion, so a component pinned to an in-range-but-not-literally-listed version (e.g. 2.14.1 against a stored range [2.0, 2.15.0) with version='*') is silently missed. Contributing perf issue: the leading-wildcard LIKE can't use idx_cpe_matches_cpe23_uri, unlike the already-existing precision-first pattern in the sibling searchCVEsByProduct (nvdDb.ts:820-854, which tries an indexed cpe_product exact/prefix match before falling back to substring).
- **Fix:** 1) Add server/database/versionRange.ts (new, named exports, no `any`): compareVersions(a: string, b: string): number — numeric-segment-aware comparator split on '.'/'-' — and isVersionInRange(version: string, range: {versionStartIncluding?: string; versionStartExcluding?: string; versionEndIncluding?: string; versionEndExcluding?: string}): boolean (no bounds at all => always in range, matching NVD's 'always vulnerable' semantics). 2) In nvdDb.ts searchCVEsByCPE: parse vendor/product/version from the incoming CPE via cpeText.split(':') indices 3/4/5 (mirrors the existing client-side extractVendorProductFromCpe in src/renderer/lib/api/vulnMatcher.ts:71-88). Keep the current literal LIKE query untouched (fast path, preserves existing passing tests). When version is present and not '_', additionally query cpe*matches scoped by vendor/product (reusing the hasCpeProductColumn()/cpe_product precision-first pattern from searchCVEsByProduct) restricted to rows where at least one version*_ column is non-null, filter those candidates in JS with isVersionInRange, merge+dedupe with the literal-match set by CVE id, re-sort by cvss_score DESC, then apply offset/limit in JS. 3) Note the 100k-component perf bullet is only partially addressed by reusing the indexed cpe_product path for the range query — a full live-scale benchmark is out of scope for this slice (see risk).
- **Tests:** TDD order — write failing first: 1) server/database/versionRange.test.ts (new): boundary cases (versionStartIncluding inclusive-equal true, versionEndExcluding exclusive-equal false, value strictly inside/outside a range, no-bounds-at-all always true). 2) server/database/scanContract.test.ts — add under the existing describe('scan contract: CPE -> CVE via searchCVEsByCPE'): a new fixture row with cpe23_uri version='\*' plus version_start_including/version_end_excluding populated; it('matches a component version that falls inside a version-range CPE entry, not just a literal-version one') asserting the CVE is returned for an in-range version (currently returns [] — this is the core regression this task fixes); it('excludes a version outside the range bounds') asserting the same CVE is NOT returned for an out-of-range version (proves it's real range logic, not a blanket vendor/product match). 3) A synthetic perf smoke test (e.g. server/database/nvdDb.perf.test.ts, batching inserts per the existing 'Pre-existing Issues' flaky-perf guidance in CLAUDE.md to avoid repeating that known flakiness): seed ~50k cpe_matches rows for one vendor/product via the cpe_product column and assert searchCVEsByCPE for one version completes under a fixed time budget — a regression guard, not a full 100k-real-CVE realism claim.
- **Risk:** Version comparison for NVD's real-world version strings (pre-release suffixes like '2.0-beta9', build metadata) is inherently heuristic — matches the PRD's own 'fuzzy matching' wording but should be documented as best-effort, not exact semver. The 100k-component performance bullet remains only partially verified by the synthetic smoke test; a genuine full-scale benchmark against real NVD data is a separate, larger workstream if a real-world regression surfaces.

### FR-03.3 — OSV Integration — headline defect already fixed on HEAD; remaining gap is missing response caching

- **Priority:** High · **Report:** GAP · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** yes
- **Since report:** The report's CONFIRMED headline bug (matchVulnerabilitiesForComponents' inverted `if (typeof window === 'undefined' || !getPlatform()?.database)` gate making OSV unreachable in the real web app) is fixed on HEAD via commits e3bd0ba ('fix(osv): query OSV live through same-origin /api/osv proxy') and 67d7db9 ('test(server): cover the /api/osv proxy route') — both confirmed ancestors of HEAD via `git merge-base --is-ancestor e3bd0ba HEAD`. A same-origin /api/osv Express proxy now exists (server/routes/osv.ts, tested in osv.test.ts) and vulnMatcher.ts now queries OSV whenever the platform is available, for both the singular and batch matchers, with real tests (vulnMatcher.test.ts:94, :396-433, :1163-1198). The PRD's separate 'Cache OSV responses' bullet, however, was never implemented and remains open — confirmed via grep, zero cache references in osv.ts or the OSV call sites in vulnMatcher.ts.
- **Root cause:** src/renderer/lib/api/osv.ts queryByPurl/queryByPurls (lines 236-327) have no response caching at all, and the OSV-query blocks that call them — src/renderer/lib/api/vulnMatcher.ts:320-330 (singular) and :487-509 (batch) — hit the live OSV endpoint on every single scan/refresh with no TTL, unlike the sibling NVD path (src/renderer/lib/refresh/refreshService.ts:62-82) which already uses getVulnCache() keyed by (purl, 'nvd'). This means every re-scan of an unchanged SBOM needlessly re-queries OSV's public, rate-limited API.
- **Fix:** In src/renderer/lib/api/vulnMatcher.ts: import getVulnCache from @/lib/cache (already used by refreshService.ts for the exact same purpose). Add one small helper used by both the singular and batch OSV blocks that checks getVulnCache().get(purl, 'osv') before calling queryByPurls/queryByPurl, and getVulnCache().set(purl, 'osv', vulns, ttlMs) after a successful call, mirroring the existing NVD-cache convention exactly. Add an optional cacheTTLHours parameter (default matching FR-03.2's documented 24h, as a new OSV_CACHE_TTL_HOURS constant in @@/constants) so callers can opt into a different TTL later without being forced to change today.
- **Tests:** TDD, write first (fails today — no cache exists so a second call always hits the OSV mock again): src/renderer/lib/api/vulnMatcher.test.ts — it('serves a second OSV lookup for the same PURL from cache instead of calling the API again'): mock the underlying fetch queryByPurls uses, call matchVulnerabilitiesForComponents([component]) twice, assert the mock was invoked exactly once. Why this test must exist: repeated scans/renders of the same project must not hammer OSV's public rate-limited endpoint — a naive 'always query' implementation (today's behavior) passes every currently-existing test yet still violates this exact PRD bullet, so only a test that asserts a SECOND call is suppressed can catch a regression. it('re-queries OSV after the cache TTL has elapsed'): vi.useFakeTimers(), advance past the TTL, assert a second live call occurs — proves the cache isn't accidentally permanent. No change needed to src/renderer/lib/cache/vulnCache.test.ts — the cache primitive's get/set/TTL contract is already covered there.
- **Risk:** Low, additive and backward compatible (default TTL means existing callers are unaffected). Caching can mask a genuinely updated OSV advisory for the TTL window — an accepted, pre-existing trade-off already made for the NVD cache, and the PRD itself asks for 'cache with TTL', not always-live.

### FR-03.4 — Hybrid Scanning — wire the already-implemented NVD-over-OSV source-priority merge through to the real scan flow

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Re-verified at HEAD after FR-03.3's OSV fix landed. The OSV gate is no longer inverted, but the specific defect this task targets (the NVD API key being silently dropped before it reaches osv.ts's merge logic) is untouched by that fix and still present.
- **Root cause:** src/renderer/lib/api/vulnMatcher.ts:238 and :354 declare `_nvdApiKey?: string` with a JSDoc that literally says 'not used, kept for API compatibility', and neither OSV call site forwards it: `queryByPurls([component.purl])` (singular, ~line 324) and `queryByPurls(purls)` (batch, ~line 498) both omit the second argument. This means osv.ts's already-implemented, already-tested NVD-over-OSV priority merge (convertOsvVulnerabilityToVulnerability, osv.ts:174-210, gated on `if (cveAliases.length > 0 && nvdApiKey)`) never activates in the real app, even though both real call sites DO fetch and pass a genuine key into matchVulnerabilitiesForComponents (src/renderer/pages/project-detail/useProjectScan.ts:63-67 handleScan; src/renderer/lib/refresh/refreshService.ts:75 inside refreshVulnerabilityData) — the key is fetched from secure storage correctly and then discarded one layer down. Compounding this: recordMatch (batch, vulnMatcher.ts:364-375) and pushMatches (singular, :246-257) dedupe purely by vuln.id and, when an id already exists in the map, only patch affectedComponents/matchQuality — never merging sources/aliases — so even once the key is wired through, a CVE already found via local NVD search first would still lose its OSV cross-reference/attribution. This root cause overlaps with FR-03.3's remaining 'merge OSV with NVD' nuance; fixing it here also improves that.
- **Fix:** 1) Rename `_nvdApiKey` to `nvdApiKey` in both function signatures/JSDoc (the leading underscore also violates this repo's absolute 'no underscore-prefixed identifiers' rule, so this is a direct cleanup of lines already being touched, not scope creep). 2) Thread it into the OSV call sites: `queryByPurls([component.purl], nvdApiKey)` (singular) and `queryByPurls(purls, nvdApiKey)` (batch). 3) Widen recordMatch and pushMatches so that when an entry for vuln.id already exists, it merges `sources` and `aliases` (union, de-duplicated) into the existing entry instead of only touching affectedComponents/matchQuality.
- **Tests:** TDD, write first (fails today because the key is silently dropped): src/renderer/lib/api/vulnMatcher.test.ts — it('forwards the NVD API key to OSV queries so a CVE-aliased OSV advisory is merged with both sources attributed'): mock OSV's underlying fetch to return a GHSA advisory aliasing a CVE the local DB does NOT have, call matchVulnerabilitiesForComponents([component], 'test-api-key'), assert the resulting vulnerability's `sources` contains both 'nvd' and 'osv' (proving osv.ts's getCveById enrichment path actually ran, which requires the key to have arrived). Why: this is the literal 'prioritize data by source reliability' + 'merge results from multiple sources' PRD bullet — without the key wired through, the app quietly serves the raw, unenriched GHSA-only entry forever. it('keeps both sources attributed when the same CVE is found via local NVD search first and OSV second'): local-NVD mock returns CVE-X, OSV mock returns an alias resolving to CVE-X too; assert the merged entry's sources includes both, not just whichever ran first — proves the dedupe-by-id merge doesn't silently drop the second source's attribution (the PRD's explicit 'display source attribution for each vulnerability' bullet). No change needed to osv.ts or its own test file — that merge logic is already correct; this fix only makes it reachable.
- **Risk:** Passing the key means one extra outbound NVD getCveById call per distinct OSV finding that carries a CVE alias — bounded by number of findings, not SBOM size, but still additional NVD API traffic; confirm ./nvd's getCveById already goes through the shared createNvdRateLimiter (used elsewhere per FR-03.2) before shipping, otherwise this could trip rate limits during a large scan. Users without a stored NVD API key see no behavior change (osv.ts's existing gate stays opt-in).

### FR-03.5 — Manual Refresh — add a user-facing cache-busting control (currently only exists as a dead service-layer flag)

- **Priority:** High · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Re-verified at HEAD, unchanged since the report. The only 'force' flags found repo-wide are startDeltaSync(false) hardcoded at Search.tsx:231 and Settings.tsx:617 — an unrelated feature (bulk local-NVD-database delta sync), not this per-project vulnerability refresh.
- **Root cause:** src/renderer/lib/refresh/refreshService.ts:35 defaults `useCache = true` and it is never overridden by either real caller: src/renderer/pages/project-detail/useProjectScan.ts:184-189 (handleRefreshVulnData) and src/renderer/pages/Dashboard.tsx:96-101 (handleRefreshVulnData) both call refreshVulnerabilityData(components, { cacheTTL, onProgress }) without ever setting `useCache: false`, and no button/checkbox anywhere in the UI exposes that option to a user.
- **Fix:** 1) src/renderer/pages/project-detail/useProjectScan.ts: change handleRefreshVulnData to accept an optional `force = false` param and pass `useCache: !force` into the refreshVulnerabilityData call (~line 184). 2) src/renderer/pages/Dashboard.tsx: identical change to its handleRefreshVulnData(projectId, force = false) (~line 91-101). 3) Add one small adjacent icon button next to the existing refresh control — src/renderer/components/ProjectCard.tsx:146-156 and the equivalent header control wired via scan.handleRefreshVulnData in src/renderer/pages/ProjectDetail.tsx — with title='Force refresh — bypass cache and query fresh data', calling the same handler with force: true. No new dialog/menu component; reuse the existing icon-button styling already on that row.
- **Tests:** TDD order: 1) src/renderer/lib/refresh/refreshService.test.ts already covers useCache:false at the service level (~lines 337-338) — confirms the underlying primitive works; no change needed, this proves the gap is purely caller-side wiring. 2) Add (in ProjectDetail.test.tsx's scan/refresh describe block, or a new useProjectScan.test.ts if the hook has no dedicated file yet): it('passes useCache: false to refreshVulnerabilityData when a force refresh is requested') — call handleRefreshVulnData(true), assert the mocked refreshVulnerabilityData was called with {useCache: false, ...}; and it('defaults to a cached refresh for a normal click') asserting useCache is true/omitted otherwise. Why: without this test, a future refactor could silently re-hardcode 'force' away and nothing would fail, even though the PRD explicitly calls for a user-facing cache-bypass — this pins the caller-level contract that today doesn't exist at all. 3) src/renderer/components/ProjectCard.test.tsx: it('calls the refresh handler with force=true when the force-refresh button is clicked, distinct from the normal refresh click'). 4) E2E: explicitly skip — cache-busting has no observable network difference in the offline seeded-DB e2e environment (OSV/live-sync are unavailable offline per project notes); a Playwright test asserting only 'button exists and is clickable' would be a shallow, non-failing assertion — call this out as an honest, intentional skip rather than padding coverage.
- **Risk:** Low — additive UI control. Watch for ProjectCard's action-icon row getting crowded on narrow viewports (a11y/visual nit, not functional).

### FR-03.6 — Automatic Refresh — wire the already-built (but dead) needsRefresh/interval primitives into a real in-app scheduler; interval selector, notification, battery guard

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** L · **Product code:** yes
- **Since report:** Re-verified at HEAD. The report understated one thing and correctly flagged another: the server DOES have a fully-built, unit-tested recursive-setTimeout scheduler (NvdDeltaSync.enableAutoSync/disableAutoSync, nvdDeltaSync.ts:423-513) — contrary to the report's 'no setInterval/cron ever reads that flag' framing — but it is for a different concern (re-syncing the shared local NVD CVE database, FR-03.2 territory) and, exactly as the report concluded, it is still never invoked from either sync route or from server startup, so it changes nothing observable. The client-side needsRefresh()/autoRefreshInterval gap the report describes for per-project vulnerability refresh is unchanged.
- **Root cause:** settings.autoRefresh (src/renderer/pages/Settings.tsx:846-861) is a bare on/off boolean wired only to client-side updateSettings, with no interval selector in the UI. settings.autoRefreshInterval (src/shared/constants.ts:51, default 24h) is read only for a passive countdown display (src/renderer/components/ProjectCard.tsx:95 via getNextRefreshTime). needsRefresh() (src/renderer/lib/refresh/refreshService.ts:130-140) — the only function that decides a refresh is due — is exported, re-exported from index.ts, and unit-tested, but has zero production callers (confirmed via repo-wide grep: only its own test file references it). Separately, the server's NvdDeltaSync.enableAutoSync/disableAutoSync scheduler (nvdDeltaSync.ts:423-513) is never invoked either — neither /sync/auto (server/routes/database.ts:662-683, a raw SQL flag-flip) nor /config/sync (:769-790, calls setAutoSyncInterval which its own doc comment says persists 'without starting the scheduler') nor server startup (server/database/initialize.ts:34-88, which calls kevService.initialize() for the analogous KEV feature but has no equivalent call here) ever arms it — but that scheduler solves a different problem (DB-wide CVE sync) than this requirement (per-project vulnerability rescanning). pauseOnBattery (src/shared/types.ts:372, defaulted in src/shared/constants.ts:106) is read nowhere in the codebase. No 'new critical vulnerability found' notification path exists anywhere.
- **Fix:** Smallest credible slice for a client-rendered SPA with no service worker / persistent background process (true tab-closed background execution is explicitly out of scope — see risk): 1) src/shared/constants.ts: add AUTO_REFRESH_INTERVAL_OPTIONS (hourly=1h/daily=24h/weekly=168h value/label pairs, mirroring the existing SYNC_SCHEDULE_OPTIONS shape at :146-151). 2) src/renderer/pages/Settings.tsx (~line 833-861): add an interval <select> next to the existing toggle, calling updateSettings({autoRefreshInterval: hours}), enabled only when settings.autoRefresh is true. 3) Add src/renderer/lib/refresh/autoRefreshScheduler.ts (new module) exporting startAutoRefreshScheduler(getProjects, updateProject, settings): () => void — a setInterval (e.g. every 5 minutes while the tab is open) that, per project, checks the already-built needsRefresh(project, settings.autoRefreshInterval); when due, settings.autoRefresh is on, and the battery guard (step 5) allows it, calls refreshVulnerabilityData in the background (no loading UI) and updates the project on completion, exactly like the manual path. 4) Wire it once at the app-shell level (e.g. src/renderer/App.tsx) via a useEffect that starts it on mount and stops it on unmount. 5) Battery guard: when settings.pauseOnBattery, feature-detect navigator.getBattery?.() and skip the run only when it resolves with charging === false; fail open (proceed) on browsers without the API so auto-refresh isn't silently disabled everywhere. 6) Notification: after a scheduled refresh, diff the new vulnerabilities against the project's previous id set for newly-appeared severity==='critical' entries and call the existing toast module, reusing the pattern already in useProjectScan.ts.
- **Tests:** TDD order — write first, all fail today since the module doesn't exist: 1) src/renderer/lib/refresh/autoRefreshScheduler.test.ts (new): it('does not refresh any project when settings.autoRefresh is false') — advance fake timers, assert refreshVulnerabilityData never called (pins today's broken default). it('refreshes a project once its autoRefreshInterval has elapsed since lastVulnDataRefresh') — fake-timer-advance past the interval, assert exactly one call — this is the core 'why': right now this never fires no matter how much time passes. it('does not refresh again before the interval elapses') — advance by less than the interval, assert zero calls. it('skips the scheduled refresh when pauseOnBattery is true and the device reports on-battery') — mock navigator.getBattery to resolve {charging:false}. it('still refreshes when pauseOnBattery is true but the device is plugged in') — {charging:true}. it('fires a toast when a scheduled refresh surfaces a new critical vulnerability not previously present'). 2) src/renderer/pages/Settings.test.tsx: it('renders an interval selector that updates settings.autoRefreshInterval') and it('disables the interval selector while auto-refresh is off'). 3) E2E: add exactly one lightweight, offline-safe content-contract test asserting the new interval <select> renders its options and the choice persists across reload; explicitly do NOT attempt to e2e-test the actual timed background refresh (would need multi-hour real time or fragile in-browser clock mocking) — call this out as an honest, intentional skip.
- **Risk:** Architectural ceiling: this only runs while a browser tab is open, so it does not satisfy a literal 'runs even when the app isn't in use' reading of 'background refresh'; that would require a server-side per-project scheduler or service-worker background sync — a materially larger, separate workstream (the existing NvdDeltaSync.enableAutoSync could be a rough template but solves a different problem and would need an equivalent built for per-project matching). Multiple open tabs of the app would each run their own scheduler and could duplicate refresh calls — acceptable since refreshes are idempotent, but worth a code comment. navigator.getBattery() is deprecated/unsupported in Firefox and Safari — the fail-open design keeps auto-refresh working there but means pauseOnBattery silently has no effect on those browsers; flag this to the product owner rather than hide it.

---

## FR-07 — Audit and Compliance

### FR-07.1 — Audit Logging — SCAN and EXPORT events never actually emitted from real user flows

- **Priority:** High · **Report:** GAP · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** yes
- **Since report:** Commit cbbd029 (2026-07-25, same day as the report but after it was generated) wired logProjectCreate/logProjectUpdate/logProjectDelete/logSettingsChange into src/renderer/store/useStore.ts (addProject:231, updateProject:242, deleteProject:268, updateSettings:78) and logVulnerabilityRefresh into the store's own refreshVulnerabilityData action (useStore.ts:369). The report's claim of 'zero callers anywhere' is now false for CREATE/UPDATE/DELETE project and SETTINGS_CHANGE. However, re-validation at HEAD (8c3cc40) shows two of the PRD's explicitly-named event categories are still not produced by any reachable code path: SCAN and EXPORT (for the primary data-export flow). Re-graded PARTIALLY_FIXED, not FIXED.
- **Root cause:** (1) src/renderer/pages/project-detail/useProjectScan.ts:29-146 `handleScan` (the 'Scan for Vulnerabilities' button's handler, exercised in ProjectDetail.test.tsx 'Vulnerability Scanning') only calls the generic `updateProject(project.id, {...})` at line 133, which fires `logProjectUpdate` (actionType 'UPDATE') — never `logVulnerabilityScan` (actionType 'SCAN', src/renderer/lib/audit/auditLogger.ts:68-88). AuditLogPanel.tsx:46 lists 'SCAN' as a filterable action type, but nothing in the running app ever produces one. (2) src/renderer/components/ExportDialog.tsx:28-51 `handleExport` — the CSV/JSON/PDF export flow actually reachable from Dashboard.tsx:474 and ProjectDetail.tsx:381 — calls `exportProjectData`/`exportAllProjects` (lines 39-43) but never `logExport` (auditLogger.ts:237-256). The only call site that logs an EXPORT event is the unrelated settings-profile export path (useStore.ts:212-222, `logAuditEvent('EXPORT', 'profile', ...)`), so the PRD's 'Log ... EXPORT events' bullet is met for profile export only, not for the export feature users actually use for compliance evidence (vulnerabilities/components/full project reports). (3, minor) src/renderer/lib/audit/auditLogger.ts:181-208 `logSettingsChange` sets `previousState: { changedFields }` (auditLogger.ts:201) — only the names of changed fields, not their prior values — so the 'Store before/after state for changes' bullet is not met for SETTINGS_CHANGE events specifically (it is met for project CREATE/UPDATE/DELETE via sanitizeProject/sanitizePartialProject).
- **Fix:** 1) In useProjectScan.ts: `import { logVulnerabilityScan } from '@/lib/audit'`; inside `handleScan`'s try block, right after the `updateProject(...)` call (after line 146, before `setScanProgress(100)`), call `logVulnerabilityScan(project.id, project.name, project.components.length, stats.total, existingVulnerabilities.length)`. 2) In ExportDialog.tsx: `import { logExport } from '@/lib/audit'`; inside `handleExport`, after the export call succeeds (after line 43, before `onClose()`), branch on `isAllProjects`: if true call `logExport('all', selectedFormat, projects?.length ?? 0)`; else map `selectedDataType` to `{project: itemCount 1, vulnerabilities: project.vulnerabilities?.length ?? 0, components: project.components?.length ?? 0}` and call `logExport(entityType, selectedFormat, itemCount, project?.id)`. 3) (minor, do only if time permits) In auditLogger.ts `logSettingsChange`, build `previousValues` by reducing `changedFields` over `previousSettings` and pass `previousState: sanitizeSettings(previousValues)` instead of `{ changedFields }` (keep the changed-field list in `metadata.description` as today).
- **Tests:** TDD order — write each test against current code first and confirm it fails, then wire the fix: 1) Extend src/renderer/pages/ProjectDetail.test.tsx 'Vulnerability Scanning' describe (sibling to the existing 'should update project with scan results' test, ~line 1744): `const audit = await import('@/lib/audit'); const scanSpy = vi.spyOn(audit, 'logVulnerabilityScan')`, click 'Scan for Vulnerabilities', `await waitFor(() => expect(scanSpy).toHaveBeenCalledWith(project.id, project.name, project.components.length, expect.any(Number), expect.any(Number)))`. This fails today because the spy is never called (only a generic UPDATE fires) — proves SCAN events are distinguishable from ordinary edits, which is the whole point of a compliance filter on action type. 2) Extend src/renderer/components/ExportDialog.test.tsx: add `vi.mock('@/lib/audit', () => ({ logExport: vi.fn() }))` alongside the existing `vi.mock('@/lib/export', ...)`; add 'logs an EXPORT audit event for a single-project export' (assert `logExport('vulnerability', 'csv', 1, 'project-1')` given the fixture's 1 vulnerability) and 'logs an EXPORT audit event for an all-projects export' (assert `logExport('all', selectedFormat, projects.length)`). Both fail today (no import, no call). 3) (if doing the minor fix) extend src/renderer/lib/audit/auditLogger.test.ts's logSettingsChange describe with a case asserting `addEvent` is called with `previousState` containing the OLD value of a changed field, not just its name. Why these matter: a test that only checks `updateProject`/`exportProjectData` was called (as today's suite does) keeps passing even if the SCAN/EXPORT-specific logger call is deleted — it can't catch this exact regression. Spying on the exported logger functions directly is what makes the tests fail when the wiring is missing or removed.
- **Risk:** Low — purely additive logging calls alongside existing scan/export logic, no behavior change to the scan or export result itself. Main risk is miscounting itemCount/componentsScanned if the selectedDataType-to-entityType mapping is wrong; the exact-argument test assertions catch that.

### FR-07.2 — Audit Log Viewing — /audit route, nav link, and date-range filter still do not exist

- **Priority:** High · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Unchanged since the report. The only commit touching audit code since 2026-07-25 (cbbd029) modified auditLogger.ts, useStore.ts and CreateProjectDialog only — it did not touch App.tsx, Sidebar.tsx, or AuditLogPanel.tsx. Re-verified directly: src/renderer/App.tsx's route table (lines 144-152) still has no '/audit' route and no lazy import for an audit page; grep for AuditLogPanel/AuditExportDialog outside components/audit still returns zero hits.
- **Root cause:** (1) src/renderer/App.tsx:144-152 — no `<Route path="/audit" .../>`; lines 15-26 lazy-load Dashboard/Settings/ProjectDetail/Search/FalsePositiveFilter/DependencyGraph/ExecutiveDashboard but there is no audit page module to route to at all. (2) src/renderer/components/shell/Sidebar.tsx:27-31 `MAIN_NAV` array has no entry for '/audit', so even a route would be undiscoverable without typing the URL manually. (3) src/renderer/components/audit/AuditLogPanel.tsx — `buildCurrentFilter()` (lines 85-93) and the filter UI (lines 183-244, 'Search' / 'Action Type' / 'Entity Type' only) have no date-range state or control at all, even though `AuditEventFilter.dateRange` (src/renderer/lib/audit/types.ts:20-24) already supports it and AuditExportDialog.tsx:31,90-95 already shows the exact preset pattern (7/30/90 days/all) to copy. So the PRD's explicit 'Filter by event type, date range, entity' bullet is unmet by the panel itself, independent of the routing gap.
- **Fix:** 1) Add src/renderer/pages/AuditLog.tsx (new, named export `AuditLog`), matching the Search.tsx/FalsePositiveFilter.tsx page convention: `<PageHeader title="Audit Log" description="Compliance record of CREATE, UPDATE, DELETE, SCAN, EXPORT, and SETTINGS_CHANGE events." />` followed by `<AuditLogPanel />` (from `@/components/audit`). 2) In App.tsx: add `const AuditLog = lazy(() => import('./pages/AuditLog').then((m) => ({ default: m.AuditLog })))` next to the other lazy pages, and `<Route path="/audit" element={<AuditLog />} />` inside the AppShell route, alongside `/settings`. 3) In Sidebar.tsx: add `{ to: '/audit', label: 'Audit Log', icon: History }` to `MAIN_NAV` (import `History` from lucide-react, or reuse `Activity` to match AuditLogPanel's own header icon — pick one, don't introduce a third convention). 4) In AuditLogPanel.tsx: add a `dateRangeFilter` state (`'all' | '7days' | '30days' | '90days'`, mirroring AuditExportDialog's existing preset pattern) rendered as a small button group in the filters panel; compute the resulting `{ start, end }` and fold it into both `loadEvents()`'s filter object (~lines 69-79) and `buildCurrentFilter()` (lines 85-93) so on-screen filtering and the export menu (FR-07.3) stay consistent.
- **Tests:** TDD order: 1) New src/renderer/components/shell/Sidebar.test.tsx: render `<SidebarContent collapsed={false} onNavigate={vi.fn()} />` inside a MemoryRouter with `useProjects` mocked to `[]`; assert `screen.getByRole('link', { name: /audit log/i })` has `href="/audit"`. Fails today (no such link) — passes after fix step 3. 2) New e2e/features/audit-log.spec.ts, plus a `navigateToAuditLog(page)` helper added to e2e/shared-helpers.ts mirroring `navigateToSearch`: after `resetAppState` and creating one project via `createProjectOnly`, click the sidebar 'Audit Log' link, assert the URL becomes `/audit`, and assert the events table shows a row with actionType `CREATE` and the created project's name in the Description column. This is the exact end-to-end proof the report demanded ('unreachable from the running application') — it fails today because there is no link/route to click, and would also fail if FR-07.1's CREATE logging were ever removed. 3) Extend src/renderer/components/audit/AuditLogPanel.test.tsx: in `beforeEach`, after `resetStore()`, use `useAuditStore.setState({ events: [...useAuditStore.getState().events, oldEvent, recentEvent] })` with two manually-constructed AuditEvent objects (addEvent's type signature omits `timestamp`, so events with controlled timestamps must be injected via `setState` directly) — one dated 40 days ago, one 2 days ago, each with a distinguishable `metadata.description`. Add a test that opens Filters, selects 'Last 7 days', and asserts only the recent event's description renders while the 40-day-old one does not. Fails today because there is no date-range control to interact with and no filtering occurs; passes after fix step 4. Asserting the actual row set (not just that a date control renders) is what proves the filter narrows results rather than just existing cosmetically.
- **Risk:** Low-medium. The route/nav addition is low risk. The date-range filter change touches `buildCurrentFilter`/`loadEvents`, which the export menu (FR-07.3) also depends on — a mistake there could silently break CSV/JSON/PDF export filtering too, so the FR-07.3 export test should be run in the same change/PR as this one.

### FR-07.3 — Audit Export — export logic is complete but unreachable (same routing gap); AuditExportDialog has zero tests

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** S · **Product code:** no
- **Since report:** Unchanged since the report — no commit since 2026-07-25 touched App.tsx, Sidebar.tsx, AuditLogPanel.tsx, or AuditExportDialog.tsx. Re-verified: src/renderer/lib/audit/auditExporters.ts (CSV/JSON/PDF, metadata: version/exportedAt/totalEvents, date-range filtering) is fully implemented and tested in isolation; the defect is purely reachability, exactly as the report stated.
- **Root cause:** Same routing gap as FR-07.2 (src/renderer/App.tsx:144-152, src/renderer/components/shell/Sidebar.tsx:27-31) — AuditLogPanel's own Export menu (AuditLogPanel.tsx:137-179, already correctly calling `exportAuditLogs` for csv/json/pdf via `buildCurrentFilter()`) and the standalone src/renderer/components/audit/AuditExportDialog.tsx (216 lines, full format/date-range/anonymize/includeFullState UI) are both fully implemented but unreachable because there is no `/audit` route or nav entry. Additionally, AuditExportDialog.tsx has zero test file, so even considered in isolation its behavior (selected options -> `exportAuditLogs` call shape) is unverified.
- **Fix:** No dedicated product code change beyond FR-07.2's fix: once the `/audit` route + nav entry + date-range filter land (FR-07.2), AuditLogPanel's inline Export menu becomes reachable and satisfies every FR-07.3 bullet end-to-end (CSV/JSON/PDF via auditExporters.ts, metadata already present, date-range via the now-shared `buildCurrentFilter()`). Recommend NOT additionally wiring AuditExportDialog.tsx into the same page — it would duplicate the same export feature twice in one screen (anonymize/includeFullState are its only extra options), which is speculative scope beyond what the PRD bullet requires (Simplicity First). Treat AuditExportDialog.tsx as pre-existing, currently-unwired richer UI; flag the 'wire it in as an Advanced Export option, or delete it' decision as a follow-up product call, out of scope for closing this PRD bullet. Do add the missing unit test for it (closes the report's explicit 'zero test file' flag) regardless of the wiring decision.
- **Tests:** 1) Extend the FR-07.2 e2e spec (e2e/features/audit-log.spec.ts) with 'should download a CSV export of the audit log from the /audit page': open the Export menu, click 'Export as CSV', `await page.waitForEvent('download')`, assert the suggested filename matches `/audit.*\.csv/i`. This is the concrete proof that the export path (not just the viewer) is reachable — fails today because there is no route to navigate to. 2) New src/renderer/components/audit/AuditExportDialog.test.tsx (does not currently exist): `vi.mock('@/lib/audit', () => ({ exportAuditLogs: vi.fn(), useAuditStore: <stub returning a fixed getStatistics()> }))`; render the dialog open; assert the default format is JSON and clicking the CSV/PDF buttons switches the selected format (aria-pressed or class-based assertion, matching ExportDialog.test.tsx's existing style); select 'Last 7 days' and click Export, then assert `exportAuditLogs` was called with `expect.objectContaining({ format: 'json', includeFullState: false, anonymize: false, filter: expect.objectContaining({ dateRange: expect.objectContaining({ start: expect.any(Date), end: expect.any(Date) }) }) })` — use a fuzzy/object-containing match rather than exact Date equality since the component computes `start`/`end` from `Date.now()` internally; also assert toggling the 'Include full state data' and 'Anonymize sensitive data' checkboxes flips the corresponding boolean in the next call. Both test files are new and fail-by-absence today (files don't exist) — write them, confirm they pass against the already-implemented dialog code (this task needs no production fix for the dialog itself, only the missing coverage).
- **Risk:** Low. This task piggybacks entirely on FR-07.2's routing fix — do not schedule it before FR-07.2 lands, since the e2e download test has nothing to navigate to until then.

---

## FR-09 Reporting

### FR-09.2 — Vulnerability report PDF renders no real charts/logo (text-only), table is unsorted and omits CVSS vector

- **Priority:** High · **Report:** GAP · **Now:** PARTIALLY_FIXED · **Effort:** L · **Product code:** yes
- **Since report:** The 2026-07-25 report's 'unreachable dead code' evidence is stale: commit ae29ea7 (2026-07-25) wired ReportPreview into ProjectDetail.tsx via a new 'Generate Report' button (src/renderer/pages/ProjectDetail.tsx:257-263, 390-396) feeding real data via buildReportData (src/renderer/pages/project-detail/helpers.ts:65-91), and commit 60442a6 (2026-07-27) gated that computation behind the dialog being open. ae29ea7 also fixed the fallback text extraction (empty/raw-HTML-leaking innerText -> working textContent-based htmlToPdfText). But the report's own caveat -- 'even if wired up... the chart bars, logo image, and table layout would not actually render' -- is exactly what remains true today: htmlToPdfText (src/renderer/lib/platform/serverAdapter.ts:86-95) discards all markup/images and keeps only bare text, so the PDF download button produces a document with no visual chart, no logo, and no color coding. The severity-sort and CVSS-vector gaps were not touched by any commit since the report.
- **Root cause:** (1) src/renderer/lib/services/reports/reportGenerator.ts:157-182 generatePDF() has exactly one PDF path: it renders HTML then calls getPlatform().generatePDF(htmlReport.content) (line 165), which resolves to src/renderer/lib/platform/serverAdapter.ts:297-316 -- a jsPDF renderer that calls htmlToPdfText() (serverAdapter.ts:86-95, used at line 301) and writes the result line-by-line with doc.text(). textContent extraction drops the <img> company-logo tag entirely and flattens the severity bar chart's <div class="bar-fill" style="height:...px;background-color:..."> (executiveReport.ts:207-219, rendered at :345-363) to empty divs, leaving only its numeric/label text -- duplicating the stats-grid numbers with zero visual shape. This is the ONLY reachable PDF-producing code path (grep confirms reportGenerator.ts is PlatformAPI.generatePDF's sole caller). (2) src/renderer/lib/services/reports/templates/executiveReport.ts: generateVulnerabilityTable (lines 448-488) renders vulnerabilities in whatever order ReportData.vulnerabilities arrives in -- no .sort() call touches the vulnerability list anywhere in reportGenerator.ts or executiveReport.ts (the only .sort at reportGenerator.ts:99 ranks topVulnerableComponents, not the table) -- so 'sorted by severity' is unmet. The table's columns (thead at :459-465, row mapping at :467-483, and the caller's field mapping at :563-570) include id/severity/cvssScore/description only; Vulnerability.cvssVector (src/shared/types.ts:171) is never read or rendered, so 'CVSS scores and vectors' is only half-satisfied.
- **Fix:** 1) src/renderer/lib/severity.ts: add an exported compareBySeverity(a, b) comparator built on the existing SEVERITY_ORDER constant (most-severe-first) -- the single source of truth per project convention, instead of hand-rolling another local severityOrder map. 2) src/renderer/lib/services/reports/templates/executiveReport.ts: in generateExecutiveReportHTML, sort a copy of data.vulnerabilities with compareBySeverity (CVSS desc as tiebreak, mirroring the existing pattern already used in src/renderer/lib/export/pdf.ts:404-409) before building the table; extend the row mapping (:563-570) to pass cvssVector: v.cvssVector, and extend generateVulnerabilityTable's param type + <thead>/<tbody> (:448-488) with a 'CVSS Vector' column rendering vuln.cvssVector || 'N/A'. This fixes both the HTML export and (per step 4) the PDF export in one place. 3) New file src/renderer/lib/services/reports/executiveReportPdf.ts exporting buildExecutiveReportPdf(data, options, metrics): jsPDF, built with jspdf + jspdf-autotable -- the library pair already used by src/renderer/lib/export/pdf.ts and prepareCompliancePdf, deliberately NOT html2canvas (considered and rejected: it would be a second, inconsistent PDF-rendering technique in the same codebase). Structure: header text + doc.addImage(...) for options.companyLogo when it is a data:image/png|jpeg URL (skip with a console.warn for unsupported formats like SVG -- documented limitation, not a crash); executive-summary risk badge as a filled rect + text, key findings as text lines; a REAL severity bar chart via doc.setFillColor + doc.rect(x,y,w,h,'F') sized per-bucket (reuse getSeverityColor -- export it from src/renderer/lib/export/pdf.ts rather than adding a third copy of the severity-color map); the vulnerability table via autoTable with the CVE/Severity/CVSS/Vector/Components/Description columns, sorted with the same compareBySeverity from step 1, severity-colored via didParseCell (same technique as src/renderer/lib/export/pdf.ts:192-200); recommendations as a numbered list; footer via the exported addFooter from src/renderer/lib/export/pdf.ts. 4) src/renderer/lib/services/reports/reportGenerator.ts generatePDF(): replace the getPlatform().generatePDF(...) branch (lines 164-173) with `const doc = buildExecutiveReportPdf(data, mergedOptions, metrics); const blob = doc.output('blob')`, returning content: blob, contentType: 'application/pdf'. The HTML path (generateHTML, unrelated 'HTML' download button) is untouched. 5) Cleanup (now-dead code as a direct result of step 4): remove generatePDF from PlatformAPI (src/renderer/lib/platform/types.ts:79) and its implementation + htmlToPdfText from src/renderer/lib/platform/serverAdapter.ts (lines 86-95, 297-316), after re-confirming no other caller exists.
- **Tests:** TDD order, failing-first: 1) src/renderer/lib/severity.test.ts -- add cases for compareBySeverity: critical<high<medium<low<none ordering, and equal-severity returns 0; why: a broken comparator would silently bury critical findings below low ones in every downstream consumer. 2) New src/renderer/lib/services/reports/templates/executiveReport.test.ts -- assert generateExecutiveReportHTML's output places a critical CVE's <tr> before a low-severity CVE's <tr> even when the input array is given in reverse order (index-position check on the HTML string) -- why: an unsorted report misdirects a reader skimming top-to-bottom toward the wrong fix first; and assert the output contains the vuln's literal cvssVector string and 'N/A' when it is undefined -- why: distinguishes the PRD's explicit 'scores AND vectors' bullet from score-only, which the current code satisfies only half of. 3) New src/renderer/lib/services/reports/executiveReportPdf.test.ts, mirroring the rigor of lib/export/pdf.test.ts: doc.addImage is called with the logo when companyLogo is a PNG/JPEG data URL (spy on the jsPDF instance) -- why: proves the logo is actually embedded as an image, not silently dropped like today; doc.rect/doc.setFillColor are called with per-severity fill colors and heights proportional to counts -- why: proves a real vector chart exists, not just text; autoTable's body rows are pre-sorted critical-first regardless of input order -- why: same intent as test 2 but at the PDF layer, which has its own (currently absent) sort call; a data:image/svg+xml logo does not throw and simply omits the image -- why: locks in the documented SVG limitation as intentional behavior instead of an accidental crash. 4) New reportGenerator.test.ts (none exists today) -- assert generatePDF() returns contentType 'application/pdf' with a Blob content, and that it no longer touches getPlatform().generatePDF (mock @/lib/platform and assert zero calls) -- why: proves the text-flattening round-trip is actually replaced, not merely supplemented. 5) Remove the htmlToPdfText describe block from src/renderer/lib/platform/serverAdapter.test.ts in the same change that deletes the function -- don't leave tests for deleted code. 6) Playwright: extend e2e/features/export-dialog.spec.ts (or add e2e/features/vulnerability-report.spec.ts) to open 'Generate Report' on a seeded offline project and click 'Download PDF', asserting a .pdf download fires with non-trivial byte size -- this is a coarse reachability smoke test only (Playwright cannot inspect PDF chart geometry), so it must not be treated as proof the chart/logo/sort/vector fixes work -- those are covered by tests 1-4; a test that only checks 'a file downloaded' cannot fail when the chart-rendering logic regresses, so it is explicitly supplementary, not a substitute.
- **Risk:** PlatformAPI.generatePDF is a public interface point; re-confirm at implementation time that reportGenerator.ts is still its only caller before deleting it (grep confirmed this during this validation, but code may have moved). jsPDF's addImage requires a format string matching the actual image bytes -- derive it from the data URL's declared MIME type rather than hardcoding, or it throws at runtime. SVG logos (allowed by the existing file input's accept="image/\*" in ReportPreview.tsx) will silently not render in the PDF under this design -- acceptable as the smallest credible slice, but worth a follow-up UX tweak (tighten accept to image/png,image/jpeg) if users hit it, not required to close this GAP. html2canvas was considered and explicitly rejected in favor of native jsPDF drawing to stay consistent with the codebase's one existing PDF-rendering convention (jsPDF + jspdf-autotable, used in lib/export/pdf.ts and prepareCompliancePdf) rather than forking a second technique -- flagging per Rule 7 in case a reviewer prefers the rasterization trade-off (pixel-perfect but non-selectable/larger-file PDF, one new dependency) instead.

### FR-09.3 — SOC 2 / ISO 27001 / HIPAA compliance reports

- **Priority:** Medium · **Report:** GAP · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** Commit 787c62b (2026-07-27, 'feat(reports): add framework compliance reports (FR-09.3, sections-only)') closed this gap after the 2026-07-25 report. It added: src/renderer/lib/export/types.ts COMPLIANCE_FRAMEWORK_META (soc2/iso27001/hipaa, with label + full standard name); src/renderer/lib/export/pdf.ts prepareCompliancePdf(project, framework, auditEvents) (lines ~604-784) producing a framework header, report metadata, an executive-summary table of plain-language counts, a due-diligence statement that explicitly disclaims any control-satisfaction claim, an audit-trail table (sourced via useAuditStore.getEventsForEntity, capped at 250 rows with a truncation notice), and an unremediated-critical-findings table (critical-severity vulns still present in the latest scan); and src/renderer/components/ComplianceReportDialog.tsx, wired into src/renderer/pages/ProjectDetail.tsx:264-270,384-388 behind a 'Compliance' button. Re-ran both test files during this validation: 58/58 tests pass (24 in pdf.test.ts's 'Compliance PDF Export (FR-09.3)' describe block, 4 in ComplianceReportDialog.test.tsx, plus the other PDF export suites in the same file).
- **Root cause:** None found on re-validation. The report's cited evidence (no SOC2/ISO27001/HIPAA templates anywhere in the codebase; the closest analog, iso21434ReportGenerator.ts, being a different framework and unwired) has been superseded by the 787c62b commit described above, which implements and wires the three PRD-named frameworks directly.
- **Fix:** No fix required for the PRD bullets as written -- all five (framework-specific templates for SOC 2/ISO 27001/HIPAA; required audit-trail information; due-diligence demonstration; unremediated-critical-findings highlighting; executive summary for non-technical stakeholders) are implemented and reachable from the UI. Do not add per-control mapping (e.g. formally asserting individual SOC 2 Trust Services Criteria or ISO 27001 Annex A controls are satisfied) speculatively -- the code's own doc comment (src/renderer/lib/export/pdf.ts:604-616) deliberately scopes this out as 'sections-only' to avoid making an unsubstantiated compliance claim; that would be a materially larger, separate feature (a control library + per-control evidence linking + likely legal/audit review) requiring its own PRD requirement, not a fix to this GAP.
- **Tests:** Existing coverage is adequate and was re-run clean (58/58): src/renderer/lib/export/pdf.test.ts 'Compliance PDF Export (FR-09.3)' (framework-specific header per framework; standard name + metadata; executive-summary counts derived from live vulnerabilities rather than possibly-stale project.statistics; critical-only filtering in the findings table; empty-state placeholder rows for zero critical findings and zero audit events; the 250-row audit-trail cap plus its truncation notice; due-diligence wording asserted to NOT claim control satisfaction; footer pagination) and src/renderer/components/ComplianceReportDialog.test.tsx (all three PRD frameworks are offered as buttons; generating for a chosen framework sources the project's audit trail via getEventsForEntity(project.id) and downloads a framework-tagged filename; defaults to SOC 2 when nothing is explicitly picked; a generation failure re-enables the button and does not close the dialog or fire a download, so the user can retry). No new unit tests are needed. Optional, non-blocking: a Playwright smoke test (extend e2e/features/export-dialog.spec.ts) that opens a seeded offline project, clicks Compliance -> ISO/IEC 27001 -> Generate PDF, and asserts a .pdf download fires, would add end-to-end reachability confidence beyond jsdom -- nice-to-have, not required to keep this closed.
- **Risk:** None for the current scope. The main risk is scope creep: a future ask to map findings to individual named controls per framework is a distinctly larger feature (control taxonomy, per-control evidence, likely audit sign-off workflow) and should be raised as a new requirement rather than folded into 'fixing' this already-closed GAP.

---

## FR-11 — Dependency Graph Visualization

### FR-11.1-a — No cycle detection/handling in buildGraphElements — 'Handle circular dependencies' bullet has zero implementation

- **Priority:** Low · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** S · **Product code:** yes
- **Since report:** Unchanged since 2026-07-25 report. Re-checked at HEAD: src/renderer/components/graph/utils.ts:43-88 (buildGraphElements) still maps every component.dependencies entry to a Cytoscape edge as-is with no cycle detection anywhere in the file, and grep for circular|cycle across src/renderer/components/graph and DependencyGraphPage.tsx still returns zero hits. Not touched by commit 1721df6 (that commit only fixed the fcose layout registration).
- **Root cause:** src/renderer/components/graph/utils.ts:70-85 — buildGraphElements() pushes an edge for every (component, depId) pair whenever the target exists in the component list, with no graph-theoretic awareness of cycles. Because Cytoscape's fcose is force-directed (not a DAG-layout algorithm), a real cycle (e.g. A→B→A from an npm peer-dependency loop in an actual SBOM) renders without crashing, but PRD FR-11.1 lists 'Handle circular dependencies' as a distinct bullet from 'Build directed acyclic graph' — i.e. cyclic input must be explicitly detected/flagged, not silently treated identically to acyclic edges. No such signal exists today, and no test proves cyclic input behaves any differently from acyclic input.
- **Fix:** Minimum slice — flag cyclic edges, don't restructure the graph: (1) In src/renderer/components/graph/utils.ts add `export function detectCycles(components: Component[]): Set<string>` using a standard 3-color (white/gray/black) DFS over the same dependency adjacency pattern already built in findPaths (utils.ts:129-135), returning the set of edge keys (`${sourceId}->${targetId}`) that participate in a cycle. (2) In buildGraphElements, after building `edges`, look up each edge's key in the detectCycles() result and set `isCycleEdge: true` on its `data` (extend the edge data shape — add `isCycleEdge?: boolean`). (3) In utils.ts getBaseStyles(), add one more stylesheet entry with selector `edge[?isCycleEdge]` (Cytoscape data-presence selector) styled distinctly (e.g. dashed 'line-style': 'dashed', a warning color) so a cyclic dependency is visually flagged without any new component/prop plumbing. No changes to DependencyGraph.tsx or DependencyGraphPage.tsx needed — pure utils.ts + types.ts change, buildGraphElements' return type/signature stays intact so current unit tests keep passing unmodified.
- **Tests:** Vitest, TDD order — write these first (fail against current code, since detectCycles doesn't exist and isCycleEdge is never set): (1) NEW `detectCycles` suite: (a) 3-node cycle A→B→C→A returns exactly the 3 edge keys; (b) pure DAG (A→B→C, no back-edge) returns an empty set; (c) self-loop (A→A) is detected; (d) one disjoint cyclic pair plus one unrelated acyclic branch only flags the cyclic edges. (2) Extend the existing buildGraphElements suite (DependencyGraph.test.tsx ~590-704): 'should mark edges participating in a circular dependency as isCycleEdge' — 3 components forming A→B→C→A, assert all 3 returned edges have `data.isCycleEdge === true`; add a 4th sibling component with a normal acyclic dependency and assert its edge has `isCycleEdge` falsy. Why this matters (Rule 9): without it, a future change to how dependency edges are derived (e.g. switching from the raw `dependencies` array to a resolved lockfile graph) could silently drop or mis-render a real cycle with no test catching the regression — these tests pin the invariant that cyclic input must always be distinguishable from acyclic input, not just 'doesn't crash'.
- **Risk:** Low — purely additive (new optional edge field + new pure function + one new stylesheet rule); does not change node/edge count or existing severity styling for the overwhelming majority of real-world acyclic SBOMs, so existing buildGraphElements/rendering tests are unaffected.

### FR-11.1-b — No graph-size cap — 'Limit graph size for performance' bullet has zero implementation

- **Priority:** Low · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Unchanged since 2026-07-25 report. Re-checked at HEAD: grep for MAX_NODES/limit/slice-based caps across src/renderer/components/graph and DependencyGraphPage.tsx still returns zero hits; buildGraphElements (utils.ts:43-88) still maps components.length nodes unconditionally.
- **Root cause:** src/renderer/components/graph/utils.ts:47 (`components.map(...)`) builds one Cytoscape node per component with no upper bound, and DependencyGraphPage.tsx:134-142 hands `filteredComponents` straight to `<DependencyGraph>`, whose useMemo (DependencyGraph.tsx:46) feeds every node/edge into `cy.layout(DEFAULT_LAYOUT_CONFIG).run()` (fcose, DependencyGraph.tsx:92/119) unconditionally on every mount/update. fcose's iterative force simulation degrades badly well before NFR-02.2's 50,000-component target, and there is no truncation or user-facing warning anywhere in the module.
- **Fix:** Minimum slice — cap node/edge count in buildGraphElements, surface a banner, don't attempt to actually solve 50k-scale rendering (that is NFR-02.2's job, a much larger perf-architecture item — out of scope here): (1) Add `export const MAX_GRAPH_NODES = 500` to src/renderer/components/graph/types.ts next to the other DEFAULT\_\* constants (500 is a starting judgment call for keeping fcose responsive on typical hardware — flag for a quick perf sanity check rather than treating it as final). (2) In buildGraphElements (utils.ts), if `components.length > MAX_GRAPH_NODES`, operate on `components.slice(0, MAX_GRAPH_NODES)` for both node and edge construction (edges to a truncated-out target are already dropped by the existing `components.some(c => c.id === depId)` guard at utils.ts:74, so no dangling-edge cleanup is needed). Keep the function's return type (`cytoscape.ElementsDefinition`) unchanged. (3) In DependencyGraphPage.tsx, import `MAX_GRAPH_NODES` from '@/components/graph/types' and, when `filteredComponents.length > MAX_GRAPH_NODES`, render a small warning line (plain text, matching the existing footer's style) e.g. 'Showing first {MAX_GRAPH_NODES} of {filteredComponents.length} components — narrow the filter to see the rest' directly above the `<DependencyGraph>` container — no new dialog/toast needed, this is a static derived-state banner.
- **Tests:** Vitest, TDD order: (1) Extend buildGraphElements suite (DependencyGraph.test.tsx): 'should cap nodes at MAX_GRAPH_NODES for very large component sets' — generate `MAX_GRAPH_NODES + 50` mock components with sequential dependencies (comp-0→comp-1→...), call buildGraphElements, assert `elements.nodes.length === MAX_GRAPH_NODES` and every edge's source/target id is within the retained range — fails today (no cap exists), passes once the slice is added. (2) NEW src/renderer/pages/DependencyGraphPage.test.tsx (none exists today — see FR-11.2-a, same new file): mock `useProjects` to return a project with `MAX_GRAPH_NODES + 10` components and assert the truncation banner text is visible and the `components` prop passed to a mocked `<DependencyGraph>` has length `MAX_GRAPH_NODES`; a second case with `MAX_GRAPH_NODES - 1` components asserts the banner is absent and the full list is passed through unmodified. Why this matters (Rule 9): a test that only checks 'graph renders' for a tiny fixture (as all current tests do) can never fail when the cap is silently removed in a refactor — these tests directly encode the NFR-02.2-adjacent performance budget the PRD bullet exists to protect.
- **Risk:** Medium — picking too low a cap silently hides real components from the user; the banner is mandatory (not cosmetic) to avoid a 'why don't I see all my components' support issue. Also must apply the cap AFTER DependencyGraphPage's existing severity/vulnerable-only filtering (it already narrows scope), not before, or a user's explicit filter choice could be defeated by truncation ordering.

### FR-11.2-a — Node click only logs to console — 'Node selection for details' shows nothing to the user

- **Priority:** Low · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Unchanged since 2026-07-25 report. Re-checked at HEAD: DependencyGraphPage.tsx:62-64 handleNodeClick is still `console.log('[DependencyGraphPage] Node clicked:', component.name)` — identical to the cited evidence, not touched by commit 1721df6.
- **Root cause:** DependencyGraphPage.tsx:62-64 — the `onNodeClick` callback wired into `<DependencyGraph onNodeClick={handleNodeClick} />` (DependencyGraphPage.tsx:137) and genuinely invoked by DependencyGraph.tsx's tap handler (DependencyGraph.tsx:49-61, unit-tested) does nothing user-visible; it only console.logs the component name. No details panel/modal is ever shown.
- **Fix:** Reuse the existing, already-tested component-details UI instead of building new UI (Rule 8/Rule 2): src/renderer/pages/ProjectDetail.tsx already shows a component's vulnerabilities via `<ComponentVulnerabilitiesPopup component=... vulnerabilities=... open=... onClose=... onViewVulnerability=... />` (ProjectDetail.tsx:411-424, component at src/renderer/components/ComponentVulnerabilitiesPopup.tsx) built on the shared `ui/dialog.tsx` primitive. In DependencyGraphPage.tsx: (1) add `const [selectedComponent, setSelectedComponent] = useState<Component | null>(null)` and `const [showDetail, setShowDetail] = useState(false)`; (2) replace the console.log body of handleNodeClick with `setSelectedComponent(component); setShowDetail(true)`; (3) import `getVulnerabilitiesForComponent` from '../pages/project-detail/helpers' (already used by ProjectDetail.tsx, same signature `(project, componentId) => Vulnerability[]`) to compute the popup's vulnerabilities prop; (4) render `{selectedComponent && <ComponentVulnerabilitiesPopup component={selectedComponent} vulnerabilities={getVulnerabilitiesForComponent(project, selectedComponent.id)} open={showDetail} onClose={() => { setShowDetail(false); setSelectedComponent(null) }} onViewVulnerability={() => navigate(`/project/${project.id}`)} />}` near the bottom of the returned JSX (navigate is already imported/used for the 'Go to Dashboard' button). Deep-linking onViewVulnerability straight to a specific vulnerability's detail view is out of scope for this PRD bullet (which only requires showing details on node selection) — navigating to the project page is the minimum credible slice.
- **Tests:** Vitest, TDD order — NEW file src/renderer/pages/DependencyGraphPage.test.tsx (no test file exists for this page today): mock `@/store/useStore`'s `useProjects` (match ProjectDetail.test.tsx's store-mocking convention) and mock `@/components/graph/DependencyGraph` to render a plain `<button onClick={() => props.onNodeClick(mockComponent)}>trigger-click</button>` (avoids re-mocking cytoscape at the page level; DependencyGraph's own onNodeClick wiring is already covered by DependencyGraph.test.tsx). Cases: (1) 'clicking a node opens the component details popup showing its name, version, and vulnerability count' — click the mocked trigger, assert the popup dialog is visible with the component's name/version text and lists its vulnerabilities (fails today because nothing renders — only a console.log fires). (2) 'closing the popup clears the selected component' — open then close, assert dialog is gone and a second click after close still works (guards against stale state re-showing old data). (3) 'onViewVulnerability navigates to the project detail page' — click a vulnerability row inside the popup, assert the mocked `useNavigate` was called with `/project/{id}`. E2E addition to e2e/features/dependency-graph.spec.ts: because Cytoscape renders to a <canvas> (not clickable by Playwright selector), use the graph's own accessible fallback listbox (DependencyGraph.tsx:310-334, `role='option'` per component, already wired to call onNodeClick via activateNode) — after uploading the 5-component sample-cyclonedx fixture and navigating to /graph, focus the listbox and click/Enter on one `role='option'` entry, then assert the details dialog becomes visible with that component's name.
- **Risk:** Low — reuses an existing, already-unit-tested dialog component; the only new logic is 2 state variables + 1 helper call in DependencyGraphPage.tsx.

### FR-11.2-b — Path highlighting is implemented but completely unreachable — no UI ever supplies highlightPath

- **Priority:** Low · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Unchanged since 2026-07-25 report. Re-checked at HEAD: grep for highlightPath/findShortestPath/findPaths across src/renderer confirms these identifiers are used ONLY inside src/renderer/components/graph/{DependencyGraph.tsx,utils.ts,types.ts,index.ts,DependencyGraph.test.tsx} — no page or other component ever imports/calls them. Not touched by commit 1721df6.
- **Root cause:** DependencyGraph.tsx:122-171 correctly renders `highlightPath`/`clearHighlight` when supplied, and utils.ts:119-217 (findPaths/findShortestPath/getPathEdges) correctly computes a path between two component IDs — both are thoroughly unit-tested in isolation (DependencyGraph.test.tsx's 'Path Highlighting' describe block, ~lines 389-440). But DependencyGraphPage.tsx (the only real caller of `<DependencyGraph>`) never passes `highlightPath`, and there is no control anywhere in the product (no 'from'/'to' picker, no button) for a user to request a path — the feature exists in code but is dead from the user's perspective.
- **Fix:** Minimum slice — add the missing UI control, wire it to the already-correct utility functions (no changes needed to DependencyGraph.tsx/utils.ts): (1) In DependencyGraphPage.tsx add two `<select>` elements ('From' / 'To', each `<option>` per `components` entry keyed by id, labelled `${name} ${version}`) plus a 'Highlight Path' button and a 'Clear Path' button, placed in the PageHeader actions area alongside the existing severity filter — reuse the same `<select>` Tailwind classes already used for the severity dropdown (DependencyGraphPage.tsx:104-114) for visual consistency. (2) Add `const [fromId, setFromId] = useState(''); const [toId, setToId] = useState(''); const [highlightedPath, setHighlightedPath] = useState<string[] | null>(null)`. (3) Import `findShortestPath` from '@/components/graph' (already re-exported via index.ts:18). On 'Highlight Path' click: `const path = findShortestPath(components, fromId, toId); if (path) setHighlightedPath(path); else toast.error('No path found between these components')` (reuse the existing `toast` helper from './Toaster', already used by ComponentVulnerabilitiesPopup.tsx for the same kind of user feedback — match convention). On 'Clear Path' click or when either select changes: `setHighlightedPath(null)`. (4) Pass `highlightPath={highlightedPath ?? undefined}` and `clearHighlight={!highlightedPath}` to `<DependencyGraph>` (DependencyGraphPage.tsx:134-142).
- **Tests:** Vitest, TDD order — same NEW DependencyGraphPage.test.tsx as FR-11.2-a (mock DependencyGraph to capture props instead of rendering the real canvas; use the REAL findShortestPath/utils.ts, do not mock it, since that is the exact wiring under test): (1) 'selecting two connected components and clicking Highlight Path passes the shortest path to DependencyGraph' — 3 mock components A→B→C (A.dependencies=['B'], B.dependencies=['C']); select A as From, C as To, click Highlight Path; assert the mocked DependencyGraph received `highlightPath === ['A','B','C']` (fails today — the prop is never passed at all, currently always undefined). (2) 'shows an error toast and does not set highlightPath when no path exists' — two unrelated mock components with no dependency edge; select both, click Highlight Path; assert a 'No path found' toast/error appears and `highlightPath` stays undefined. (3) 'Clear Path resets the highlighted path' — highlight a path, click Clear, assert `highlightPath` prop is undefined again. E2E: the current sample-cyclonedx.json fixture has no `dependencies` block (all 5 components are dependency-less) — a 'path found' e2e case needs a NEW small fixture (e.g. e2e/fixtures/sbom/sample-cyclonedx-with-deps.json, 3 components with an explicit CycloneDX `dependencies` array A→B→C). Add one e2e case in dependency-graph.spec.ts asserting the From/To selects and Highlight/Clear buttons are visible and interactable, and that selecting an unrelated pair (or the existing dependency-less fixture) surfaces the 'No path found' toast — this proves the controls are reachable end-to-end without depending on canvas-pixel inspection (Playwright cannot assert Cytoscape's internal edge-highlight styling since it's canvas-rendered).
- **Risk:** Low-medium — purely additive UI; the exact From/To control placement/design is a UX judgment call not specified by the PRD bullet (which only requires the capability to exist), so a quick design sanity-check is worth doing before implementation, but no existing behavior is touched.

### FR-11.2-c — No 'Export as image' feature exists anywhere for the dependency graph

- **Priority:** Low · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** S · **Product code:** yes
- **Since report:** Unchanged since 2026-07-25 report. Re-checked at HEAD: grep for html-to-image/dom-to-image/toDataURL/.png(/.jpg( across src/renderer/components/graph and DependencyGraphPage.tsx still returns zero hits; no export button exists in the zoom-controls block (DependencyGraph.tsx:337-390).
- **Root cause:** No code path anywhere converts the rendered Cytoscape canvas to a downloadable image. DependencyGraph.tsx's showControls block (lines 337-390) only renders Zoom In/Zoom Out/Fit/Reset buttons; there is no fifth 'Export' button and no handler.
- **Fix:** Minimum slice — Cytoscape ships a built-in `cy.png({...})` method (returns a data URI), so no new npm dependency is needed (confirms the report's finding that no html-to-image/dom-to-image package was ever added is fine — it was never necessary). (1) In DependencyGraph.tsx add one more button to the existing showControls block (after Reset, DependencyGraph.tsx:374-388) labelled/titled 'Export as Image'. (2) Add `const handleExportImage = useCallback(() => { if (!cyRef.current) return; const dataUrl = cyRef.current.png({ full: true, scale: 2, bg: '#ffffff' }); const link = document.createElement('a'); link.href = dataUrl; link.download = 'dependency-graph.png'; link.click() }, [])` — this mirrors the exact download-trigger pattern already used by `downloadJson` in src/renderer/lib/export/json.ts:224-233 (createElement('a') → set href/download → .click()), matching existing convention (Rule 11) rather than inventing a new pattern or pulling in a new library.
- **Tests:** Vitest, TDD order — extend DependencyGraph.test.tsx: (1) add `png: vi.fn(() => 'data:image/png;base64,mock')` to the existing `mockCy` object (DependencyGraph.test.tsx:48-61). (2) 'clicking Export as Image calls cy.png() and triggers a download' — render the graph with 1+ components, click the new export button (5th button in the controls group), assert `mockCy.png` was called with the chosen options and that a download was triggered — match the exact assertion style used in src/renderer/lib/export/json.test.ts:247 ('should trigger JSON file download', which mocks `document.createElement`) so the test asserts `link.click()` fired via the mocked anchor rather than trying to validate real PNG bytes. Why this matters (Rule 9): a test that only checks the button exists (no click assertion) could never fail if the handler were later left as a no-op stub — asserting `cy.png()` was actually invoked with the click ties the test to the real business behavior. E2E addition to dependency-graph.spec.ts: upload the 5-component sample fixture, navigate to /graph, click the export button, and assert via `page.waitForEvent('download')` that a download fires with a `.png` suggested filename — do not assert on the PNG's pixel content (headless canvas rendering fidelity is not the point of this test, only that the export action is wired end-to-end).
- **Risk:** Low — Cytoscape's own `.png()` API, no new dependency; isolated to a single new button/handler inside DependencyGraph.tsx.

### FR-11.2-d — Interactive graph with zoom/pan — fcose runtime crash — confirm fix and note residual test-coverage gap

- **Priority:** Low · **Report:** GAP · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** FIXED by commit 1721df6 (2026-07-30, after the 2026-07-25 report). Verified at HEAD: package.json:80 now lists `cytoscape-fcose: ^2.2.0` as a real dependency (report found it absent), and DependencyGraph.tsx:20 calls `cytoscape.use(fcose)` at module load, before any of the three `cy.layout({name:'fcose'})` call sites (DependencyGraph.tsx:72, 92, 119) can run — this is exactly the missing registration the report identified as CRITICAL ('No such layout `fcose` found' at runtime for any project with real components). The commit also hardened e2e/features/dependency-graph.spec.ts's 'should update footer counts when filter changes' test (now lines 240-258) to upload a real 5-component SBOM before navigating to /graph, instead of only ever exercising the empty-state path as the report described.
- **Root cause:** Already fixed — see staleNote. No remaining defect in the registration itself.
- **Fix:** No further product code change required for the crash itself. Optional low-priority test-clarity hardening only: the current e2e regression guard (dependency-graph.spec.ts:240-258) is indirect — it proves the page doesn't crash (via App.tsx:129's app-wide ErrorBoundary swallowing a hypothetical re-regression and blanking the footer) rather than directly asserting the graph rendered or that zoom/pan works. No code change is needed to close this finding; the suggestion below is purely optional test strengthening, not a requirement to satisfy the PRD bullet (which is already met: DEFAULT_LAYOUT_CONFIG (fcose) runs without throwing, and the pre-existing Zoom In/Out/Fit/Reset controls plus Cytoscape's default drag-to-pan satisfy 'zoom/pan').
- **Tests:** Optional hardening only (not blocking, not required to close this item): add one more e2e assertion in the same 'should update footer counts when filter changes' test (dependency-graph.spec.ts) or a new adjacent case that directly checks the populated-graph DOM state is NOT the empty-state placeholder — e.g. `await expect(page.getByText('No components to display')).not.toBeVisible()` right after uploading the 5-component fixture and navigating to /graph — this converts the currently-indirect 'footer survived' proxy into a direct assertion that the real Cytoscape/fcose code path rendered, which would fail immediately (not just via a crash) if the layout registration ever regressed again.
- **Risk:** Low — already fixed and has working (if indirect) e2e coverage; the only residual risk is test-clarity, not product behavior.

---

## FR-10 Settings and Configuration

### FR-10.1 — Theme and Appearance — no test proves 'system' theme actually resolves OS preference; font-size persistence untested

- **Priority:** Medium · **Report:** SHALLOW · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** no
- **Since report:** The report-era 'persist preferences across sessions' concern for THEME is now resolved: memory note theme-persist-reload.md (RESOLVED 2026-07-30) shows e2e/critical-flows/settings.spec.ts:65 'should persist the selected theme across a full page reload' now passes, and store/useStore.test.ts has a 'persistence across reload (rehydration)' suite. That part of the SHALLOW grade no longer applies. What is still genuinely untested: (1) the 'System theme detection' bullet — no unit or e2e test ever exercises the settings.theme === 'system' branch against a real or mocked prefers-color-scheme value, only that the 'system' button is visible (settings.spec.ts:38-47); (2) font-size preference persistence across reload is never asserted (only that clicking 'large' calls updateSettings({fontSize:'large'}), Settings.test.tsx:329).
- **Root cause:** src/renderer/App.tsx:108-121 computes `settings.theme === 'system' ? matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light' : settings.theme` inside a useEffect keyed on [settings.theme] — this is correct, but zero test (unit or e2e) drives it: e2e/critical-flows/settings.spec.ts only clicks 'light'/'dark' explicitly and never emulates a color-scheme + selects 'system'; there is no App.test.tsx. Similarly no test reloads the page after changing fontSize (only the theme-specific reload test exists at settings.spec.ts:65).
- **Fix:** No product code change required — the implementation already satisfies the PRD bullet ('System theme detection', 'Persist preferences across sessions'); this is a test-coverage gap only. Do not add a live matchMedia change-listener (not required by the PRD bullet, would be speculative scope creep per Rule 2).
- **Tests:** Add to e2e/critical-flows/settings.spec.ts (Playwright, real browser matchMedia): (1) 'should resolve system theme from OS dark-mode preference' — call page.emulateMedia({ colorScheme: 'dark' }) before navigating, click the 'system' theme button, assert html has class 'dark' and not 'light' (proves the OS-preference branch, not just that the button exists); (2) 'should resolve system theme from OS light-mode preference' — emulateMedia({ colorScheme: 'light' }), click 'system', assert html has 'light' not 'dark' (the WHY: without this, a regression that always resolves system->dark regardless of OS preference would still pass every existing test); (3) 'should persist the selected font size across a full page reload' — click 'large', page.reload(), assert document.documentElement.style.fontSize is '16px' (mirrors the existing theme-reload test's pattern at settings.spec.ts:65-83). All three are pure additions to the existing describe block — no fixtures/mocks change.
- **Risk:** Low — additive tests only, no production code touched, so no regression risk. The only risk is flakiness from emulateMedia timing, mitigated by calling it before navigation (Playwright applies it to the whole browsing context).

### FR-10.2 — Settings Profiles — 'Set Default' has no UI affordance despite working store logic

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** S · **Product code:** yes
- **Root cause:** src/renderer/components/SettingsProfileCard.tsx:90-115 (the Actions row) only renders 'Switch to Profile'/'Active Profile' and a delete button — there is no 'Set Default' control. src/renderer/pages/settings/ProfilesSection.tsx:13-58 never calls the store's setDefaultSettingsProfile action (confirmed present and unit-tested at the store layer: src/renderer/store/useStore.ts:157-170 and src/renderer/lib/settings/profiles.ts:177-192 setDefaultProfile), so the PRD bullet 'Set default profile' has no reachable path in the actual UI, only test-only direct store calls.
- **Fix:** 1) src/renderer/components/SettingsProfileCard.tsx: add a required prop `onSetDefault: (profileId: string) => void`; add a 'Set Default' button in the Actions row (next to 'Switch to Profile'/'Active Profile'), disabled (or hidden) when `profile.isDefault` is true, mirroring the existing disabled-delete-when-active pattern (title='Already the default profile' when disabled). 2) src/renderer/pages/settings/ProfilesSection.tsx: destructure `setDefaultSettingsProfile` from useStore (already exported, useStore.ts:40/157), add a `handleSetDefaultProfile` wrapped in the same try/catch+alert pattern as handleDeleteProfile/handleSwitchProfile (lines 40-58), and pass `onSetDefault={handleSetDefaultProfile}` to <SettingsProfileCard>. No changes needed to profiles.ts or useStore.ts — that logic is already correct and tested.
- **Tests:** TDD order: (1) src/renderer/components/SettingsProfileCard.test.tsx — first add a failing test 'should render Set Default button for non-default profile' (queries for the new button; fails because it doesn't exist yet), then 'should call onSetDefault when Set Default is clicked' asserting the callback receives profile.id (the WHY: proves clicking the control actually reaches the handler, not just that a button exists), then 'should not render/should disable Set Default button for the already-default profile' (prevents a no-op action being offered). Update the existing `renderCard` helper (line 53-57) to pass a new `mockOnSetDefault` prop since it's a required prop — this will surface any other call sites needing the same update. (2) New src/renderer/pages/settings/ProfilesSection.test.tsx (does not exist yet) — mock useStore to expose settingsProfiles/setDefaultSettingsProfile, render ProfilesSection, click a non-default profile's 'Set Default' button, assert `setDefaultSettingsProfile` was called with that profile's id; add an error-path case asserting `alert` fires when the store throws (matches handleDeleteProfile's existing error-alert convention).
- **Risk:** Low — additive UI control wired to an already-tested store action; no changes to persistence format or existing profile behavior.

### FR-10.3 — Database Configuration — 'Bandwidth limiting for updates' and 'Storage location for database' have no UI or enforcement

- **Priority:** High · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Root cause:** Bandwidth: `DatabaseUpdateSchedule.bandwidthLimitKBps` (src/shared/types.ts:371, default 0 in src/shared/constants.ts:105) is defined but never read anywhere outside test fixtures (confirmed by grep — zero non-test references); `SyncConfigUpdate` (src/shared/types/ipc.ts:642-644) doesn't even carry the field, so there's no client->server path for it, and the NVD API client (server/database/nvd/nvdApiV2Client.ts) only has a request-count RateLimiter (lines 458-499) with no byte-rate throttle. Storage location: server/config.ts:14-25 derives DB_PATH solely from the `DATA_DIR` env var at process start; there is no field anywhere in the type system, no read endpoint (GET /stats at server/routes/database.ts:334-357 returns totalCves/lastUpdate/dbSize but not the path), and no write endpoint — a user cannot see or change it from the UI at all. Additionally (found during re-validation, not in the original report): PUT /config/storage and PUT /config/perf (server/routes/database.ts:792-821) are literal no-ops that `console.log` and return `{success:true}` without persisting anything — so even the storage/perf fields that DO have UI (maxSizeMB, pruneOldCves, pruneOlderThanYear) are decorative server-side. That stub-ness is adjacent to, but distinct from, this task's two missing bullets; flagging as a risk, not fixing it here (out of scope per Rule 2 — the PRD bullets in question are bandwidth + storage-location, not prune enforcement).
- **Fix:** Storage location — this is genuinely the smaller of the two, but 'live relocation of an open better-sqlite3 file' is out of scope as a large/risky feature (needs safe DB-handle close/reopen, cross-drive move, Windows file-lock handling, and a restart story) — ship the smallest credible slice: (1) server/routes/database.ts GET /stats: add `dbPath: config.DB_PATH` to the response (import { config } from '../config.js'); (2) src/shared/types/ipc.ts DatabaseStats: add `dbPath: string`; (3) src/renderer/pages/Settings.tsx: add a read-only 'Storage Location' row in the existing Storage Management block (~lines 1005-1082), sourced from the existing getStats() call already wired at lines 588-591/626-630/667-671, with helper text 'Set via the DATA_DIR environment variable; requires an application restart to change.' Explicitly flag in the PR/ticket that a UI-driven live move is a separate, larger follow-up requiring a product decision on downtime/restart semantics — do not attempt it in this slice. Bandwidth limiting: (1) src/shared/types/ipc.ts SyncConfigUpdate: add `bandwidthLimitKBps?: number`; (2) new server/database/nvd/bandwidthThrottle.ts exporting a pure `computeThrottleDelayMs(bytesDownloaded: number, kbpsLimit: number): number` (returns 0 when kbpsLimit <= 0); (3) server/database/nvd/nvdApiV2Client.ts: accept `bandwidthLimitKBps` in its constructor options, and after each page response, measure the response payload size and `await this.delay(computeThrottleDelayMs(bytes, this.bandwidthLimitKBps))` before the next request (reuses the existing private `delay()` at line 1093); (4) server/routes/database.ts GET/PUT /config/sync (lines 751-790): extend both to read/persist `bandwidthLimitKBps` (mirror how autoSyncIntervalHours is persisted via deltaSync — add an equivalent setter on the delta-sync/bulk-download manager, e.g. `setBandwidthLimitKBps(kbps)`, threaded into `createNvdApiV2Client` calls in bulkDownloadManager.ts/nvdDeltaSync.ts); (5) src/renderer/pages/Settings.tsx: add a numeric input ('Bandwidth Limit (KB/s, 0 = unlimited)') next to the existing Sync Schedule control (~lines 931-1003), calling `getPlatform().database.updateSyncConfig({ bandwidthLimitKBps: value })`.
- **Tests:** TDD order: (1) new server/database/nvd/bandwidthThrottle.test.ts — failing-first unit tests: `computeThrottleDelayMs(0, 100)` === 0; `computeThrottleDelayMs(1024, 0)` === 0 (unlimited); `computeThrottleDelayMs(102400, 100)` ≈ 1000ms (100KB at 100KB/s should take ~1s) — encodes the WHY: a wrong formula (e.g. inverted, or ignoring kbpsLimit<=0) would fail these concrete numeric assertions, not just 'returns a number'. (2) server/routes/database.test.ts (or wherever config/sync is currently tested — check for existing coverage first): PUT /config/sync with `{bandwidthLimitKBps: 500}` returns success and a subsequent GET /config/sync echoes it back; PUT with a negative/non-numeric value is rejected (matches the existing `Invalid syncInterval` rejection pattern at line 772-776). (3) GET /stats: assert response.stats.dbPath equals config.DB_PATH (a fake/mismatched path here means the UI would show the wrong location). (4) src/renderer/pages/Settings.test.tsx: extend the existing 'Storage and Performance Settings Changes' block with a case asserting `updateSyncConfig` is called with `{bandwidthLimitKBps: <value>}` when the new input changes, and a rendering case asserting the Storage Location text is displayed from `getStats()`'s `dbPath`. (5) Do NOT add an e2e test that asserts real network throttling (the seeded offline e2e DB has no live NVD calls) — cap verification at the unit level for the throttle math and the config round-trip.
- **Risk:** Medium — the bandwidth-throttle wiring touches the NVD sync/download hot path (nvdApiV2Client.ts), so a bug in computeThrottleDelayMs or its wiring could visibly slow down or (if inverted) not visibly change bulk-download/delta-sync behavior; keep the default (0/unlimited) behavior byte-for-byte identical to today when the setting is untouched, and cover that default explicitly in tests. The storage-location slice is low-risk (read-only) by design specifically to avoid the higher-risk live-relocation feature.

### FR-10.4 — Notification Preferences — enable/disable, desktop toggle, and category prefs have zero settings UI

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** S · **Product code:** yes
- **Root cause:** All the business logic already exists and works: src/renderer/lib/notifications/notificationsStore.ts:17-19 (updatePreferences/setCategoryEnabled actions) and :22-31 (DEFAULT_PREFERENCES with enabled/desktopEnabled/categories), and notificationService.ts:16-56,150 genuinely branches on `preferences.desktopEnabled`/`preferences.enabled` when deciding whether to show a browser Notification. But there is no rendered control anywhere: src/renderer/components/NotificationCenter.tsx only reads `preferences.enabled` (line 19,90) to decide whether to show its bell icon — it never lets the user change anything — and grep across src/renderer/pages/Settings.tsx and pages/settings/\*.tsx for 'notif' returns zero hits. The only exercised path is tests calling `useNotificationsStore.getState().updatePreferences(...)` directly.
- **Fix:** 1) New src/renderer/pages/settings/NotificationsSection.tsx, self-contained like AppearanceSection.tsx (reads/writes its own store, not the main useStore): use `useNotificationPreferences()` + `useNotificationsStore()` actions directly (they're a separate zustand store, not part of AppSettings — do not add these fields to AppSettings/profiles, since notification prefs are correctly global/app-level, not per-profile). Render: (a) a top-level 'Enable Notifications' toggle -> `updatePreferences({ enabled })`; (b) a 'Desktop Notifications' toggle -> `updatePreferences({ desktopEnabled })`, disabled when `enabled` is false; (c) four category toggles (critical_vuln, scan_complete, update_available, system) -> `setCategoryEnabled(category, value)`, each disabled when `enabled` is false. Match the existing toggle markup pattern already used in Settings.tsx (role='switch', aria-checked, the same translate-x-1/6 pill styling, e.g. lines 1045-1059). 2) src/renderer/pages/settings/SettingsNav.tsx: add `{ id: 'notifications', label: 'Notifications', icon: Bell }` to SECTIONS (Bell already imported/used elsewhere in the codebase's icon set). 3) src/renderer/pages/Settings.tsx: import and mount `<NotificationsSection />` alongside `<AppearanceSection />` (~line 737).
- **Tests:** TDD order: (1) new src/renderer/pages/settings/NotificationsSection.test.tsx — failing-first: render with a mocked useNotificationsStore (matching the notificationsStore.test.ts mocking convention already in the repo), assert the four category toggles and two top-level toggles are visible with correct aria-checked reflecting the store state; click 'Desktop Notifications' -> assert `updatePreferences` called with `{desktopEnabled: false}` (or true); click the 'critical_vuln' category toggle -> assert `setCategoryEnabled('critical_vuln', false)` called (the WHY: this is the exact behavior notificationService.ts:150 branches on — a broken wire here means a user who thinks they disabled critical-vuln alerts still gets them); assert category toggles are disabled (not clickable) when the top-level 'enabled' is false, mirroring notificationsStore's `addNotification` early-return guard at notificationsStore.ts:43. (2) Add one Playwright case to e2e/critical-flows/settings.spec.ts: 'should toggle desktop notifications from settings' — click Settings, toggle 'Desktop Notifications' off, reload, assert it's still off (via the visible switch state) — proves persistence through the notificationsStore's own `persist` middleware (notificationsStore.ts:99-105), the same reload-survives pattern already established for theme.
- **Risk:** Low — purely additive UI wired to a pre-existing, already-tested store; no changes to notificationsStore.ts or notificationService.ts logic.

### FR-10.5 — CVSS Configuration — cvssVersion/showCvssBreakdown/severityThreshold are dead settings fields with zero UI or consumption

- **Priority:** Low · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Root cause:** Confirmed unchanged from the report at HEAD: `cvssVersion`/`showCvssBreakdown` exist only as AppSettings fields (src/shared/types.ts:434-435), defaults (src/renderer/lib/settings/profiles.ts:302-303), and a zod schema used only for profile import/export (src/renderer/lib/settings/importExport.ts:71-72) — grep confirms zero UI and zero functional read of `settings.cvssVersion`/`settings.showCvssBreakdown` anywhere. The actual CVSS breakdown toggle in src/renderer/components/VulnerabilityDetailModal.tsx:25 is `React.useState(false)`, fully disconnected from settings. `severityThreshold`/`severityThresholds` does not exist anywhere in the codebase (confirmed by grep) — and severity itself is computed once, at ingestion, by FIVE independent duplicate `getSeverityFromScore` implementations with hardcoded 9.0/7.0/4.0 cutoffs (src/renderer/lib/cvss/parser.ts:267-273, src/renderer/lib/api/nvd.ts:52, osv.ts:102, providers/nvdProvider.ts:61, providers/osvProvider.ts:51), then stored as `.severity` on the Vulnerability object and read as a plain field by dozens of downstream badges/filters/dashboards.
- **Fix:** This PRD item bundles one small real fix with one item that is a genuinely large feature if taken literally — split them explicitly (Rule 1, push back rather than force a fake minimal implementation): (A) 'Toggle CVSS breakdown display' — small, real fix: in VulnerabilityDetailModal.tsx, read `const settings = useSettings()` (existing hook, already used by App.tsx) and initialize `const [cvssExpanded, setCvssExpanded] = React.useState(settings.showCvssBreakdown)` instead of the hardcoded `false` (line 25); add a toggle for `showCvssBreakdown` in a new small settings section (see below) that calls `updateSettings({ showCvssBreakdown })`. The user's in-modal expand/collapse (`setCvssExpanded`) still works per-session; the setting only controls the default. (B) 'Select CVSS version (3.0, 3.1)' — the app has no dual-version data model (Vulnerability carries exactly one `cvssVector`, whose version prefix is fixed by whatever NVD published — src/renderer/lib/cvss/parser.ts:26-27); making this a real per-record choice would require a schema/ingestion change (storing a second vector) which is out of proportion to a Low/Nice-to-Have bullet. Smallest credible slice: add the UI control (persists `settings.cvssVersion`) and, in CvssVectorString/VulnerabilityDetailModal, if `breakdown.version !== settings.cvssVersion`, render a small informational note ('This CVE only provides a CVSS {breakdown.version} score') — genuine, testable, non-speculative; do NOT attempt score recalculation between versions. (C) 'Configure severity thresholds' — flagging plainly per the task's guidance: a global, app-wide configurable severity taxonomy is a LARGE feature (it would require either re-deriving `.severity` live everywhere it's read today, or a bulk reclassification pass over every stored Vulnerability whenever the threshold changes — both are invasive, cross-cutting, and need a product decision on semantics that hasn't been made). Recommended MVP instead: add `severityThresholds: { critical: number; high: number; medium: number; low: number }` to AppSettings (defaults matching the current 9.0/7.0/4.0/0.1 spec cutoffs so existing behavior is unchanged by default) + zod schema in importExport.ts (matching the cvssVersion/showCvssBreakdown pattern), add a numeric-input UI, and thread it through as an optional parameter to ONLY `parser.ts`'s `getSeverityFromScore`/`parseCvssVector` (used solely by the CVSS breakdown widgets in VulnerabilityDetailModal), defaulting to the current hardcoded values when absent — i.e. the setting affects the CVSS-detail-modal's own severity label/color, not the stored `.severity` field or any list/dashboard/filter. Document this scope limit directly in the UI copy ('Applies to the CVSS detail view only') so it isn't mistaken for a global override. (D) New settings/CvssSection.tsx (self-contained, same pattern as AppearanceSection.tsx) hosting all three controls; register it in SettingsNav.tsx SECTIONS and mount it in Settings.tsx.
- **Tests:** TDD order, failing-first: (1) src/renderer/lib/cvss/parser.test.ts (exists — strengthen it) — add cases: `getSeverityFromScore(8.5, { critical: 9.0, high: 7.0, medium: 4.0, low: 0.1 })` returns 'high' (default cutoffs, matches today); `getSeverityFromScore(8.5, { critical: 8.0, high: 7.0, medium: 4.0, low: 0.1 })` returns 'critical' (custom cutoff changes the bucket — the WHY: proves the parameter is actually consulted, not just accepted and ignored); `parseCvssVector` with a custom threshold produces a `breakdown.severity` reflecting it. (2) src/renderer/components/VulnerabilityDetailModal.test.tsx (check if it exists; if not, this is the right place to add it) — render with `settings.showCvssBreakdown: true` (mocked useSettings) and assert the breakdown section starts expanded without any click (fails today since it's hardcoded useState(false)); render with a vulnerability whose `cvssBreakdown.version` is '3.0' and `settings.cvssVersion: '3.1'`, assert the mismatch note is shown; assert it is NOT shown when they match. (3) new src/renderer/pages/settings/CvssSection.test.tsx — clicking each control calls `updateSettings` with the right partial payload (cvssVersion / showCvssBreakdown / severityThresholds), matching the AppearanceSection.test.tsx-equivalent pattern used for theme/fontSize in Settings.test.tsx. (4) src/renderer/lib/settings/importExport.test.ts (exists — strengthen) — round-trip export/import of a profile carrying non-default `severityThresholds` and assert the zod schema rejects an out-of-order threshold set (e.g. high > critical) if such a guard is added, or explicitly document that ordering is NOT validated if it isn't (avoid an assertion that can't fail).
- **Risk:** Medium — item (C) touches a widely-duplicated severity computation; scoping it strictly to the CVSS-detail-modal call site (not the 5 ingestion-time getSeverityFromScore copies, not the stored .severity field) is essential to avoid an inconsistent UI where the same CVE shows different severities in different screens. If a future ticket wants a true global override, that is a separate, larger (L/XL) initiative requiring a product decision on whether reclassification is retroactive — call this out explicitly rather than silently expanding scope.

---

## FR-01

### FR-01.1 — Project Creation: enforce unique name, stamp updatedAt on edit, prove 1,000-project capacity

- **Priority:** High · **Report:** SHALLOW · **Now:** PARTIALLY_FIXED · **Effort:** M · **Product code:** yes
- **Since report:** Since the 2026-07-25 report, two of the five sub-bullets were fixed: (1) CreateProjectDialog.tsx:33 now generates ids via the real ulid() (src/renderer/lib/audit/ulid.ts), not `project-${Date.now()}-${Math.random()}`; CreateProjectDialog.test.tsx:198-215 now asserts isValidUlid(createdProject.id) — the 'ULID-based' bullet is DONE. (2) server/routes/projects.test.ts (new, 111 lines) now gives full supertest coverage of POST/GET/DELETE /api/projects including id-sanitization/path-traversal — this closes the 'server round trip has NO test coverage at all' complaint that was dragging down the 'persist across restarts' bullet. Still open and reproduced live at HEAD: no unique-name check anywhere (CreateProjectDialog.tsx, useStore.ts, server/routes/projects.ts all still have zero 'unique'/'duplicate' hits); updateProject() still never stamps updatedAt itself and the edit-save path still omits it (confirmed by reading ProjectDetail.tsx:344-347 and EditProjectDialog.tsx:23, and by the existing ProjectDetail.test.tsx:2120-2123 assertion which expects an exact `{name, description}` payload with no updatedAt); and 'Support minimum 1000 concurrent projects' still has zero test evidence anywhere in the repo (fresh grep, no hits).
- **Root cause:** (1) Unique name: src/renderer/components/CreateProjectDialog.tsx:17-29 `handleSubmit` validates only `!name.trim()` and length<3; it never reads the store's `projects` list nor compares against existing names — grep across CreateProjectDialog.tsx, useStore.ts and server/routes/projects.ts for 'unique'/'duplicate' returns zero hits. (2) updatedAt not stamped on edit: src/renderer/pages/ProjectDetail.tsx:344-347 (`EditProjectDialog`'s `onSave` callback) calls `updateProject(project.id, updates)` where `updates` is exactly `{ name, description }` returned by src/renderer/pages/project-detail/EditProjectDialog.tsx:20-24; `updateProject` in src/renderer/store/useStore.ts:233-239 does a bare `{ ...p, ...updates }` merge and never auto-stamps `updatedAt`. Every other `updateProject(...)` call site (SbomUploadDialog.tsx:261-265, BinarySbomDialog.tsx:148-152, ContainerScanDialog.tsx:205-207, ProjectDetail.tsx:182-186 handleRemoveSbom, useProjectScan.ts:133-136) explicitly passes `updatedAt: new Date()` — the edit-dialog path is the one caller that breaks this established per-caller convention, so editing a project's name/description silently fails to update its 'last update' timestamp and won't reorder it in any updatedAt-sorted view (Dashboard.tsx:169 sorts projects by `updatedAt` desc). (3) 1000-project capacity: no cap exists in addProject/updateProject/deleteProject (plain array spread/map/filter, useStore.ts:227-270), and the localStorage `partialize` (useStore.ts:434-444) already strips `vulnerabilities`/`components`/`dependencyGraph` per project, keeping footprint small — the SHALLOW grade here is a pure test-evidence gap, not a known code defect.
- **Fix:** 1) In src/renderer/components/CreateProjectDialog.tsx: add `const projects = useStore((s) => s.projects)`; in `handleSubmit`, after the existing length check and before the `// Create project` block, add: trim the input, check `projects.some((p) => p.name.trim().toLowerCase() === trimmedName.toLowerCase())`, and if true `setError('A project with this name already exists')` and `return`. Client-side only, matching the existing all-client-side validation pattern in this component — do not add a server-side uniqueness check (projects aren't keyed by name server-side, and this is the sole creation entry point). 2) In src/renderer/pages/ProjectDetail.tsx, change the `EditProjectDialog`'s `onSave` handler from `updateProject(project.id, updates)` to `updateProject(project.id, { ...updates, updatedAt: new Date() })` — mirrors the exact pattern already used at ProjectDetail.tsx:186, SbomUploadDialog.tsx:265, etc. Do NOT centralize the stamp inside useStore.ts's `updateProject` reducer itself: other callers (Dashboard.tsx:106, useProjectScan.ts:193) intentionally omit `updatedAt` for vuln-refresh-only updates (they set `lastVulnDataRefresh` instead), and centralizing would incorrectly bump `updatedAt` on every background vuln refresh too. 3) No product code change for the 1000-project bullet; close the test-evidence gap with the new perf/scale test below.
- **Tests:** TDD, failing-first: (a) src/renderer/components/CreateProjectDialog.test.tsx — extend the existing `mockStore` object to include a `projects: Project[]` field (default `[]`) exposed through the same `selector(mockStore)` mock pattern already used for `addProject`; add 'should show an error and not create a project when an existing project already has that name' (seed `mockStore.projects` with a project named 'Test Project', submit 'Test Project', assert the new error text renders and `mockAddProject` is NOT called) and 'should treat names as duplicate case-insensitively and with surrounding whitespace' (seed 'Test Project', submit ' test project ', same assertions) — encodes the WHY: prevents two indistinguishable projects from coexisting in Dashboard/search/export, which is the literal PRD bullet. (b) src/renderer/pages/ProjectDetail.test.tsx — strengthen the 3 existing exact-match `toHaveBeenCalledWith('test-project-id', {...})` assertions in 'Edit Project Dialog' > 'should save project changes' (~line 2120) and 'Edit Project Edge Cases' > 'should trim whitespace from name on save' (~2642) and 'should save with undefined description...' (~2658) to also require `updatedAt: expect.any(Date)` in the expected object — these fail today (updatedAt key absent) and pass once the fix lands. Add one new test asserting the timestamp is genuinely fresh, not just present: capture `Date.now()` immediately before clicking 'Save Changes', then assert `mockUpdateProject.mock.calls[0][1].updatedAt.getTime() >= capturedNow` — this is the stronger 'WHY' check (a regression that reuses the old fixture's `updatedAt` unchanged would pass `expect.any(Date)` but fail this). (c) New file tests/perf/projects.perf.test.ts (matches the existing tests/perf/\*.perf.test.ts convention from the NFR-01 suite) — 'FR-01.1 / NFR-02.1 — 1,000 concurrent projects': call `useStore.getState().resetStore()`, `addProject` 1000 synthetic projects, assert `projects.length === 1000`; `updateProject` on one project from the middle and one at the end and assert the rename lands (proves no silent truncation/cap at scale); `deleteProject` one and assert length drops to 999; include a soft wall-clock assertion (e.g. total elapsed < 1s) as a canary against an accidental O(n^2) regression. Comment that this file is shared evidence for NFR-02.1 (Scalability — 1,000+ projects) so whoever picks up that separate report item should reference it rather than duplicating the test.
- **Risk:** Unique-name check is client-only; a second browser tab, or any future non-UI project-creation path (API/CLI), could still create a duplicate name — acceptable today given NFR-02.4's single-user-desktop framing, but flag if multi-user/API creation is added later. The updatedAt fix deliberately touches only the one caller missing it, per the codebase's existing per-caller-stamps-it convention (Rule 11) — do not refactor the other 5 call sites. The 1000-project item overlaps NFR-02.1 in the same validation report (separate reqId, same underlying claim); coordinate so it isn't implemented twice in the same remediation pass.

### FR-01.2 — Project Editing and Deletion: wire deleteProjectFromServer so deletion actually cascades to server-persisted scan data

- **Priority:** Medium · **Report:** SHALLOW · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** yes
- **Since report:** Since the report, the 'no test submits the form and asserts updateProject is called with the edited values' shallow-coverage complaint for the edit bullet is FIXED: ProjectDetail.test.tsx now has a real 'should save project changes' test (~line 2108) plus an 'Edit Project Edge Cases' block (~2605-2663) that fills the form, clicks Save, and asserts `updateProject` is called with the edited `{name, description}` — this is genuine, not wiring-only, coverage. The headline finding is completely unchanged: `deleteProjectFromServer` (src/renderer/lib/api/projectPersistence.ts:26-28) still has zero callers anywhere in the codebase (fresh grep confirms only its own definition matches) — `deleteProject` in useStore.ts never calls it. server/routes/projects.test.ts is new and proves the DELETE endpoint itself works correctly (including idempotent no-op on a missing file) — the server side is not the problem; the client simply never calls it.
- **Root cause:** src/renderer/store/useStore.ts:261-270 — `deleteProject(id)` removes the project from the in-memory/localStorage-persisted array (`set(...)`) and calls `logProjectDelete(projectToDelete)`, but never calls `deleteProjectFromServer(id)` (fully implemented at src/renderer/lib/api/projectPersistence.ts:26-28, wrapping `apiDelete('/projects/' + id)`). The target route, `DELETE /api/projects/:projectId` (server/routes/projects.ts:77-92), is real and correct, and is now covered by server/routes/projects.test.ts:96-110 — but with no caller, the JSON file at `DATA_DIR/projects/<id>.json` is never removed. That file is written by `saveProjectToServer`, which `updateProject` calls whenever `updates.vulnerabilities || updates.components || updates.allowedLicenses` is set (useStore.ts:244-259) — i.e. exactly for any project that has ever had an SBOM imported or been scanned. So the PRD's 'deletion must cascade to associated scan results and vulnerabilities' bullet fails specifically for the projects that matter most, leaving orphaned vulnerability/component payloads on disk after a user 'deletes' the project in the UI. Both the single-delete path (Dashboard.tsx:181, ProjectDetail's delete-confirm flow) and the bulk-delete loop (Dashboard.tsx:211) funnel through this same `deleteProject` action, so one fix covers both.
- **Fix:** In src/renderer/store/useStore.ts: add `deleteProjectFromServer` to the existing import from `@/lib/api/projectPersistence` (currently `import { saveProjectToServer, loadProjectFromServer } from '@/lib/api/projectPersistence'` at line 17). Inside `deleteProject`, after the `set(...)` call, add `deleteProjectFromServer(id).catch((err) => { console.error('[Store] Failed to delete project from server:', err) })` — fire-and-forget, matching the exact error-handling convention `updateProject` already uses for its `saveProjectToServer` call (useStore.ts:256-258). Call it unconditionally (not gated on `projectToDelete` being found locally), since the server route is already idempotent for a missing file (server/routes/projects.test.ts:105-109 proves DELETE on a nonexistent project still returns 200). No server-side change needed.
- **Tests:** TDD, failing-first: (a) src/renderer/store/useStore.test.ts — add `vi.mock('@/lib/api/projectPersistence', () => ({ saveProjectToServer: vi.fn().mockResolvedValue(undefined), loadProjectFromServer: vi.fn().mockResolvedValue(null), deleteProjectFromServer: vi.fn().mockResolvedValue(undefined) }))` near the top alongside the existing `@/lib/refresh`/`@/lib/settings` mocks (today the module is unmocked and its real network call silently rejects via the store's own `.catch`, so a mock is required to assert call args) and import the mocked `deleteProjectFromServer`. In the `projects` describe block, add: 'should delete the server-persisted copy when a project is deleted, so orphaned scan data does not survive deletion (FR-01.2)' — `addProject(mockProject)`, then `deleteProject(mockProject.id)`, then `expect(deleteProjectFromServer).toHaveBeenCalledWith(mockProject.id)`. This fails today (never called) and passes once the fix lands. (b) No change needed in Dashboard.test.tsx's 'Bulk Delete' block — it mocks `deleteProject` itself at the store-selector level (confirmed: Dashboard.test.tsx:987-1042 uses `const mockDelete = vi.fn(); ... deleteProject: mockDelete`), so it never exercises the real reducer; the one useStore.test.ts addition is sufficient because both single- and bulk-delete route through the same store action. (c) Optional stretch, not required to close this item: extend e2e/workflows/project-management.spec.ts's 'should delete the project after confirmation' test (line 229) to first trigger an SBOM import (so `saveProjectToServer` actually runs and a `DATA_DIR/projects/<id>.json` exists), then after deleting via the UI, call `page.request.get('/api/projects/' + id)` and assert `body.data === null` — this is the only test that would additionally catch a simultaneous regression in the server route itself, but the Vitest test above already isolates and proves the specific client-wiring defect this task fixes.
- **Risk:** Fire-and-forget with only console.error on failure means a transient server/network failure during delete leaves an orphaned file with no user-visible retry or surfaced error — this exactly matches the existing pattern in `updateProject`'s `saveProjectToServer` call (Rule 11: match existing convention), so it's consistent rather than novel risk, but worth flagging if compliance/audit requirements tighten later (e.g. would want a retry queue or a toast on failure).

---

## FR-05

### FR-05.1 — Health score trend default falsely reports "stable" instead of "unknown" before real history exists

- **Priority:** High · **Report:** SHALLOW · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** yes
- **Since report:** The report's core concern (trend computed by lib/health/trends.ts but never wired to real data) was largely fixed by commit 12b72e8 (2026-07-27, after the 2026-07-25 report): it added src/renderer/lib/health/healthHistory.ts (per-project daily score history in localStorage) and wired HealthTab.tsx to derive a genuine history-based trend badge, with 12 new passing tests. Re-validating at HEAD (ran the actual functions) found one residual defect the new wiring did not fix: calculateProjectHealth's trend-aggregation default. Status downgraded from an implicit SHALLOW to PARTIALLY_FIXED, not closed.
- **Root cause:** src/renderer/lib/health/healthScore.ts:239-248 (calculateProjectHealth): `let trend: ProjectHealthSummary['trend'] = 'stable'` is the default whenever improvingCount and degradingCount don't dominate. calculateComponentHealth (healthScore.ts:9-32) never sets ComponentHealth.previousScore (grep confirms no production code ever writes it), so trends.ts's calculateTrend(score, undefined) always returns 'unknown' for every component in production -- improvingCount and degradingCount are therefore always 0, and the aggregate silently defaults to 'stable' instead of 'unknown'. Verified by direct execution: calculateProjectHealth([calculateComponentHealth(component, [])]).trend === 'stable'. HealthTab.tsx:53-55 (`historicalTrend === 'unknown' ? projectHealth : {...}`) only overrides this with the real history-derived trend once historicalTrend !== 'unknown' (>=2 persisted daily snapshots); on a project's first view (history.length < 2, e.g. the offline e2e fixture, or any freshly onboarded project) the buggy 'stable' default passes straight through to HealthDashboard.tsx, which then renders a 'Stable' badge (lines 71-76) and suppresses the 'No Historical Data Available' banner (line 45, gated on trend === 'unknown') -- users see a fabricated trend with zero historical basis.
- **Fix:** In src/renderer/lib/health/healthScore.ts, calculateProjectHealth (~lines 239-248): add `const hasKnownTrend = componentHealths.some((h) => h.trend !== 'unknown')` and change the initial default from `let trend: ProjectHealthSummary['trend'] = 'stable'` to `let trend: ProjectHealthSummary['trend'] = hasKnownTrend ? 'stable' : 'unknown'`. Leave the existing `if (improvingCount > degradingCount * 1.5) ... else if (degradingCount > improvingCount * 1.5) ...` override branches untouched. No other file needs to change -- HealthTab.tsx's existing override logic already does the right thing once projectHealth.trend correctly reports 'unknown'.
- **Tests:** TDD order: 1) src/renderer/lib/health/healthScore.test.ts, describe('calculateProjectHealth') -- add a failing-first case 'should report trend as unknown (not stable) when every component trend is unknown', building 2 ComponentHealth objects with trend: 'unknown' and asserting result.trend === 'unknown'; must not touch the existing improving/degrading/balanced-stable tests (lines ~502-633, which use real known trend values) -- they must keep passing unchanged, proving the fix is scoped to the true no-data branch. 2) src/renderer/pages/project-detail/HealthTab.test.tsx -- add a case using a project with >=1 real component (the existing makeProject() uses components: [], which sidesteps this bug via calculateProjectHealth's separate empty-array early return) and no seeded localStorage; assert capturedTrend.value === 'unknown', not 'stable'. 3) e2e/features/health-dashboard.spec.ts -- add 'shows "No Historical Data Available" (not a false Stable badge) on first view', using the existing openHealthDashboardForImportedProject helper (resetAppState clears localStorage, guaranteeing Day-1 conditions, fully offline); assert the banner text is visible and no 'Stable' text renders inside the Average Health Score card.
- **Risk:** Low -- a narrow, backward-compatible change to one aggregation default. The only observable behavior change is that 'Stable' no longer appears before real history exists (replaced by the already-built 'No Historical Data Available' banner), which is the correct FR-05.1 behavior. No type/API changes.

### FR-05.2 — Health category boundaries -- already corrected to PRD's 90/75/60/40

- **Priority:** High · **Report:** GAP · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** FIXED_SINCE_REPORT. Commit 708f268 ('fix(health): align category thresholds with FR-05.2 ranges', 2026-07-27, two days after the report) corrected getHealthCategory's boundaries from 90/75/50/25 to the PRD's 90/75/60/40 and updated the previously-wrong pinned test assertions in the same commit. Re-verified by reading src/renderer/lib/health/healthScore.ts:198-204 and healthScore.test.ts at HEAD, and by running `npx vitest run src/renderer/lib/health` (all 171 tests green).
- **Root cause:** (Historical, now fixed) getHealthCategory used >=50 for 'fair' and >=25 for 'poor'; PRD FR-05.2 requires >=60 and >=40 respectively. At HEAD, healthScore.ts:198-204 reads `if (score>=90) excellent; >=75 good; >=60 fair; >=40 poor; else critical` -- exact match to PRD. The report's specific complaint ('tests assert the deviating boundaries, does not validate against PRD ranges') no longer applies: healthScore.test.ts:55,93,140,332-336 now assert against the correct 60/40 edges (e.g. score 58 -> 'poor' per the 40-59 range; score 60 -> 'fair').
- **Fix:** No production change needed for this reqId -- already correct at HEAD. One adjacent, still-open inconsistency was found during re-validation: src/renderer/components/executive/widgets/ProjectHealthComparison.tsx has its own local, duplicate getHealthColor(score) with DIFFERENT (still wrong) boundaries (90/70/50/30). This does not regress FR-05.2's own canonical function, so it is filed and fixed under the FR-05.3 task (comparison-view bullet) instead of here, to avoid a duplicate work item -- see FR-05.3's fix for the concrete change (export getHealthCategory, reuse it + getHealthChartColor in that widget).
- **Tests:** No new test required for FR-05.2 itself -- healthScore.test.ts already exercises the exact PRD boundaries (60 -> fair, 58 -> poor) through calculateComponentHealth, the project's established convention for testing this private function only via its public callers. A stricter (optional, not required) follow-up would add exact-edge cases at 90/89, 75/74, and 39/40, but the existing boundary-adjacent assertions already fail if the thresholds regress, satisfying the 'tests encode the WHY' bar.
- **Risk:** None -- already fixed and independently re-verified; no code change proposed here.

### FR-05.3 — Trend line now shipped; project-comparison view still untested and uses a different, PRD-noncompliant color mapping

- **Priority:** High · **Report:** SHALLOW · **Now:** PARTIALLY_FIXED · **Effort:** M · **Product code:** yes
- **Since report:** PARTIALLY_FIXED since the report. The 'trend line for score changes over time' bullet (previously a confirmed GAP -- no LineChart/history existed anywhere) was fixed by commit 12b72e8 (2026-07-27): added src/renderer/components/HealthTrendChart.tsx (aria-labelled recharts LineChart) + src/renderer/lib/health/healthHistory.ts (localStorage daily score history), wired into HealthTab.tsx, with a full passing test suite (healthHistory.test.ts, HealthTrendChart.test.tsx, HealthTab.test.tsx) -- re-verified green at HEAD. The 'comparison view for multiple projects' bullet is unchanged since before the report (ProjectHealthComparison.tsx traces back to acd0518/449eb25) and remains open; re-validation upgraded it from the report's 'unverified coloring logic' to a confirmed color/threshold bug (see rootCause).
- **Root cause:** Two FR-05.3 bullets were open at report time. (a) trend line: fixed, see staleNote. (b) comparison view: src/renderer/components/executive/widgets/ProjectHealthComparison.tsx:24-30 defines its own local getHealthColor(score) with boundaries 90/70/50/30 -- both a duplicate of, and inconsistent with, the canonical FR-05.2 category function (now correctly 90/75/60/40, exposed via getHealthChartColor(category) in src/renderer/lib/health/healthScore.ts:280-293). Concretely: a project scoring 70 is colored 'light green' by the widget's local >=70 threshold, even though FR-05.2 classifies 70 as 'fair' (60-74, yellow under getHealthChartColor); a project scoring 35 is colored 'orange' by the local >=30 threshold, even though FR-05.2 classifies 35 as 'critical' (<40, red). No test exercises this at all: confirmed no ProjectHealthComparison.test.tsx exists, and src/renderer/components/executive/ExecutiveDashboard.test.tsx:70-74 mocks the whole widget out, so nothing pins either the old or the correct colors.
- **Fix:** 1) src/renderer/lib/health/healthScore.ts: export the existing private getHealthCategory(score) (line 198) so score->category mapping has one canonical implementation. 2) src/renderer/lib/health/index.ts: add getHealthCategory to the health-score re-export list (line 8). 3) src/renderer/components/executive/widgets/ProjectHealthComparison.tsx: `import { getHealthCategory, getHealthChartColor } from '@/lib/health'`; delete the local getHealthColor(score) function (lines 24-30); change the Cell fill from `fill={getHealthColor(entry.healthScore)}` to `fill={getHealthChartColor(getHealthCategory(entry.healthScore))}`. This is the minimum change: it removes only the duplicate/wrong thresholds, leaving the widget's data shaping (top-8 slice, name truncation, tooltip, aria-label) untouched. Explicitly OUT OF SCOPE (flagged, not fixed): the `healthScore` values being colored come from a second, independent ad hoc formula in src/renderer/lib/analytics/metricsCalculator.ts:377-396 (`calculateProjectHealthScore`: vulnRatio*10 + criticalCount*5 + highCount\*2 - patchRatio bonus) that does not implement FR-05.1's specified 40/20/20/20 weights, and is shared by other FR-06 Executive Dashboard widgets (RiskGauge, aggregate metrics). Reconciling the two health-score algorithms is a much larger, cross-FR (FR-05 <-> FR-06) change and should be tracked as its own follow-up, not folded into this surgical color-boundary fix.
- **Tests:** TDD order, new file src/renderer/components/executive/widgets/ProjectHealthComparison.test.tsx (none exists today): 1) Write the file first using the recharts-mocking convention already established in HealthDistributionChart.test.tsx (mock BarChart/Bar/XAxis/YAxis/Tooltip/ResponsiveContainer as pass-through divs; Cell as `<div data-testid="cell" data-fill={fill} />`), but do NOT mock '@/lib/health' -- use the real getHealthChartColor/getHealthCategory so the test enforces actual FR-05.2 conformance, not an assumption. 2) Case 'colors bars by the FR-05.2 health category, not the widget's own ad hoc thresholds': projectMetrics with scores [95, 70, 35]; assert cell 2's fill equals getHealthChartColor('fair') and cell 3's equals getHealthChartColor('critical'). These two scores are specifically chosen because they diverge from the old local thresholds (70 was miscolored light-green; 35 was miscolored orange) -- the test fails against current ProjectHealthComparison.tsx and only passes after the fix, proving it encodes the real defect. 3) Case 'renders at most the top 8 projects': pass 10 projectMetrics; assert `screen.getAllByTestId('cell')` has length 8, guarding the 'Top {n} projects' UI contract. 4) Case 'renders the empty state when there is no project data': projectMetrics=[]; assert 'No project data available' is shown and no chart/cell renders. 5) src/renderer/components/executive/ExecutiveDashboard.test.tsx should need no change (it intentionally tests wiring via a shallow mock, not the widget's internals) -- run it after the fix to confirm.
- **Risk:** Low-medium -- visibly changes the comparison chart's bar colors (e.g. a 70-scoring project moves from light-green to yellow) to match the rest of the Health Dashboard; no existing test pins the old colors, and the change is confined to one presentational widget. The flagged metricsCalculator health-score-formula divergence is intentionally left untouched to avoid destabilizing FR-06 Executive Dashboard widgets that share it.

---

## FR-08

### FR-08.1 — Global Search — advanced AND/OR/NOT syntax + save search queries

- **Priority:** High · **Report:** GAP · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** Report (2026-07-25) is stale for this item. Since then: commit 7a88ff2 'feat(search): boolean AND/OR/NOT + quoted phrases in global search (FR-08.1)' added parseSearchQuery/matchesParsedQuery to src/renderer/lib/search/searchIndex.ts (OR-of-AND groups, NOT negation, quoted literal phrases, only-uppercase operators) and rewired searchIndex() to use them while preserving relevance/exact-match boosts. Commit 8c2233d 'feat(search): save & reload global search queries (FR-08.1)' added src/renderer/lib/search/savedSearches.ts + src/renderer/components/SavedSearches.tsx and wired both into src/renderer/pages/Search.tsx (save current query, reload from chip, delete). Commit 462b61d then closed 3 review-diff findings (loose save-gate, phrase-spanning-fields false positive, missing integration tests). Re-verified at HEAD by reading all four files plus a targeted `npx vitest run` of searchIndex.test.ts, SavedSearches.test.tsx, savedSearches.test.ts (50/50 passing) — tests are genuine (e.g. 'OR widens the match to either term', 'NOT excludes results containing the negated term', 'save -> chip -> reload round-trip'), not tautological. Residual, OUT OF SCOPE: the separate NVD/CVE-ID/CPE search mode (server/routes/database.ts:164-167) still literal-wraps its FTS5 query and does not support AND/OR/NOT — this was cited by the report only as supporting evidence for the (now-fixed) missing-boolean-syntax finding, not as its own PRD bullet, and CVE-ID/CPE lookups are single-token by nature, so this is not treated as a remaining gap against FR-08.1's acceptance bullets.
- **Root cause:** N/A — already fixed. (Original root cause, now resolved: server/routes/database.ts:164-167 always literal-phrase-wrapped queries and src/renderer/lib/search/searchIndex.ts used plain .includes() substring matching with no boolean parsing; savedSearch/searchHistory had zero implementation anywhere.)
- **Fix:** No further production-code change required for the PRD bullets. If desired as a future (non-blocking) enhancement, the NVD-search boolean-syntax residual could be closed by translating parseSearchQuery()'s output into a parameterized FTS5 MATCH expression (escaping special chars per term) inside server/routes/database.ts's 'text' search branch — but this is a distinct, higher-risk change (FTS5 syntax + injection-safety) and is NOT required to satisfy the FR-08.1 acceptance bullets as written.
- **Tests:** No new tests required — already covered and re-verified: src/renderer/lib/search/searchIndex.test.ts describe('boolean search syntax (FR-08.1)') (AND default, explicit AND, OR, NOT, case-sensitivity of operators, quoted phrase, phrase-can't-span-fields, parseSearchQuery/matchesParsedQuery unit cases); src/renderer/lib/search/savedSearches.test.ts (load/save/delete, corrupt-data guard, overwrite-by-name, throws on empty name/query); src/renderer/components/SavedSearches.test.tsx; src/renderer/pages/Search.test.tsx describe('Saved searches (FR-08.1)') (save->chip->reload round trip, delete, hidden in NVD mode). Ran `npx vitest run` on all four files at HEAD: 50/50 passing.
- **Risk:** None — already shipped and tested; verification-only effort.

### FR-08.2 — Component Filtering — add patch-availability filter and wire save/load/delete filter presets into ComponentsTab

- **Priority:** High · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Confirmed unchanged at HEAD. Re-checked by reading the full current src/renderer/pages/project-detail/ComponentsTab.tsx (337 lines) and grepping for 'patch' and 'FilterPresets' — zero hits in either case. Unlike FR-08.1/FR-08.3, no commit in the log touches ComponentsTab.tsx for this requirement.
- **Root cause:** src/renderer/pages/project-detail/ComponentsTab.tsx's filter predicate (lines 129-158) checks type/vuln-presence/license/coverage but never checks Component.patchInfo — there is no patch-availability state, select, or predicate term at all (grep for 'patch' in the file: no hits). Separately, the file never imports `@/components/FilterPresets` (grep: no hits) and has no getCurrentFilters/handleSavePreset/handleLoadPreset/handleDeletePreset/localStorage-persisted preset state — the pattern that already exists in src/renderer/pages/project-detail/VulnerabilitiesTab.tsx (lines 34-41 state init, 48-54 persist effect, 122-195 save/load/delete + getCurrentFilters, 218-224 <FilterPresets> render) was never applied to the Components tab, so component filter combinations (type+license+vuln-presence+patch) cannot be saved at all. The data needed for the missing filter already exists and is proven-real elsewhere in the codebase: Component.patchInfo?.hasFixAvailable (src/shared/types.ts:142,158-163) is already consumed by src/renderer/lib/export/csv.ts:187, pdf.ts:236/269/468, json.ts:175, and src/renderer/lib/health/healthScore.ts:109 — it just isn't wired into this tab's filter UI.
- **Fix:** 1) src/renderer/pages/project-detail/helpers.ts: add `export function hasComponentPatchAvailable(component: Component): boolean { return component.patchInfo?.hasFixAvailable === true }` next to the existing `hasAvailablePatch` (Vulnerability-side) helper, same file/style; add `Component` to the `import type { Project, Vulnerability } from '@@/types'` line (it isn't currently imported there). 2) src/shared/types.ts: extend `FilterPreset['filters']` (currently lines 613-624) with two new optional fields — `license?: string[]` and `hasVulnerabilities?: boolean` — reusing the already-present-but-unused `componentType?: Component['type'][]` field and the existing `hasPatch?: boolean` field (already used by VulnerabilitiesTab for the Vulnerability-side patch filter; safe to reuse here because presets are namespaced per-tab by localStorage key, not by shape). 3) src/renderer/pages/project-detail/ComponentsTab.tsx: import `FilterPresets` from `@/components/FilterPresets` and `FilterPreset` + `hasComponentPatchAvailable` (from `./helpers`); add `componentPatchFilter: 'all' | 'available' | 'unavailable'` state; add `filterPresets` state seeded from `localStorage.getItem(\`component-filter-presets-${project.id}\`)` (own namespace, distinct from VulnerabilitiesTab's `vuln-filter-presets-${projectId}`key) plus a`useEffect`that persists it on change — mirror VulnerabilitiesTab.tsx lines 34-54 exactly; add`getCurrentFilters()`/`handleSavePreset`/`handleLoadPreset`/`handleDeletePreset`mirroring VulnerabilitiesTab.tsx lines 122-195, mapping componentTypeFilter<->filters.componentType (single-element array, same 'all'-vs-array convention as severity), componentLicenseFilter<->filters.license, componentVulnFilter<->filters.hasVulnerabilities (true='vulnerable', false='safe', absent='all'), componentPatchFilter<->filters.hasPatch; add a`<select aria-label="Filter by patch availability">`(options All/Has Patch/No Patch) next to the existing filter selects (inside the`project.components.length > 0 &&`block, lines 36-105) and render`<FilterPresets presets={filterPresets} currentFilters={getCurrentFilters()} onSavePreset={handleSavePreset} onLoadPreset={handleLoadPreset} onDeletePreset={handleDeletePreset} />`alongside them; add`matchesPatch = componentPatchFilter === 'all' || (componentPatchFilter === 'available' && hasComponentPatchAvailable(component)) || (componentPatchFilter === 'unavailable' && !hasComponentPatchAvailable(component))`to the filter predicate (line ~157) and AND it into the final`return`; add `setComponentPatchFilter('all')` to the existing 'Clear filters' button handler (lines ~180-189) since it now controls an additional filter dimension.
- **Tests:** TDD, new file src/renderer/pages/project-detail/ComponentsTab.test.tsx (mirror the just-added VulnerabilitiesTab.test.tsx pattern: real render, no FilterPresets/CvssRangeSlider/MultiSelectFilter mocking, so the actual save/load dialog and selects are exercised). WHY: a preset that silently drops the patch dimension, or an inverted patch predicate, would hide/show the wrong components with no red test. (1) Write failing first: 'patch-availability filter narrows to components with vs without a fix available' — two components, one with `patchInfo: { hasFixAvailable: true, fixedVersions: [], vulnerableVersions: [] }`, one with no patchInfo; select 'Has Patch' -> assert only the patched component's name renders and the other doesn't; select 'No Patch' -> assert the inverse. (2) 'saving a filter preset persists type+license+patch and reloading re-applies exactly those' — set type filter to 'library' and patch filter to 'available', open the Presets save dialog (real FilterPresets UI), type a name, submit; assert `localStorage.getItem('component-filter-presets-<project.id>')` now contains `filters.componentType=['library']` and `filters.hasPatch=true`; unmount/remount (simulating reload) with that localStorage populated, click Load, assert the rendered list is narrowed to exactly the library+patched component. (3) 'Clear filters resets patch filter back to All' — set patch filter to 'available', click Clear filters, assert the select's value is back to 'all' and both components reappear. Also add unit tests for the new helper in src/renderer/pages/project-detail/helpers.test.tsx, mirroring the existing `hasAvailablePatch` describe block: `hasComponentPatchAvailable` returns true only when `patchInfo.hasFixAvailable === true`; returns false when patchInfo is undefined; returns false when `hasFixAvailable === false`.
- **Risk:** Low-medium. FilterPreset is a shared type consumed by both tabs — mitigate collision by giving ComponentsTab its own localStorage key (`component-filter-presets-*`) so existing vuln presets are untouched, and by only adding optional fields (no breaking change to VulnerabilitiesTab's existing filters shape). ProjectDetail.test.tsx globally stub-mocks `@/components/FilterPresets` (lines 258-296) and only ever renders one tab's content at a time (src/renderer/pages/ProjectDetail.tsx:326-328 conditionally mounts exactly one of ComponentsTab/VulnerabilitiesTab), so existing TC-CM-00x tests and the 'Filter Presets CRUD' block are unaffected. Must confirm the new `<select>`/`<FilterPresets>` are only rendered inside the existing `project.components.length > 0` guard so the empty-state assertions (TC-CM-007) are unaffected.

### FR-08.3 — Vulnerability Filtering — close residual shallow-test coverage for CVSS-range/source and the mislabeled reference-tag preset test

- **Priority:** High · **Report:** SHALLOW · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** no
- **Since report:** Substantially improved since the report. Commits 2f4eb12 'feat(vulns): add patch-availability and exploit-status filters' and 040b972 'test(vulns): cover VulnerabilitiesTab patch/exploit filters' replaced the old reference-tag-only implementation the report described ('implemented only via the reference-tag multi-select... has no genuine test') with independent `patchFilter`/`exploitFilter` state in src/renderer/pages/project-detail/VulnerabilitiesTab.tsx (lines 31-32, 104-116, 288-317) backed by real predicates `hasAvailablePatch`/`isExploitedVuln` in helpers.ts (checking `patchInfo.patchAvailability`/`patchedVersions` and `isKev`/`exploitStatus`, not reference tags), plus getCurrentFilters() now persists both into presets (VulnerabilitiesTab.tsx:186-192). A new dedicated test file, src/renderer/pages/project-detail/VulnerabilitiesTab.test.tsx, drives the real (unmocked) 'Patch Availability' and 'Exploit Status' selects end-to-end with genuine narrowing assertions plus a 'Clear Advanced Filters' reset test — closing the report's 'TC-VM-005 only asserts the label renders' finding for those two filters specifically. Ran `npx vitest run` on this file at HEAD: 4/4 passing. What the report flagged as SHALLOW that is NOT yet fixed: (a) CVSS-range and Source filters are still only verified through the preset-load path in src/renderer/pages/ProjectDetail.test.tsx (lines 3189-3219), where `@/components/FilterPresets` is fully stub-mocked (lines 258-296) — there is still no test that drives the real CvssRangeSlider/MultiSelectFilter controls inside VulnerabilitiesTab and observes the list narrow; (b) the test titled 'should load a preset with reference tags filter' (ProjectDetail.test.tsx:3221-3254) still loads a preset with `filters: { source: ['nvd'] }` — not any reference-tag filter — and asserts both fixture CVEs stay visible; it is functionally a duplicate of the source-filter test at line 3206 and cannot fail if reference-tag filtering breaks. TC-VM-005 (line 1537) is also still shallow ('should filter vulnerabilities by reference tags' only asserts the 'Reference Tags' label renders).
- **Root cause:** src/renderer/pages/ProjectDetail.test.tsx:3189-3219 exercises CVSS-range/source filtering only via `handleLoadPreset`'s direct state setters (`setCvssRange`/`setSourceFilter`), never through the actual `CvssRangeSlider`/`MultiSelectFilter` DOM controls, because those components are replaced by a stub mock at lines 258-296 in that file. src/renderer/pages/ProjectDetail.test.tsx:3221 is mistitled and tests the wrong code path (source filter, not reference tags) — confirmed by reading its body: the preset payload is `{ source: ['nvd'] }` and both fixture CVEs are `source: 'nvd'`, so the assertion 'both are visible' would pass even if `referenceTagFilter` filtering were deleted entirely.
- **Fix:** Test-only; no production code change needed (the underlying CvssRangeSlider/MultiSelectFilter components and their wiring in VulnerabilitiesTab.tsx already work correctly — this is closing a test-coverage gap, not a functional one). 1) In src/renderer/pages/project-detail/VulnerabilitiesTab.test.tsx, add two tests that open Advanced Filters (existing `openAdvancedFilters` helper) and drive the real controls: CVSS range via `screen.getAllByRole('slider')` (the two real `<input type="range">` elements the unmocked CvssRangeSlider renders — index 0 is min, index 1 is max per FilterPresets.tsx:226-243) with `fireEvent.change`; Source via clicking the button labeled 'Source' to open the MultiSelectFilter dropdown then `screen.getByLabelText('OSV')` checkbox (per FilterPresets.tsx:319-332, each option is a `<label>` wrapping a checkbox + text, so `getByLabelText(optionLabel)` resolves it once the dropdown is open). 2) In src/renderer/pages/ProjectDetail.test.tsx, fix the mistitled test at line 3221: either rename it to accurately describe what it tests (a source-filter regression with mixed-reference fixtures) since it's a harmless duplicate, or replace its body to genuinely drive the 'Reference Tags' MultiSelectFilter (open Advanced Filters, click the 'Reference Tags' button, check 'Patch Available', assert `CVE-WITH-PATCH` is visible and `CVE-NO-REFS` is not) — since `referenceTagFilter` isn't persisted to a preset (out of scope: not a PRD-08.3 bullet), this must be driven through direct UI interaction, not `handleLoadPreset`. 3) Optionally strengthen TC-VM-005 (line 1537) the same way, or delete it as now-redundant once the new VulnerabilitiesTab.test.tsx reference-tag test exists — do not keep both a fake and a real assertion for the same behavior.
- **Tests:** TDD order: (1) Failing first — `it('CVSS range filter (min,max sliders) narrows to vulns within range', ...)` in VulnerabilitiesTab.test.tsx: two vulns, `cvssScore: 9.8` (critical) and `cvssScore: 4.0` (high); open Advanced Filters; `fireEvent.change` the max-slider input down to 5 (or min-slider up to 9); assert only the matching severity group's heading remains, mirroring the existing patch/exploit test's `criticalGroup()`/`highGroup()` helpers. WHY: must fail if `onChange={setCvssRange}` wiring or `handleMinChange`/`handleMaxChange`'s boundary checks (`newMin <= localMax` etc.) regress — the preset-load test can't catch that because it bypasses the slider entirely. (2) `it('Source multi-select filter narrows to the selected source', ...)`: one `nvd` vuln, one `osv` vuln; open the Source dropdown, check 'OSV'; assert only the OSV vuln's group remains. (3) Fix/rename ProjectDetail.test.tsx:3221's test so its title and assertions match (drive the real 'Reference Tags' MultiSelectFilter via direct interaction, not a preset), so it can actually fail if the `referenceTagFilter` predicate (VulnerabilitiesTab.tsx:95-100) breaks.
- **Risk:** Low — additive/corrective test changes only. Main risk is DOM-selector fragility for the unlabeled range inputs (no aria-label on CvssRangeSlider's sliders, per FilterPresets.tsx:226-243); mitigate with `getAllByRole('slider')` positional indexing as used above rather than a label query, and note this lack of an accessible name as a separate (out-of-scope) NFR-04 observation, not something to silently 'fix' as part of this task.

---

## NFR

### NFR-01.1 — Application Startup < 3 seconds

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** S · **Product code:** no
- **Root cause:** No test anywhere measures time-to-usable-UI. Re-confirmed via grep across e2e/, server/, src/renderer/ for startup/TTI timing assertions tied to a 3s budget — zero hits. Post-migration, 'startup' = server cold boot (server/index.ts) + client first paint, a different concept than the PRD's Electron-era wording; unreconciled either way.
- **Fix:** Add one Playwright test that navigates cold to /dashboard against a production build (not the Vite dev server) and asserts time from navigation start to a real content marker (e.g. `getByRole('button',{name:'New Project'})` visible) is < 3000ms, measured with Date.now() bracketing page.goto()+the assertion — not waitForTimeout. No production code change expected unless the measurement itself reveals a regression.
- **Tests:** New e2e/critical-flows/startup-performance.spec.ts — `should reach a usable dashboard within 3 seconds of navigation`. WHY: must fail if server boot adds a blocking sync step or the client adds a render-blocking fetch before first paint; a fixed waitForTimeout could never catch that.
- **Risk:** Medium — must run against a built/served app, not `vite dev`, or the number is meaningless; cold-CI-runner variance may need a documented tolerance.

### NFR-01.2 — SBOM Import (1000 components) < 5 seconds

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** S · **Product code:** no
- **Root cause:** src/renderer/lib/parsers/cyclonedx.ts:164 `parseCycloneDX` has zero timing assertions in cyclonedx.test.ts (re-confirmed: no performance.now()/Date.now()/'5000' hits). The only 1000-item timed test in the repo (lib/generators/**tests**/cyclonedxGenerator.test.ts:158) measures export, not import.
- **Fix:** No production change expected. Add a timing test to cyclonedx.test.ts using a generated/fixture CycloneDX 1.5 JSON bom with 1000 components; call `await parseCycloneDX(json, 'large-sbom.json')` wrapped in performance.now(), assert elapsed < 5000 AND result.components.length === 1000 (sanity so timing isn't from an early bail-out).
- **Tests:** src/renderer/lib/parsers/cyclonedx.test.ts — new `describe('NFR-01.2 performance')` / `it('parses a 1000-component CycloneDX SBOM in under 5 seconds')`. WHY: must fail if per-component mapping (mapJsonComponentToComponent) regresses to O(n^2) (e.g. an accidental nested find() over all components per item).
- **Risk:** Low.

### NFR-01.3 — Vulnerability Scan (1000 components) < 10s local / < 60s API

- **Priority:** High · **Report:** GAP · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** no
- **Since report:** tests/perf/scan.perf.test.ts (commit d8c3617, this session) now asserts the local-DB half (<10s/1000 components) against the real matchVulnerabilitiesForComponents orchestration — a genuine, honestly-scoped test (its own comment documents that DB search is stubbed). The <60s 'API' half remains completely untested; re-verified at HEAD.
- **Root cause:** tests/perf/scan.perf.test.ts:9-11 mocks `queryByPurls` (the OSV/'API' half, called once per scan at src/renderer/lib/api/vulnMatcher.ts:498) to resolve instantly with an empty Map, and its `makeComponents()` (lines 30-41) sets only `cpe`, never `purl`, so the `purls.length>0` guard at vulnMatcher.ts:489 is never true — the API branch is never exercised at any latency. Separately, the local-DB search mock (lines 70-73) resolves on the same microtask, so the test can't detect that vulnMatcher.ts's per-component loop issues 1000 _sequential_ awaited calls to `getPlatform().database.search` — in production each is an HTTP round trip, so realistic per-call latency x 1000 sequential awaits is the real risk factor for the 60s ceiling, and nothing measures it today.
- **Fix:** 1) Add a second `it` in scan.perf.test.ts that gives the local-DB mock a small realistic per-call delay (e.g. setTimeout 5ms) and asserts total elapsed < 60_000ms for 1000 components — documented as standing in for the network-bound half of the budget. 2) Give a subset of makeComponents() fixtures a `purl` and add one non-zero delay (e.g. 2000ms) to the queryByPurls mock so the OSV branch is exercised at least once. 3) No production change unless step 1 reveals the sequential loop already exceeds budget under realistic latency — if so, batching the local-DB calls would be a separate FR-03-scoped fix; flag it, don't fold it in here.
- **Tests:** tests/perf/scan.perf.test.ts — extend with `it('stays under the 60s API budget when local-DB search and OSV calls carry realistic latency')`. WHY: must fail if a serial/unbatched per-component network call is reintroduced, or if OSV's single batched call regresses into N per-component calls.
- **Risk:** Medium — synthetic latency values are a judgment call; document as representative, not measured, per the existing test's own convention.

### NFR-01.4 — Dashboard Load < 2 seconds (100 projects)

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** S · **Product code:** no
- **Root cause:** src/renderer/pages/Dashboard.test.tsx has mockProjects fixtures (e.g. lines 246, 308) but zero performance.now()/Date.now()/'100 project' hits — re-confirmed by grep. It is functional/render-correctness only.
- **Fix:** No production change expected. Add a test that builds 100 mock Project objects (reuse the file's existing mockProjects-building pattern, scaled up) and times `render()` via the file's own `renderDashboard()` helper from call to the stats/list being visible; assert < 2000ms. Note this measures jsdom render+commit, an honest proxy for real browser paint, not identical to it.
- **Tests:** src/renderer/pages/Dashboard.test.tsx — new `describe('NFR-01.4 performance')` `it('renders with 100 projects in under 2 seconds')`. WHY: must fail if per-project severity/stat aggregation runs unmemoized at O(n^2) across renders.
- **Risk:** Low — jsdom timing is a proxy for browser paint; state that limitation explicitly rather than overclaiming coverage.

### NFR-01.5 — Search Response < 1 second for 10,000+ components

- **Priority:** High · **Report:** GAP · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** tests/perf/search.perf.test.ts (commit d8c3617, this session) builds a 15,000-component/5,000-vulnerability SearchIndex and asserts both a broad-match and a boolean AND/NOT query complete in <1000ms against the real buildSearchIndex()/searchIndex() (src/renderer/lib/search/searchIndex.ts) — this is the literal PRD wording ('10,000+ components... Query execution time'). The report's own citation (server/database/performance/queryCache.test.ts) evaluated a different code path (server-side NVD query cache); the literal requirement as worded is now genuinely covered by a real, non-mocked-away assertion. Verified by reading the current test file.
- **Root cause:** (Historical, now closed) no test exercised searchIndex()/buildSearchIndex() at >=10,000-component scale with a latency assertion.
- **Fix:** N/A — already implemented at HEAD; no further code change needed for the literal PRD bullet.
- **Tests:** Already present and passing: tests/perf/search.perf.test.ts. No action needed.
- **Risk:** Low.

### NFR-01.6 — UI Responsiveness < 100ms for all interactions

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** no
- **Since report:** The report's own cited evidence (e2e/critical-flows/cve-database-sync.spec.ts, loose ~5s-threshold timing tests) has since been deliberately test.skip()'d this session (now lines 227-236) with a comment stating the threshold was 'arbitrary and untied to any documented SLA' per Rule 9 — so even that loose stand-in no longer runs. The underlying GAP status is unchanged (arguably clearer now: zero executing timing assertions of any kind), but the report's specific citation is stale.
- **Root cause:** No automated interaction-latency budget exists in the CI-run suite; the only other artifact is the stale, pre-migration manual MCP report (e2e/mcp/specs/performance/page-load-performance.md, 2026-05-04), not CI-run.
- **Fix:** No production change expected initially. Add a narrowly-scoped Playwright test measuring a purely client-side interaction (command palette open via Ctrl+K, or sidebar toggle via Ctrl+Shift+S — already bound at src/renderer/components/shell/AppShell.tsx:44) using Date.now() around the keypress through the resulting DOM change, median of several samples (not one) to avoid CI jitter false-negatives.
- **Tests:** New `describe('NFR-01.6 performance')` in e2e/features/command-palette.spec.ts (or a new e2e/critical-flows/ui-responsiveness.spec.ts) — `it('opens the command palette within 100ms of Ctrl+K (median of 5 presses)')`. WHY: must fail if a debounce/animation delay or synchronous heavy work is added to the open handler.
- **Risk:** High — 100ms is extremely tight for browser-automation-measured latency; likely to be flaky or too loose to be meaningful. Recommend flagging to product that this PRD bullet may need re-scoping to 'client-only interactions' rather than a blanket 100ms gate on everything.

### NFR-02.1 — Scalability — 1,000+ projects

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** no
- **Root cause:** No test creates or loads 1,000 projects anywhere — re-confirmed via grep for '1000 project'/'1,000 project' across server/ and src/renderer/ (zero hits). Projects are one JSON file per project under PROJECTS_DIR (server/routes/projects.ts:8,36-51).
- **Fix:** No production change expected unless the scale test surfaces a real cap or O(n^2) directory scan. Use the new supertest harness (server/test-utils/testApp.ts) to POST 1,000 minimal project payloads to /api/projects, then GET the list and assert all round-trip and the listing operation completes in a reasonable time.
- **Tests:** server/routes/projects.test.ts — new `describe('NFR-02.1 scale')` `it('creates, lists, and deletes 1,000 projects without error')`. WHY: must fail if a filename-collision, in-memory cap, or O(n^2) directory listing regression is introduced.
- **Risk:** Medium — 1,000 real file writes in a test adds CI disk/time cost; consider whether a smaller-but-still-credible N is acceptable if 1,000 proves too slow.

### NFR-02.2 — Scalability — 50,000+ components per project

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** no
- **Root cause:** No test exercises a project with 50,000 components. Re-confirmed: the '50000' hits found in dbSeedingService.test.ts, dbVersionManager.test.ts, nvdApiV2Client.test.ts, and Search.test.tsx are all CVE-count fixtures, unrelated to component count; VirtualList.test.tsx exercises large-list rendering generically, not this specific scale/scenario.
- **Fix:** No production change expected if virtualization is genuine. Extend VirtualList.test.tsx's existing render-timing pattern to 50,000 items, and raise a project-scale variant of tests/perf/search.perf.test.ts's COMPONENT_COUNT to 50,000 to align with this exact PRD number (distinct from NFR-01.5's 15,000/10k+ target).
- **Tests:** src/renderer/components/VirtualList.test.tsx — new `it('renders a 50,000-item list without timing out')`. WHY: must fail if virtualization is bypassed (e.g. the full array gets mapped to real DOM nodes instead of a windowed subset).
- **Risk:** Low.

### NFR-02.3 — Scalability — 1,000,000+ vulnerability records

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** no
- **Since report:** The flaky-insert issue CLAUDE.md documents ('inserts 200,000 rows one-by-one under a 60s timeout') is independently fixed: server/database/dbSeedingService.test.ts:545-554 now uses a single recursive-CTE INSERT instead of 200 batched round-trips, removing the flakiness. This does NOT close the scale gap — still capped at 200,000 rows (1/5 of the PRD's 1,000,000+) and still scoped only to checkFirstRun() state-detection, not query/search/dashboard behavior at scale. CLAUDE.md's 'Pre-existing Issues' note about this test is now itself stale and should be updated separately.
- **Root cause:** server/database/dbSeedingService.test.ts:534-563 is the only 6-figure-scale evidence in the repo, capped at 200,000 rows, scoped to seeding-state detection only.
- **Fix:** No production change expected. Add a companion test at 1,000,000-row scale reusing the now-fast recursive-CTE insert pattern (a single SQL statement scales far better than 1M prepared-statement round-trips) that exercises a real query path — e.g. NvdDatabase.searchCVEsByText()/getTotalCVECount() (server/database/nvdDb.ts:766) — not just checkFirstRun().
- **Tests:** server/database/nvdDbEnhanced2.test.ts (or new server/database/scale/millionRecords.test.ts) — `describe('NFR-02.3 scale')` `it('searchCVEsByText returns correct results against 1,000,000 seeded CVE rows')`. WHY: must fail if the FTS5/index-based query plan regresses to a full table scan at this scale — the actual thing 'handle 1M+ records' is guarding against, not just successful insertion.
- **Risk:** Medium — a 1M-row in-test SQLite DB increases CI runtime/memory; scope to one dedicated, generously-timed-out test file, as dbSeedingService.test.ts already does.

### NFR-02.5 — Scalability — Database Size 10GB+

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** M · **Product code:** no
- **Root cause:** No test provisions or asserts behavior against a 10GB+ database file; the only related figure (mocked ~987MB dbSize in the stale pre-migration MCP report) is not real or automated. Re-confirmed: no '10GB'/'10gb' hits anywhere in test files.
- **Fix:** Literally provisioning a 10GB SQLite file in CI is impractical — smallest credible slice: prove the query paths that matter at that scale use indexed/FTS lookups, not full-table scans, since an indexed query's latency is size-invariant (the actual mechanism the PRD requirement depends on). Assert EXPLAIN QUERY PLAN for searchCVEsByText/searchCVEsByCPE/searchCVEsByProduct (server/database/nvdDb.ts:766,789,827) never contains a full `SCAN TABLE` step. Pair with the NFR-02.3 1M-row fix above as the largest practical proxy, explicitly noting literal 10GB provisioning is out of scope for the automated suite.
- **Tests:** server/database/nvdDbEnhanced2.test.ts — new `describe('NFR-02.5 — index usage at scale')`, one case per search method, asserting the query plan's `detail` field shows index/FTS `USING` (not `SCAN TABLE cves`/`SCAN TABLE cpe_matches`). WHY: must fail if a migration drops or fails to create an index — the failure mode that would make a 10GB database unusably slow.
- **Risk:** Medium — EXPLAIN QUERY PLAN text is SQLite-version-sensitive; assert on structured `detail`/`opcode` fields, not exact string matches, to avoid brittleness.

### NFR-04.4 — Usability — Keyboard Shortcuts (all actions accessible via keyboard)

- **Priority:** High · **Report:** SHALLOW · **Now:** PARTIALLY_FIXED · **Effort:** S · **Product code:** yes
- **Since report:** e2e/features/command-palette.spec.ts has been fully rewritten this session — verified by reading the current file: zero `expect([true,false]).toContain(...)` tautologies remain, header comment documents the rewrite. Ctrl+Shift+S is now a real global listener (src/renderer/components/shell/AppShell.tsx:39-51, commit d94ec1d), closing one previously-mislabeled shortcut. The remaining defect is narrower than the original report implied.
- **Root cause:** src/renderer/lib/commands/registerCommands.ts still advertises `shortcut` labels for six commands (Ctrl+Shift+D at line ~22, Ctrl+N, Ctrl+I, Ctrl+E, F1, Ctrl+,) shown in the CommandPalette UI, with no matching global keydown listener anywhere in src/renderer (confirmed by grep — only Ctrl+K/Ctrl+Shift+P in CommandPalette.tsx and Ctrl+Shift+S in AppShell.tsx exist). Every action IS reachable via Ctrl+K -> arrow -> Enter, so the literal PRD bullet is technically met through the palette; the defect is advertised-but-unbound direct shortcuts, a correctness/UX bug more than a strict a11y gap. Note: Ctrl+N and F1 are also browser-reserved in most browsers and cannot be reliably intercepted client-side.
- **Fix:** Recommend the minimal fix (Rule 2): remove the misleading `shortcut` labels for the six unbound commands in registerCommands.ts, matching the precedent already set for 'Go to Search' (comment at ~line 41-42 documents removing a similarly-misleading label there). Do NOT add new global bindings (larger, speculative scope per Rule 2, and Ctrl+N/F1 are not reliably bindable in a browser anyway) — flag that as a separate, larger UX decision for product if truly desired.
- **Tests:** Extend/add a unit test (e.g. a new src/renderer/lib/commands/registerCommands.test.ts, checking first whether one already exists) asserting the six delabeled commands have `shortcut: undefined`. WHY: must fail if a misleading label is re-added without a matching global binding — the exact defect found here.
- **Risk:** Low.

### NFR-04.5 — Usability — Accessibility (WCAG 2.1 Level AA)

- **Priority:** High · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Confirmed unchanged by re-reading e2e/a11y/accessibility.spec.ts at HEAD: still only fails on impact==='critical' (line 54), still only 4 pages (dashboard, search, settings, executive; lines 16-21). The task's staleness hint ('a11y axe gate already landed this session') refers to this gate's pre-existing status as a real, green, automated CI check (vs. the stale manual MCP/Lighthouse reports) — that was already true at report time; nothing has tightened the bar since.
- **Root cause:** e2e/a11y/accessibility.spec.ts:54 (`const critical = violations.filter((v) => v.impact === 'critical')`) lets serious/moderate AA violations (e.g. contrast, missing labels) pass silently — they're attached to the report (lines 47-52) but never asserted on. CORE_PAGES (lines 16-21) covers 4 of the app's routes; ProjectDetail, VEX views, and the dependency/attack graph are unvisited.
- **Fix:** 1) Tighten the filter at line 54 to include 'serious' impact, not just 'critical' — a staged tightening, not a blind jump to zero-violations (which could redden the gate before triage). 2) Add ProjectDetail (needs a seeded project id via the existing e2e project-creation helper) to CORE_PAGES as the highest-value missing page. 3) Run locally first and triage/fix any newly-failing serious violations before landing — never silently loosen the filter back to make it pass.
- **Tests:** e2e/a11y/accessibility.spec.ts — modify the impact filter (line 54) and CORE_PAGES (lines 16-21). WHY: the point of an axe gate is to catch real AA regressions; one that only catches 'critical' misses most real-world AA issues, so the tightened gate must be proven to actually fail on a deliberately-introduced regression (e.g. temporarily strip an aria-label) as a one-off manual verification, not a permanent test.
- **Risk:** Medium — tightening the filter will likely surface pre-existing serious violations needing real fixes before the gate can go green; budget triage time, don't assume zero.

### NFR-07.1 — Maintainability — Code Coverage 95% minimum

- **Priority:** High · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** no
- **Root cause:** vitest.config.ts:78-83 enforces statements 90 / branches 80 / functions 90 / lines 90 — all below the PRD's 95%, so CI (.github/workflows/ci.yml:36-37, `npm run test:coverage`) does not fail if coverage sits in the 90-95% band. Re-confirmed unchanged at HEAD. No current, committed coverage number exists post-migration — the only local snapshot is gitignored and stale (pre-migration).
- **Fix:** Do NOT raise thresholds blindly. 1) Run `npm run test:coverage` to get real current statements/branches/functions/lines percentages. 2) If all four are already >=95%, raise vitest.config.ts:78-83 to 95 across the board. 3) If any is below 95%, identify the largest-gap files from the coverage report and add targeted tests for real, meaningful gaps (not coverage-padding tests for dead code — some low-coverage lines may be candidates for deletion instead, per other dead-code findings elsewhere in this codebase) before raising the gate.
- **Tests:** No new test file up front — this is coverage-report-driven; whichever files step 1 flags as under-95% get real tests added for their uncovered branches/functions. Config change: vitest.config.ts thresholds.
- **Risk:** Medium — resist writing low-value tests purely to hit a number (Rule 9); size (S vs L) is unknown until the real coverage report is run.

### NFR-07.2 — Maintainability — Code Quality: ESLint with zero warnings

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Root cause:** package.json:25 `lint: eslint .` has no `--max-warnings 0`; `.github/workflows/ci.yml` runs plain `npm run lint`. Re-ran `npx eslint .` directly at HEAD: still exactly 20 warnings (react-hooks/exhaustive-deps x5, react-refresh/only-export-components x10, no-case-declarations x4, no-empty-pattern x1) — none fail CI.
- **Fix:** This is a documented, deliberate CLAUDE.md deprioritization, not an oversight — per Rule 7, surface the conflict rather than silently resolving it either way. Recommend option A (matches PRD's literal 'zero'): fix all 20 (add missing hook deps or an explicit, justified eslint-disable-next-line for genuinely-intentional-deps cases; split constants/utils out of the 10 flagged component files; add block scoping for the 4 no-case-declarations; name the 1 empty-pattern Playwright fixture param), then add `--max-warnings 0` to the lint script. Verify each of the 5 exhaustive-deps fixes individually first — CLAUDE.md flags them as 'intentional', so blindly adding a dep could introduce an infinite render loop.
- **Tests:** No new Vitest/Playwright test — the regression guard is `npx eslint . --max-warnings 0` exiting 0, wired into package.json's lint script so CI enforces it going forward. WHY this counts as the 'test': the actual intent of 'zero warnings' is that no future PR introduces a new one — only a `--max-warnings 0` CI gate encodes that.
- **Risk:** Low — mechanical fixes per CLAUDE.md's own categorization; main risk is the 5 'intentional deps' cases needing individual verification, not a blanket dep-array fix.

### NFR-07.3 — Maintainability — Documentation: All APIs documented with JSDoc

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** L · **Product code:** yes
- **Since report:** Re-checked all 8 route files at HEAD (one more than the report's 7, since server/routes/osv.ts was added this session): backup.ts, container.ts, intelligence.ts, projects.ts, storage.ts still have 0 JSDoc blocks; database.ts has 1; sbom.ts has 2; the new osv.ts has 3. The new route set a better documentation example but the five untouched files remain fully undocumented — gap unchanged for those.
- **Root cause:** server/routes/projects.ts:36 `projectRouter.post('/', ...)` and the equivalent handlers in backup.ts, container.ts, intelligence.ts, storage.ts have no JSDoc describing the endpoint contract (request/response shape, error cases).
- **Fix:** Add a JSDoc block above every Router.<method>(...) handler in the 5 zero-JSDoc files plus fill database.ts's remaining routes, following the in-repo convention already set by the new osv.ts (@route/@param/@returns + one-line behavior description) — Rule 11, match existing convention rather than inventing a new one. Purely additive documentation, no logic change.
- **Tests:** No runtime test (JSDoc isn't executable). Add a lightweight regression gate so this doesn't rot again — either an ESLint `jsdoc/require-jsdoc` rule scoped to `server/routes/**/*.ts` targeting exported router handler call expressions, or a small script (`scripts/check-route-jsdoc.ts`) failing on any `Router.<method>(` call with no preceding `/**` block, wired into `npm run lint` or a new `npm run check:api-docs`. WHY a gate is needed: without one, 'add JSDoc' silently rots on the next new route — exactly what happened here (osv.ts got JSDoc, the five older files didn't, with nothing to catch the inconsistency).
- **Risk:** Low — mechanical, but ~30+ individual handlers across 5 files plus new tooling is real effort, not a quick pass.

### NFR-08.1 — Testability — Unit Tests 95% coverage requirement

- **Priority:** High · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** S · **Product code:** no
- **Since report:** Same underlying evidence and root cause as NFR-07.1 (vitest.config.ts:78-83); re-confirmed unchanged at HEAD. This is the same fix, tracked under a second req ID per the PRD's separate NFR-07/NFR-08 rows — do not implement it twice.
- **Root cause:** vitest.config.ts:78-83 enforces 90/80/90/90, not 95%, identical to NFR-07.1. The 4,600+-test suite being green establishes pass-rate, not coverage percentage — those are different metrics, and the real current percentage is unverified (no current committed coverage report).
- **Fix:** Cross-reference NFR-07.1's fix — running `npm run test:coverage` and either raising the threshold (if already >=95%) or closing real gaps first is a single piece of work that satisfies both req IDs simultaneously.
- **Tests:** Same as NFR-07.1: coverage-report-driven, no test written blind ahead of the real numbers.
- **Risk:** Low — the substantive risk/effort is already captured under NFR-07.1; this entry exists only because the PRD rows are separate.

### NFR-08.2 — Testability — Integration Tests: all API endpoints covered

- **Priority:** High · **Report:** GAP · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** Commit af3bfe0 ('test(server): supertest integration harness + /api/projects coverage (NFR-08)') plus follow-on commits this session added server/test-utils/testApp.ts (real Express app via extracted createApp(), throwaway temp DATA*DIR, dev-mode auth skip) and now every route file has a corresponding *.test.ts: backup, container, database, intelligence, osv, projects, sbom, storage (2,323 lines, 154 tests). Verified by direct run at HEAD: `Test Files 8 passed (8) / Tests 154 passed (154)`. This closes the report's exact citation (zero server/routes/\_.test.ts files existed at report time).
- **Root cause:** (Historical, now closed) zero test files existed for server/routes/\*.ts; the app's own REST surface had no route-level integration tests.
- **Fix:** N/A — already implemented at HEAD across all 8 route files.
- **Tests:** Already present and passing: server/routes/{backup,container,database,intelligence,osv,projects,sbom,storage}.test.ts. Optional (not required by the PRD bullet, not verified here): a per-route HTTP-method inventory to confirm literally every individual endpoint is hit, not just that each file has tests.
- **Risk:** Low.

### NFR-08.4 — Testability — BDD Tests: all major features defined with Gherkin

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** L · **Product code:** no
- **Since report:** Directory layout changed slightly (tests/bdd/features/ now uses subfolders: analytics/, audit/, database/, export/, parsers/, sbom-generator/, plus example.feature) but the substance is unchanged: same 10 .feature files, same broken script, no cucumber config.
- **Root cause:** package.json:41 `test:bdd: vitest --run tests/bdd-simple.test.ts` points to a nonexistent file (confirmed: `tests/bdd-simple.test.ts` does not exist). The real suite (tests/bdd/features/\*_/_.feature, 10 files) has step definitions (tests/bdd/step-definitions/\*.steps.ts) using the installed `@cucumber/cucumber` devDependency, but no cucumber config file exists anywhere in the repo, and .github/workflows/ci.yml has zero 'bdd'/'cucumber' references. The 10 features still don't cover VEX generation, attack/dependency graph, dashboards, or KEV/EPSS scoring.
- **Fix:** 1) Fix the broken script: point `test:bdd` at the real cucumber-js CLI with a new tests/bdd/cucumber.cjs config specifying feature/step-definition paths and a TS transpile hook (tsx/ts-node) for the .ts step defs. 2) Wire `npm run test:bdd` into .github/workflows/ci.yml as a real step. 3) Explicitly do NOT attempt 'all major features' in this pass — each missing major feature (VEX, attack graph, dashboards, KEV/EPSS, command palette) needs its own .feature + step defs, which is a large, open-ended follow-up, not this fix; say so plainly rather than silently under-scoping the requirement.
- **Tests:** No new Vitest/Playwright test — the regression guard is `npm run test:bdd` exiting 0 in CI (currently exits 1 immediately). Verify the existing example.steps.ts scenario has a real assertion, not just 'the runner didn't crash', so wiring drift (a .feature edited without updating its .steps.ts) is caught. WHY: a BDD suite that runs but asserts nothing is as useless as the current broken script.
- **Risk:** Medium — cucumber-js + tsx/ts-node module interop (ESM vs CJS) can be fiddly; and even after the wiring fix, the 'all major features' breadth gap remains open and should be reported as a residual gap, not glossed over (Rule 12).

---

## SR

### SR-01.1 — Encrypt API keys in local storage — real-crypto test coverage

- **Priority:** High · **Report:** SHALLOW · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** no
- **Since report:** The 2026-07-25 report's only gap was missing test coverage — server/services/storage/secureStorage.ts:29-53 (AES-256-GCM encrypt/decrypt, PBKDF2 machine key) was already correct, but no test exercised the real code path or asserted the on-disk file was non-plaintext. At HEAD on fix/prd-gap-remediation, server/services/storage/secureStorage.test.ts (91 lines, currently UNTRACKED — not yet committed) exists and imports the real module (no platform/IPC mocking), asserting: (1) the persisted ciphertext !== plaintext and the raw credentials.json string does not contain the plaintext key, (2) encrypt->decrypt round-trips exactly, (3) a tampered/too-short ciphertext returns null instead of throwing. Ran `npx vitest run server/services/storage/secureStorage.test.ts` — 3/3 pass. This closes the SHALLOW gap; only remaining action is to commit the file.
- **Root cause:** Historical only: server/services/storage/secureStorage.ts:29-53 (encrypt/decrypt) had no covering test; server/services/storage/secureStorage.test.ts did not exist as of the report date.
- **Fix:** No further product code change needed — secureStorage.ts's crypto is already correct. Action item is purely process: commit the already-written server/services/storage/secureStorage.test.ts (currently untracked in the working tree) so it lands in version control and runs in CI.
- **Tests:** Already added: server/services/storage/secureStorage.test.ts — 'never persists the plaintext API key to the credentials store' (reads raw file, asserts ciphertext !== plaintext and raw string doesn't contain the key), 'round-trips encrypt -> decrypt back to the original API key', 'returns null instead of throwing when the stored ciphertext is tampered/too short'. Verified green (3/3) via `npx vitest run server/services/storage/secureStorage.test.ts`. No further test authoring required.
- **Risk:** Low — test-only, already written and passing; the only remaining step is committing an untracked file so it isn't silently lost.

### SR-01.3 — No telemetry without explicit consent — add a regression guard (no telemetry exists today)

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** S · **Product code:** no
- **Since report:** Unchanged since the report. Re-confirmed at HEAD: repo-wide grep for telemetry|sendBeacon|google-analytics|mixpanel|amplitude|posthog|segment.io across src/ and server/ returns zero hits, and grep for 'consent' returns zero hits. AppSettings (src/shared/types.ts:424-441) has no consent/telemetry field. The requirement is trivially satisfied by absence, exactly as the report found, and nothing has been added to close the 'no regression gate' concern.
- **Root cause:** No telemetry/tracking code exists (confirmed by grep at HEAD), so the PRD bullet is currently met by omission — but there is no test, lint rule, or dependency allowlist anywhere that would catch a future PR silently introducing telemetry without a consent gate. src/shared/types.ts:424-441 (AppSettings) has no consent field to point future telemetry code at.
- **Fix:** Minimum slice only — do NOT add a speculative telemetryConsent settings field, Settings UI toggle, or consent-gating utility module now: there is no telemetry consumer to wire it to, so that would be speculative code per Simplicity First (no abstractions for single-use/non-existent features). Instead add a static regression-guard test with no production code change: new tests/policy/telemetryConsent.test.ts that (a) reads package.json dependencies+devDependencies and asserts none of a small known-telemetry-SDK denylist is present (mixpanel, amplitude-js, posthog-js, @segment/analytics-next, @amplitude/analytics-browser, react-ga, react-ga4, universal-analytics, @microsoft/applicationinsights-web), and (b) recursively scans src/ and server/ source text (excluding node_modules/dist/tests/fixtures) for telemetry call-site patterns (navigator.sendBeacon(, gtag(, .track(, ga('send) and asserts zero matches. If either check ever fires, that is the signal for a developer to add an explicit consent gate before merging, rather than the invariant being silently broken.
- **Tests:** TDD order: (1) write tests/policy/telemetryConsent.test.ts asserting the two invariants above against the current repo state — it should pass immediately (this documents intent going forward, not a bugfix, since nothing is broken today). (2) To prove the test can actually fail (Rule 9), do NOT mutate the real package.json/source as a permanent test — instead add a second small unit test that calls the same denylist/pattern-matching helper function directly against an in-memory fixture list containing 'mixpanel' (or a fixture string containing 'navigator.sendBeacon(') and asserts it is flagged; this proves the detector works without coupling the test to real repo state. Keep both cases in tests/policy/telemetryConsent.test.ts.
- **Risk:** Low. Main risk is false positives from the text-scan denylist (e.g., a legitimate identifier matching '.track(' in an unrelated context like drag-and-drop tracking) — keep patterns specific and scoped to src/+server/ only, exclude tests/fixtures/docs, and review any failure manually rather than auto-suppressing.

### SR-02.1 — Regular dependency updates for known vulnerabilities — add Dependabot configuration

- **Priority:** Medium · **Report:** GAP · **Now:** STILL_OPEN · **Effort:** S · **Product code:** yes
- **Since report:** Unchanged since the report; re-confirmed at HEAD. `find . -iname "dependabot*"` and `-iname "renovate*"` (excluding node_modules) both return zero results. .github/workflows/ci.yml:143 still runs `npm audit --audit-level=moderate || true`. .github/workflows/sbom-scan.yml:27 still points SBOM_PATH at the fixture `e2e/fixtures/sbom-with-vulns.json` with the comment '<- replace with your SBOM', not this project's own dependency tree.
- **Root cause:** No .github/dependabot.yml or renovate.json exists anywhere in the repo (confirmed at HEAD). .github/workflows/ci.yml:143 (`npm audit --audit-level=moderate || true`) means even manual detection never blocks a merge. .github/workflows/sbom-scan.yml:27 scans a vulnerability fixture, not package-lock.json, so it doesn't substitute for dependency monitoring. Re-running `npm audit --audit-level=high` at HEAD confirms this is not theoretical: 39 vulnerabilities exist today (17 high, 7 critical), including `xlsx` with 'No fix available' — there is currently no automated process that surfaces or tracks these over time.
- **Fix:** Minimum credible slice — add automated dependency-update tooling only; explicitly do NOT flip the CI audit gate to hard-fail in this same change (see risk). Add .github/dependabot.yml:

```
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
```

Leave ci.yml:143's `|| true` as-is in this change; separately file a follow-up to run `npm audit fix` for the fixable subset and resolve/replace `xlsx` (no fix available) before that gate can safely become blocking.

- **Tests:** TDD order: (1) write tests/policy/dependencyUpdates.test.ts FIRST (fails red because .github/dependabot.yml doesn't exist yet): it('has a dependabot config with a weekly npm ecosystem update schedule') — fs.existsSync('.github/dependabot.yml'), read raw text, assert /package-ecosystem:\s*["']?npm/ and /interval:\s*["']?(daily|weekly)/ both match (rejects e.g. a schedule changed to 'monthly' or removed, which would defeat 'regular'); it('also tracks github-actions ecosystem updates') — assert a second package-ecosystem: "github-actions" block exists. (2) Add .github/dependabot.yml → test goes green. Why it can fail when logic changes: deleting the file or weakening the schedule breaks the test, encoding 'regular dependency updates' as an enforced artifact rather than a one-time add.
- **Risk:** Medium. (a) Dependabot's security-update PRs also depend on the repo's GitHub-Settings 'Dependabot alerts' toggle, which this code change cannot enable or verify — flag to the repo owner separately. (b) Do not couple this to making `npm audit` blocking in the same PR: doing so today would immediately break CI, since `npm audit --audit-level=high` currently reports 17 high + 7 critical findings including `xlsx` with no available fix — that cleanup is a larger, separate workstream.

### SR-02.2 — Static analysis before releases — add a real security SAST (CodeQL) scan of the app's own source

- **Priority:** Medium · **Report:** SHALLOW · **Now:** STILL_OPEN · **Effort:** M · **Product code:** yes
- **Since report:** Unchanged since the report; re-confirmed at HEAD (no codeql-action/init or codeql-action/analyze steps exist anywhere in .github/workflows — only sbom-scan.yml:72's upload-sarif). New finding beyond the report: ci.yml's own triggers (`branches: [ main, develop ]`, ci.yml:5,7) don't match this repo's real branches — `git branch -a` shows only `master` plus feature branches, no `main`/`develop` — so even the existing ESLint gate the report credited may never fire on pushes/PRs to the actual integration branch. Also newly observed: release.yml's build step (`npm run dist:win/mac/linux`, release.yml:94-99) and ci.yml's build job (`npm run pack`, ci.yml:79) reference scripts that don't exist in package.json at all (verified via a dump of package.json scripts) — both pipelines are already stale/non-functional, independent of this SR item.
- **Root cause:** The only 'static analysis' gate today is ESLint (ci.yml:36-37, release.yml:57-58) — a style/type linter, not a security SAST tool. The repo's one CodeQL reference, sbom-scan.yml:70-74 (`github/codeql-action/upload-sarif`), only uploads SARIF produced by the vulnshield CLI scanning a fixture SBOM (sbom-scan.yml:27, e2e/fixtures/sbom-with-vulns.json) for dependency vulnerabilities — it never runs codeql-action/init + analyze against this repo's own TypeScript/JavaScript source, so no source-level SAST exists. Additionally, ci.yml:5,7 (`branches: [ main, develop ]`) doesn't match the repo's actual branches (only `master` + feature branches exist), so any new SAST workflow must explicitly target `master` to actually run.
- **Fix:** Minimum credible slice — add a dedicated CodeQL analysis workflow correctly scoped to the real branch, rather than trying to gate the already-broken release.yml pipeline: new .github/workflows/codeql.yml with `on: push: branches: [master]` and `on: pull_request: branches: [master]` (not ci.yml's stale main/develop), permissions: security-events: write (mirroring sbom-scan.yml:19), and a single `analyze` job: github/codeql-action/init@v3 with `languages: javascript-typescript`, then an explicit `npm ci` build step (Vite+tsc+CLI project — default autobuild may not compile it fully), then github/codeql-action/analyze@v3. Do NOT wire this as a `needs:` dependency of release.yml's build/sign job: release.yml's dist:win/mac/linux scripts don't exist in package.json (pre-existing, out-of-scope breakage) so gating on it wouldn't deliver a real control. Separately (outside this code change, since it's a GitHub repo setting) recommend making the new CodeQL check + lint check required status checks on `master` via branch protection.
- **Tests:** TDD order: (1) write tests/policy/staticAnalysisGate.test.ts FIRST (fails red because .github/workflows/codeql.yml doesn't exist yet): it("has a CodeQL analysis workflow that scans this repo's own source") — assert the file exists and its raw text contains both 'codeql-action/init' and 'codeql-action/analyze' and a 'languages:' line including 'javascript-typescript' (or 'javascript'); it("triggers CodeQL on the repository's real integration branch") — raw-text-assert the workflow's branches list includes 'master', specifically guarding against copy-pasting ci.yml's dead 'main'/'develop' filter (the concrete 'why': without this assertion a future edit could silently reintroduce the same non-firing-trigger bug ci.yml already has). (2) Add codeql.yml → tests green. Caveat to state plainly: these are static text/config assertions runnable offline in Vitest — they verify the workflow file's shape, not that CodeQL actually completes successfully on GitHub's runners; that can only be confirmed by watching a real Actions run after pushing, which is outside this test suite's reach.
- **Risk:** Medium. (a) CodeQL's default autobuild may not correctly build this mixed Vite+tsc+CLI project; may need an explicit `npm ci && npm run build:all` step before analyze for full coverage — verify via a real PR run, not just the local text-assertion test. (b) This does not by itself block a release the way 'before releases' implies unless paired with a branch-protection required-status-check on `master` — a repo-settings change to request from the repo owner, tracked separately since it's outside code. (c) ci.yml's stale main/develop branch filters are a larger pre-existing issue (potentially meaning lint/unit/e2e/security-scan jobs never run on real PRs to master) that is out of scope for SR-02.2 specifically but should be raised to the team as a separate, high-priority finding.

---

## CR

### CR-03.1 — CycloneDX: Support v1.0 - v1.5 (JSON/XML)

- **Priority:** High · **Report:** SHALLOW · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** yes
- **Since report:** Report (2026-07-25) cited two problems: (1) the XML path crashed on real fast-xml-parser output (TypeError at old cyclonedx.ts:446 mapComponentType) because cyclonedx.test.ts mocked fast-xml-parser and pre-flattened <components><component> before it reached the real code; (2) no test fixture pinned specVersion 1.0/1.1/1.2/1.3, only 1.4/1.5, so 'support 1.0-1.5' was asserted by permissive-default behavior, not verified. Both are now fixed on this branch: commit 277d048 (2026-07-25) added an explicit SUPPORTED_CYCLONEDX_SPEC_VERSIONS allow-list (src/renderer/lib/parsers/cyclonedx.ts:136, '1.0'..'1.6') plus assertSupportedSpecVersion() gating (called from parseCycloneDXJson at cyclonedx.ts:213 and parseCycloneDXXml at cyclonedx.ts:270), and fixed XML specVersion extraction to read the xmlns namespace instead of the BOM revision attribute (extractXmlSpecVersion, cyclonedx.ts:152-155). Commit 642e519 (2026-07-27) fixed the real crash: fast-xml-parser's isArray option now forces only repeatable child elements (component, vulnerability) to arrays instead of the singular containers, deleted the fast-xml-parser mock from cyclonedx.test.ts (real parser now exercised), and added src/renderer/lib/parsers/cyclonedx.xml.test.ts for real-parser-shape coverage. cyclonedx.test.ts:861-921 ('CycloneDX specVersion support 1.0-1.5 (CR-03.1)') now runs it.each(['1.0','1.3','1.5']) for both JSON and XML (asserting metadata.formatVersion round-trips), plus explicit-rejection tests for an unsupported specVersion ('99.9') in both formats. Verified at HEAD (8c3cc40, branch fix/prd-gap-remediation): `npx vitest run src/renderer/lib/parsers/cyclonedx.test.ts src/renderer/lib/parsers/cyclonedx.xml.test.ts` and the CVSS suite together -> 71/71 pass; `npx eslint` on the touched files -> 0 errors; `tsc -p tsconfig.app.json --noEmit` -> 0 errors referencing these files. No remaining code or test work for this sub-requirement.
- **Root cause:** Historical (now fixed): src/renderer/lib/parsers/cyclonedx.ts extractComponentsFromXml (pre-642e519) assumed fast-xml-parser's isArray forced the singular <components> container to an array, but real output nests as `bom.components = [{ component: [...] }]`; the extractor treated that wrapper object itself as one component and crashed reading an undefined `type` in mapComponentType. Compounding this, there was no specVersion allow-list (pre-277d048), so 1.0-1.3 fixtures were simply never exercised even though the mapping logic was version-agnostic.
- **Fix:** Already implemented, no further change required. (For reference, the shape of the fix that landed: add a version allow-list + assertSupportedSpecVersion() gate called from both parseCycloneDXJson and parseCycloneDXXml; fix XML specVersion extraction to read the xmlns namespace; fix fast-xml-parser isArray config to force only repeatable child elements, not singular containers; delete the fast-xml-parser mock so tests hit the real parser.)
- **Tests:** Already added and passing: src/renderer/lib/parsers/cyclonedx.test.ts:861-921 (it.each(['1.0','1.3','1.5']) for JSON and XML asserting formatVersion round-trip; explicit-rejection test for specVersion '99.9' in both formats) and src/renderer/lib/parsers/cyclonedx.xml.test.ts (real, unmocked fast-xml-parser coverage of nested components/vulnerabilities). No further test work needed for this sub-requirement.
- **Risk:** Residual/acceptable deviation: SUPPORTED_CYCLONEDX_SPEC_VERSIONS also includes '1.6' beyond the PRD's literal '1.0-1.5', intentionally and with an inline comment (cyclonedx.ts:129-134) explaining that the Syft SBOM-from-binary feature emits 1.6 and must round-trip (SyftService.test.ts). This is a documented superset, not a gap against the CR-03.1 bullet (1.0-1.5 is still fully supported and tested) -- flagged for awareness only, no action needed.

### CR-03.3 — CVSS: Support v3.0 and v3.1

- **Priority:** High · **Report:** SHALLOW · **Now:** FIXED_SINCE_REPORT · **Effort:** S · **Product code:** yes
- **Since report:** Report's orchestrator-verified finding (tied to FR-04.3) was that parseCvssVector() (src/renderer/lib/cvss/parser.ts:78 at time of report) returned null for every valid CVSS 3.0/3.1 vector because parseMetricValue mapped short prefixes (AV/AC/PR/UI/C/I/A) directly onto CVSS_METRIC_VALUES, which is keyed by full camelCase names -- only the hand-cased 'S'->'scope' worked, everything else threw and was swallowed by the outer try/catch. There was also no lib/cvss/parser.test.ts, so the bug was invisible to the suite. Both are fixed on this branch by commit 4b3b178 (2026-07-25): added an explicit PREFIX_TO_METRIC_KEY map (src/renderer/lib/cvss/parser.ts:69-80) used by parseMetricValue (parser.ts:83-102), which now throws a descriptive error for an unrecognized prefix or an unrecognized metric value (fails loudly -> parseCvssVector returns null instead of silently passing the raw letter through), and added src/renderer/lib/cvss/parser.test.ts with 6 cases: a real CVSS 3.1 vector (asserts version '3.1', all 8 full metric names, baseScore 9.8, severity 'critical' against the known NVD reference score), a real CVSS 3.0 vector (version '3.0', scope 'Changed', baseScore 9.6 against the known reference score), plus null-return cases for empty string, too-few-metrics, unrecognized metric value ('AV:Z'), and non-CVSS input. Verified at HEAD (8c3cc40): `npx vitest run src/renderer/lib/cvss/parser.test.ts` -> 6/6 pass (part of the 71/71 combined run); `npx eslint` -> 0 errors; `tsc -p tsconfig.app.json --noEmit` -> 0 errors. No remaining code or test work for this sub-requirement.
- **Root cause:** Historical (now fixed): src/renderer/lib/cvss/parser.ts parseMetricValue used `const mappingKey = prefix === 'S' ? 'scope' : prefix` then indexed CVSS_METRIC_VALUES[mappingKey], but CVSS_METRIC_VALUES (src/shared/constants.ts) is keyed by full metric names ('attackVector', 'attackComplexity', ...), not raw prefixes ('AV', 'AC', ...); every metric except Scope hit an undefined bucket, `mapping[value]` threw, and the outer try/catch in parseCvssVector caught it and returned null unconditionally.
- **Fix:** Already implemented, no further change required. (For reference: an explicit prefix->fullKey lookup table plus loud failure -- throw, not silent passthrough -- on an unmapped prefix or unrecognized short value.)
- **Tests:** Already added and passing: src/renderer/lib/cvss/parser.test.ts (6 cases covering real 3.1 and 3.0 vectors with known reference base scores, plus 4 null-return edge cases including the specific 'unrecognized metric value' regression this bug would have mishandled). No further test work needed for this sub-requirement.
- **Risk:** None material for this bullet. Note for awareness only: calculateBaseScore(metrics) does not branch on CVSS version (3.0 vs 3.1) -- this is spec-correct (the official CVSS v3.0 and v3.1 base-score formula and metric tables are identical; v3.1 only clarified the Roundup rounding algorithm's floating-point implementation, which does not change scores for the vectors in scope here), so no version-specific scoring logic is required to satisfy 'Support v3.0 and v3.1'. Out of scope for this task; mentioned only so a future reviewer doesn't mistake it for a gap.

---

## Backlog closed out — 2026-08-22

Both remaining rows are worked as far as they honestly go. Nothing in this document is now
"open pending someone doing the work"; what is left is either a product feature nobody has
asked for or a metric that cannot be moved without writing dishonest tests.

**NFR-07.1 / NFR-08.1 (coverage).** Statements **95.61**, lines **96.44** and functions
**95.31** all meet the PRD's 95%. Floors are 95/89/95/96, with functions set _at_ the
requirement. **Branches remain at 89.90 and are the one metric that will not reach 95.** Closing
that gap needs ~555 more covered branches. **Counted, not assumed** — there are 1,097 uncovered
branches in total, and by type they are:

| kind          | count | what they mostly are                                                      |
| ------------- | ----- | ------------------------------------------------------------------------- | --- | ----------------------------------- | --- | ------------------------------------ |
| `if`          | 476   | guards like `if (!this.db) return` on state the caller cannot produce     |
| `binary-expr` | 358   | `                                                                         |     | `/`??`fallback defaults —`?? []`, ` |     | 0`, `?? DEFAULT_SEVERITY_THRESHOLDS` |
| `cond-expr`   | 240   | overwhelmingly `error instanceof Error ? error.message : 'Unknown error'` |
| `switch`      | 23    | `default:` arms on closed unions                                          |

Reaching 95% means covering roughly half of those. A minority are genuinely reachable and worth
testing, but the bulk are fallback arms that only execute if a value the types forbid shows up at
runtime; the only ways to "cover" them are tests asserting something no user can trigger, or
deleting the guards. Recommend treating 95/89/95/96 as the standing bar and renegotiating the
branch target itself.

**NFR-08.4 (BDD breadth).** 82 -> **108 scenarios / 511 steps**. Two features were `@wip` on
justifications that named the wrong module (see `tests/bdd/README.md` for the corrected
reasons). Everything still excluded needs behaviour that does not exist:

| still `@wip`                         | needs                                                                                                                   |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| `hybrid-scanner.feature` (15)        | a real hybrid scanner; `hybridScanner.ts` is a declared stub, and the CLI injects `cli/scanner/localScanner.ts` instead |
| `update-scheduler.feature` (8 of 17) | calendar scheduling — time-of-day, day-of-week, next-run calculation, timezones. The app schedules by interval          |
| `nvd-database.feature` (9 of 26)     | filtered list queries by severity / CVSS range / date, caller-visible transactions, clear-all-data                      |
| `sbom-generator.feature` (10 of 18)  | UI-only concerns, plus CycloneDX XML output (`generateCycloneDX` throws for `format: 'xml'`)                            |
| `export-formats.feature` (1)         | jsPDF's default export is not a constructor under plain Node                                                            |
| `example.feature` (1)                | `@ui`; needs a live server                                                                                              |

**Two gaps found while closing these, neither tracked here before and neither fixed:**

1. **No test file is type-checked.** `tsconfig.spec.json` is absent from the root tsconfig's
   `references`, and `tsconfig.app.json` excludes test files, so `tsc --build` skips all of them.
   A correctly configured project (bundler resolution, DOM lib, path aliases, all test globs)
   reports **778 errors**, overwhelmingly incomplete mock shapes. Its own project.
2. **`retry: 2` in CI masks flakes.** Four separate intermittent failures were found locally in
   two days; none had ever failed CI. Worth deciding whether the retry should stay now that the
   causes behind it are fixed.
